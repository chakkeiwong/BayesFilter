# Phase 7 Serious Attempt-2 Repair Subplan

Date: 2026-07-13

Status: `EXECUTABLE_FOR_NO_RUNTIME_REPAIR_ONLY`

## Phase Objective

Repair the attempt-1 two-descriptor historical-retirement defect and redesign
attempt 2 to preserve every terminal attempt-1 artifact under versioned output
paths. Re-run no-runtime gates, materialize and review a new terminal proposal
manifest, then stop for a new exact human approval. Do not execute workers or
HMC under this subplan.

The scientific and numerical contract remains unchanged: the same fixed typed
transition, serious execution identity, target, seeds, topology, float64 Host
XLA/JIT route, counts, thresholds, and eight-hour cap.

## Entry Conditions

1. Attempt-1 authority and claim are permanently consumed.
2. Attempt-1 infrastructure failure and terminal manifest parse and preserve
   exact hashes recorded by the attempt-1 result.
3. The attempt-1 public result, log, and ordinary output-manifest reservations
   are empty mode-`0400` terminal evidence and remain untouched.
4. The original 2378-byte historical archive and archive manifest remain exact.
5. No Phase 7 worker, HMC process, progress artifact, or private sample exists.
6. No approval authorizes attempt 2.

## Skeptical Audit

| Risk | Control |
| --- | --- |
| Wrong baseline | Keep the proposal-bound transition and serious execution identities unchanged. |
| Proxy promoted | No-runtime tests and the failed reservation are engineering evidence only. |
| Hidden overwrite | Treat every attempt-1 path, including the empty former live result, as immutable terminal evidence. Use new versioned paths for every attempt-2 proposal, authority, claim, result, progress, sample, log, and manifest. |
| Incomplete root cause | Add a failing regression with two separately opened descriptors to the same old inode before changing the verifier. |
| Weak identity comparison | Replace descriptor-integer equality only with strict device/inode, constrained retired metadata, byte, archive, parent, and replacement-path checks. |
| Unauthorized retry | Attempt-2 runtime remains impossible until a new terminal proposal manifest passes review and receives a new exact human statement. |
| Scientific drift | Do not change target, transition, execution identity, thresholds, counts, seeds, topology, XLA/JIT, dtype, or interpretation. |

Audit verdict: `PASS_FOR_NO_RUNTIME_ATTEMPT2_REPAIR_ONLY`.

## Required Artifacts

- immutable attempt-1 result and exact-byte integrity verifier;
- two-descriptor regression demonstrating the pre-repair failure;
- corrected descriptor identity verifier and adversarial negative tests;
- attempt-2 versioned serious proposal, manifest, authority, claim, result,
  progress, log, private-sample, ordinary-manifest, and infrastructure paths;
- attempt-2 output protocol that creates its result exclusively and never
  replaces or mutates attempt-1 evidence;
- refreshed proposal/source inventory and exact hashes;
- bounded implementation review packet and review record;
- exact attempt-2 proposal/manifest review; and
- refreshed result/ledger/runbook/handoff stopped at the new approval boundary.

## Required Checks, Tests, And Reviews

1. Reproduce the current defect with two separately opened old-inode
   descriptors; prove failure occurs before the repair.
2. Test accepted same-inode/different-descriptor retirement plus rejection of
   different inode, changed bytes, unexpected links/metadata, changed archive,
   changed archive manifest, changed parent, wrong replacement, wrong owner,
   wrong mode, nonempty replacement, symlink, and hard-link cases.
3. Test that attempt-1 exact bytes, modes, link counts, schemas, embedded hashes,
   and terminal cross-links are required before any attempt-2 proposal write.
4. Test that all attempt-2 output paths are versioned, distinct, absent before
   proposal/claim, and created exclusively; no code path may replace or unlink
   an attempt-1 path.
5. Test authority isolation: attempt-1 approval/authority/claim cannot build,
   authorize, or launch attempt 2.
6. Run CPU-hidden serious-authority, controller, smoke-compatibility, and full
   nine-module migration gates; Python compilation; whitespace and
   authority/bypass scans; exact artifact parsing; and process absence.
7. Obtain fresh bounded read-only implementation review and separate exact
   attempt-2 proposal/manifest reviews. Review grants no runtime authority.

## Evidence Contract

| Field | Contract |
| --- | --- |
| Engineering question | Can the corrected, versioned attempt-2 launcher safely reach the unchanged serious controller without mutating attempt-1 evidence? |
| Baseline | Original reviewed Phase 7 scientific/runtime contract plus immutable attempt-1 failure evidence. |
| Primary pre-runtime criterion | Root-cause regression, adversarial identity/path tests, attempt-1 integrity, versioned output isolation, combined gates, and reviews pass. |
| Promotion veto | N/A before runtime; no convergence evidence is produced. |
| Continuation veto | Any attempt-1 drift, source/identity drift, unversioned collision, authority reuse, output mutation, invalid artifact, or unreviewed contract change. |
| Explanatory only | Test counts, hashes, and the attempt-1 failure stage. |
| Not concluded | No HMC feasibility, convergence, recovery, ranking, production/default/GPU readiness, Phase 8, NeuTra, or scientific validity. |

## Forbidden Claims And Actions

- Do not rerun or modify attempt 1.
- Do not restore the historical blocker to the old live pathname; the archive
  preserves it and the empty pathname is terminal attempt-1 evidence.
- Do not delete, rewrite, chmod, relink, or reuse any attempt-1 artifact.
- Do not reuse the consumed approval, authority, or claim.
- Do not run a worker, HMC transition, XLA compile transition, burn-in, or
  retained sampling during repair or proposal preparation.
- Do not change the fixed scientific/runtime contract.
- Do not run Phase 8 or NeuTra.

## Exact Next-Phase Handoff

After repair tests and reviews converge, materialize only a new versioned
attempt-2 proposal and terminal manifest. Reverify all inherited and attempt-1
evidence, review the exact artifacts, update the handoff with the new embedded
manifest hash, and stop for this exact future shape:

`I approve AUTHORIZE_PHASE7_TYPED_IDENTITY_TWO_WORKER_CPU_XLA_SERIOUS_ATTEMPT2 bound to Phase 7 attempt-2 authority proposal manifest <exact sha256:...>.`

No hash is valid until the reviewed terminal manifest exists.

## Stop Conditions

- Attempt-1 evidence cannot be verified exactly.
- The root cause does not reproduce with production-shaped descriptors.
- Repair requires mutation of attempt-1 evidence or a scientific/runtime
  contract change.
- Attempt-2 paths cannot be made collision-free and exclusively created.
- A worker or HMC transition occurs during repair.
- The same material review blocker remains after five repair rounds.
- The attempt-2 proposal passes: stop for a new exact human approval.
