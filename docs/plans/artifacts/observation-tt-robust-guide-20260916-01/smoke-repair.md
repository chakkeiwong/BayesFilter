Smoke-01 failed in reporting because one smoke repetition gives undefined MCSE
under the shared n-1 variance estimator; strict JSON rejected NaN. No numerical
claim is valid from this attempt. Use four repetitions as in the main plan.
The failure report and existing particles remain intact. The finalizer also
failed to serialize the result; manifest reconciled with an explicit failure
classification and conservative 150-second numerical accounting bound.
This localized harness repair does not change the scientific contract.
