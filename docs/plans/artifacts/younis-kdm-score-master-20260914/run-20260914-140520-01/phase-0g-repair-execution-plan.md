# Phase 0F selection repair and 0G continuation

Continue the authorized master after the 118 executed rows in the 0D--G slice. The attempted FD held-out run stopped in validation: the coordinator required a standard LEDH selection even for the separate FD estimator. The FD selection already binds LEDH controls, stencil, directions, step, device, dimensions and horizon. Repair the dispatch check, retain every prior artifact, and repeat selection and held-out plumbing at the repaired revision.

This continuation allocates at most 200 numerical rows, 300 attempts, two CPU process-hours and three GPU launches/device-hours, within the overall 12 CPU-hour, 8 GPU-hour and 12-GPU-launch envelope. Five GPU launches have been used. Stop for the first applicable overall or slice limit. Focused tests and compilation consume these allocations. Use fresh directories for retries. This allocation is an engineering slice; the large scientific comparison grid remains conditional on feasibility and precision planning.

Question: do the remaining proposal/model consumers, scoped selection, and reports execute with their stated mathematical targets? Compare against the exact Gaussian filter, UKF, bootstrap and adapted PF as applicable. Pass requires actual consumer-to-provider tests, complete tangents and normalizers, preserved partitions, finite covariances and complete requested-row accounting. Uncertain rankings, failure to improve a candidate, and small pilot sample sizes do not terminate implementation repairs. Invalid laws, omitted derivatives, unsupported support, nonfinite quantities, stale evidence or exhausted budget stop their dependent runs.

Defaults audit: retain the existing tiny N=8, d=2, T=3 CPU FP64/XLA mechanics setting only for wiring. It is a convenience choice, not a tuned scientific default. GPU checks use FP32/TF32/XLA and verified growth. Existing controls remain unpromoted baseline controls; FD selection must choose among explicitly frozen candidate designs and must not access held-out data. SGQF signed weights remain integration weights. Any KDM covariance extension must specify conditioning and persistent state; a responsibility-weighted covariance mark alone is not a complete mixture filter. Fixed twist is not fitted iAPF.

Skeptical audit: the corrected entry check must consume the FD-issued artifact rather than allow an unselected row. New method tests must reach the actual LEDH executor; covariance accuracy alone cannot establish score improvement. Initial/terminal twist normalization and changed resampling laws must be checked before interpreting a value. No smoothing method or degenerate-support claim is introduced. These conditions answer the active question and permit the next bounded runs.

Artifacts remain under this campaign root: new selection/held-out state files, exact source snapshots, test logs and phase result/refresh notes. They establish mechanics only. Any later statistical comparison needs its own fixed sample size, oracle-error criterion, uncertainty analysis and conditional heuristic table before launch.

## Persistent-mixture and fitted-twist consumer slice

Run phase-0e-new-provider-study.json:12rows, six methods, two final particle streams on one scalar affine dataset, N=16,T=3, CPU FP64/XLA. The scalar choice makes the full Gaussian terminal psi fit identifiable; N=16 exceeds its three-feature QR requirement. The two iterations, positive floor ratio.01, initial covariance4, and mixture within-component fraction.5 are explicit unpromoted mechanics hypotheses. Full fitted coefficients and per-iteration fit errors are saved; fitting uses common offline streams and final replicates use independent streams. No ridge is introduced into the fit. A rank or positive-precision failure invalidates that candidate, triggers an explicit fit repair, and does not support rejecting twisting generally.

The baseline ladder is Kalman oracle, bootstrap, locally adapted PF, shared canonical LEDH, persistent-mixture LEDH covariance, and fitted Gaussian-plus-floor psi-APF. Primary pass:all actual consumer rows finish with finite full six-parameter derivatives and proper target labels; all18consumer/coordinator/reporting tests have passed. Kalman distance and timing are explanatory only in this one-dataset fixture. No scientific ranking, particle-model score unbiasedness, high-dimensional fitted-iAPF claim, or default promotion follows. Preserve output in new-providers-cpu-01 and then fresh versioned GPU/selection directories. Exact commands and runtime settings are captured by the run manifest and logs.
