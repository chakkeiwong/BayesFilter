# Corrected Parameter-Authority Phase 45 Repair and Refresh

Date: 2026-08-26  
Source result: `docs/plans/bayesfilter-ssl-lstm-q20-parameter-authority-corrected-phase45-result-2026-08-26.md`  
Branch: `n512_replication_order_reproduced_but_support_mixed`  
Next version: `v2.8-support-envelope-diagnostic`

## Interpretation

The second independent N=512 bank reproduces the lower-than-bank-A ordering
for both transport residuals in all four arms. That relation is descriptive,
not a statistical ranking. N512-b is not uniformly below the historical old
comparator, and both N=512 banks retain material nonzero residuals. The result
therefore strengthens a finite-support/proposal explanation without proving it.

The target, theta measure, objective, trainer state, and canonical LEDH/HMC
boundaries remain valid as governed. Whitening remains vetoed; changing the
objective remains deferred.

## Repair decision

The next smallest discriminating artifact is a third independently generated
N=512-c bank followed by a read-only support/proposal envelope over authority,
A, B, C, N512-a, N512-b, and N512-c. This tests whether the two N=512 draws
share an unrepresentative support pattern. It does not retrain NeuTra, select a
checkpoint, change the proposal schedule, or change any promotion criterion.

## Required gates and stop rules

1. N512-c passes the existing theta-measure pilot contract and has distinct
   pilot/tensor hashes.
2. All seven M0 receipts are finite, `[N,4]`, target/status valid, and use the
   exact frozen target/protocol signatures.
3. The report recomputes proposal log density from the retained geometry and
   checks every stored proposal log against it; no caller-stamped density is
   accepted.
4. Pairwise support-envelope metrics are finite and retain raw values; no
   threshold is promoted from this diagnostic.
5. No bank is pooled, selected, used for training, or used to alter the
   whitening/objective/HMC gates.

An unavailable target/proposal, repeated unrepaired infrastructure failure,
or exhausted campaign budget is a continuation veto. A mixed or poor envelope
is a repair trigger, not proof that the target or research direction is wrong.
