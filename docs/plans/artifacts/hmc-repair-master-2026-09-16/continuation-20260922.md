# September 22 continuation records

The active entry point is the HMC repair master program in docs/plans.
M21-r2/confirmation-queue-progress.json and reconciliation-progress.json are
live local records for the persistent, bounded confirmation service. Committed
copies record publication-time progress, not a claim that future work finished.

Compact plans, commands, source manifests, decisions and test inventories are
versioned here. Raw tensors, numerical checkpoints, frozen package copies and
full worker logs remain in the same local campaign roots. They are deliberately
not added to Git. The new package snapshot is exactly reproducible by git
archive of f9c86f41ac44efc5e6a5a5220633073fce803431 and its recorded file hashes.
The official generated docs/main.pdf is rebuilt locally from the committed
LaTeX sources; its input/output hashes and inspected pages are in m24-r1.

Older r1 confirmation queues are stopped prelaunch planning history. Run only
the queue named by the active master; never relabel old-source evidence as
merged-source results. Every failed numerical attempt remains charged once.
A nonrejection, precision cap or completed command is not scientific closure.
