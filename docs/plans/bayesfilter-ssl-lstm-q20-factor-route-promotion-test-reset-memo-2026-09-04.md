# Factor-route promotion-test reset memo

Date: 2026-09-04  
Governing master: `docs/plans/bayesfilter-ssl-lstm-q20-tempered-rkl-transport-ensemble-master-program-2026-09-02.md`  
Completed plan: `docs/plans/bayesfilter-ssl-lstm-q20-factor-route-promotion-test-plan-2026-09-04.md`  
Terminal result: `docs/plans/bayesfilter-ssl-lstm-q20-factor-route-promotion-test-result-2026-09-04.md`  
Next plan: `docs/plans/bayesfilter-ssl-lstm-q20-factor-route-fresh-tuning-admission-plan-2026-09-04.md`

## State transition

This reset closes the bounded factor-route mechanics campaign and refreshes
the master authority.  The factor route moves from an untested opt-in
optimization to `DEFAULT_ELIGIBLE_PENDING_FRESH_TUNING`: it passed identity,
q=20 value/score/status parity, paired HMC transition parity, and short-chain
mechanics checks.  The strict route remains the numerical and repository
default authority.  No Phase 9B posterior run is reopened.

## Repairs and evidence

The bridge and fixed-beta adapter now bind the component's
`principal_sqrt_backend` identity.  A stale strict tuning handoff is rejected
by a factor adapter.  The P1 benchmark now accepts an explicit candidate
selector and enforces `CUDA_VISIBLE_DEVICES=0` for GPU evidence.  The first
all-candidate P1 failure and the first repaired multi-GPU-visible run remain
preserved as diagnostic attempts; the fresh `p1-gpu-attempt3/` center and
varied manifests are the authoritative GPU parity receipts.  The authoritative
P4 identity receipt is `p4-identity-attempt3/admission.json`, which includes
elapsed-time and CPU-device-policy fields.

P1, P2, P3, and P4 all pass their declared hard mechanics screens.  The
standalone P4 fixture is a stale-cross-backend rejection test; positive valid
handoff construction is covered by four measured-grid tuner regression tests.
Native TFP divergence telemetry is unavailable in this environment, so the result
does not report zero divergences; the frozen finite energy-error bound is the
available hard screen.  Timings, acceptance, short-chain movement, and
eigensystem counts remain descriptive.

## Remaining decision boundary

The mechanics result does not supply a fresh factor-specific tuning artifact,
six-scope replay, untouched downstream validation, R-hat/ESS evidence,
whitening evidence, or mode-discovery evidence.  Those are required by the
separate fresh-tuning admission plan.  That plan has a new output root,
backend-bound identities, a canary before the 10,000-second full campaign,
and stale-artifact rejection.  It is pending a new campaign authorization;
this reset does not silently authorize it.

## Reproducibility

All authoritative artifacts are under
`docs/plans/artifacts/ssl-lstm-q20-factor-route-promotion-2026-09-04/` and
record commands, Git/worktree metadata, environment, device policy, memory
growth, XLA/TF32 settings, seeds, source hashes, and wall time.  Prior
attempts are not overwritten or relabeled.
