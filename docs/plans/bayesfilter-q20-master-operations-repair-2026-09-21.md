# q20 master continuity and command approvals

September 22 current state: recovery phases R1–R6 are implemented and checked;
`job.json` now names running `campaign-04` and repaired isolated source.
Read the [execution review](bayesfilter-q20-recovery-execution-result-2026-09-22.md).
The older statuses below are historical. Fixed control commands remain unchanged.

Current status at September 21, 21:24: execution is paused. Read the
[pricing stop diagnosis](bayesfilter-q20-pricing-stop-diagnosis-2026-09-21.md)
before restarting; the repeated resource/timeout path and initial ensemble cost
reservation both require repair. About 48.78 campaign hours remain.

September 21, 16:56 update: active execution continues in `campaign-02` under
the [ensemble pricing timeout repair](bayesfilter-q20-ensemble-pricing-continuation-2026-09-21.md).
The first continuation completed plain tuning without a verified member, then
paused on the ensemble pricing attempt's 20-minute cap. Its ledger is preserved.
One measured pricing retry now runs with the same numerical source, scientific
protocol, total allowance and deadline. The fixed control commands follow the
current campaign named in `job.json`.

The owner requested an executable phase/repair/refresh cycle and fewer approval
interruptions. The scientific question, q20 posterior, permitted NeuTra methods,
source snapshot, evidence requirements, September 25 18:00 Shanghai deadline and
total compute balances remain those of the active campaign.

## Findings and scope

The forward master runs qualification, pricing, training, tuning, sampling and
reference assessment. The current run has not stopped: training completed and
tuning is active. Earlier ledger stops include a bootstrap numerical failure,
cost deferral and over-conservative cost reservation. They were not all security
denials. The original coordinator also caps attempts at one, returns on GPU
unavailability, and has no durable phase-level next-action record. Its eight-hour
tuning allocation is independent of the service's final calendar deadline.

The first deadline continuation addressed only a settled beta-one tuning budget
pause. It has no general resource-wait loop or bounded checkpoint recovery and
cannot resume its own existing successor. Calling it a complete autonomous
repair master would be wrong. Arbitrary new numerical/code defects still require
an agent to inspect and implement a repair; a scheduler cannot certify an
unimplemented repair by retrying it.

The installed route doctor reports READY_USER_APPROVAL, with both direct and
shell-wrapped canonical probe rules allowed. Local allow does not override a
managed reviewer. The official documentation fetch was attempted for Codex rules
and permissions, including trusted execution, and returned HTTP 403. Therefore
use the installed CLI's actual execpolicy parser/checker to validate the proposed
rules, without claiming that an unread official page established behavior.

## Repair

Replace the waiting one-shot continuation with a tested supervisor that keeps
the existing numerical source unchanged. Preserve the old observer and its
launch receipts as history. Never stop the active numerical master to install
this operational repair.

After each attempted phase, classify completion, resource wait, resumable
infrastructure interruption, allocation exhaustion, numerical rejection, or
unknown failure. Append the evidence path, actual cost, decision and next action
to one phase journal and refresh one current next-phase record. Continue the
existing master after a passed phase. Retry an available matching checkpoint for
a classified resource/infrastructure interruption at most once per phase, within
the existing cumulative stage/campaign funds and shared two-hour repair hold.
The single extra attempt is a convenience bound against persistent failure,
not scientific evidence. GPU waits recheck after 30 seconds (inherited observer
cadence) and consume actual probe/worker time; they do not spend a new allowance.
Do not retry unknown code exceptions, corrupted identities, scientific vetoes
or exhausted money/time/work-unit budgets as though they were transient errors.
Emit an explicit agent-repair task or user-budget boundary instead.

The owner's calendar extension permits the already planned one-time tuning
reallocation, preserving downstream funds. Recovery must keep that cumulative
ceiling, spent counters, exact numerical evidence and verification rules across
retries. A resource pause must not nominate the ensemble fallback. A completed
candidate failure retains the original master branch.

Expose fixed `status` and `ensure` commands through
`scripts/q20_campaign_control.py`. `ensure` is idempotent, preserves an active
supervisor, and installs/restarts only this q20 supervisor under the recorded
job and deadline. It has no arbitrary command, path, budget or environment
overrides. Runtime uses a versioned copy of the reviewed supervisor. Record both
its source hash and the unchanged numerical snapshot. Set memory growth and
thread environment inside the launcher so callers need no shell assignments,
heredocs, redirections, or changing timestamp arguments.

Prepare direct argv and exact `/bin/bash -lc` and `/bin/bash -c` allow rules for
only those fixed commands. Validate positive and negative examples with the
installed `codex execpolicy check`. Ask the platform once to install this small
rule block in the user's rules file and activate the prepared supervisor. Do not
relax the sandbox, change reviewer/gateway settings, grant arbitrary Python or
shell access, or remove unrelated existing rules. The user has authorized the
scope; writing outside the workspace still needs tool permission.

## Skeptical audit and checks

The incorrect baseline would be to attribute every pause to permissions. The
read-only route report and campaign ledgers distinguish those layers. A service
being active does not establish posterior convergence or even a valid next
phase. Checkpoint corruption and candidate rejection must never become retry
triggers; resource failure must never become method rejection. Source bindings
must remain consistent despite concurrent unrelated workspace edits. Every
attempt must still consume the existing budget. These are the primary pass/fail
criteria for this engineering repair.

Before activation, test actual tiny CPU checkpoint continuation through fresh
verification and exact retained-draw parity; simulated resource wait and retry;
unknown failure, numerical/budget stop, retry exhaustion and phase refresh;
duplicate-launch prevention; and exact rule matching including shell chaining
negative cases. CPU checks explicitly hide GPUs and support engineering only.
Audit the actual saved stage request identities. Existing scientific thresholds
and production numerical code are unchanged.

Artifacts: `docs/plans/artifacts/q20-master-operations-2026-09-21/`. Preserve
the route report, proposed rules, rule-check output, focused test logs, launch
receipt and source hashes. Runtime phase decisions live with the continuation
campaign. The implementation is acceptable when known recoverable conditions
advance without new human requests and all stated real stop conditions remain
enforced. A result note must state remaining limits; it must not claim automatic
repair of arbitrary future defects.

## Verification and activation decision

Twenty distinct latest checks pass: nine deadline/checkpoint checks, including
the actual CPU worker entry and exact retained-draw parity, plus eleven phase
recovery and control checks. The latter cover resource waiting without consuming
the single infrastructure retry, repeated failure, unknown error, budget stop,
shared invalidity, full plain-NeuTra progression and the existing ensemble branch
after a completed candidate failure. One retry-count bug found by these checks
was repaired. An expanded orchestration fixture initially omitted the real
campaign's default stage-limit behavior; that fixture was corrected. Logs retain
both failures and passing follow-ups.

The proposed rule block passes six intended-command cases and five negative
cases, including appended shell commands. Those negative tests isolate the new
block; existing unrelated broad rules are preserved. The actual saved campaign
qualification, pricing, training and tuning request identities still match,
and all 490 numerical source files remain unchanged. The reviewed supervisor is
copied to the versioned artifact root, independently checksummed in `job.json`.

Audit verdict: activate the bounded operational repair. It improves known
resource/checkpoint continuation and records phase decisions; it does not claim
to repair arbitrary numerical or source defects. The final installation command
adds the reviewed block to `~/.codex/rules/default.rules`, preserving a backup,
verifies effective local decisions, and calls the exact fixed `ensure` command.
Only the waiting observer is eligible for replacement; the active GPU master is
preserved. New numerical work still requires the same scientific prerequisites.

## Installed state and operating commands

At September 21, 02:21 Asia/Shanghai, six narrow rules were installed in
`/home/ubuntu/.codex/rules/default.rules`. The original file was backed up as
`default.rules.before-q20-control-20260921`; unrelated rules were preserved.
Effective combined-rule checks allow all six intended forms. The direct `status`
and `ensure` commands were then executed successfully with trusted tool access.
The second `ensure` returned `already_running` and preserved the same service PID.

Use exactly these commands, without added shell environment assignments,
redirections, command chaining or a changing launch string:

```bash
/home/ubuntu/anaconda3/envs/tfgpu/bin/python /home/ubuntu/python/BayesFilter/scripts/q20_campaign_control.py status
/home/ubuntu/anaconda3/envs/tfgpu/bin/python /home/ubuntu/python/BayesFilter/scripts/q20_campaign_control.py ensure
```

The active supervisor is `bayesfilter-q20-master-operations-20260921-01.service`,
PID 864606. The numerical parent remains PID 93403, continuously active since
September 20 at 17:22:12. It is still in beta-one tuning. The new supervisor is
waiting for its settled budget/resource pause; it has not launched a competing
worker. About 60.4 campaign hours remain. Its hard service bound and stop grace
end before September 25 at 18:00 Asia/Shanghai.

After takeover, the successor's `phase-events.jsonl` preserves phase decisions
and `next-phase.json` identifies current action, evidence, spend and deadline.
During the existing run, `status.json` records the parent phase and takeover
action. The original numerical master's in-memory loop is not hot-patched.

| Decision | Primary criterion status | Veto status | Main uncertainty | Next justified action | Not concluded |
| --- | --- | --- | --- | --- | --- |
| Activate repaired supervisor and fixed command rules | Twenty engineering checks, eleven rule checks and actual request replay pass | Unchanged numerical source; no new scientific admission | A later unclassified code/numerical defect may need an agent repair | Continue tuning; take over the settled pause; then verified sampling/reference phases | Arbitrary self-repair, convergence and immunity from managed approval controls |

The remaining intentional stops are exhausted funds/deadline/work limits,
unclassified failures or identity corruption needing agent investigation, and
scientific outcomes requiring the existing repair branch or new owner direction.
Code repair within the authorized scope does not itself require a new human
approval. Local rules cannot override a managed reviewer or broader platform
controls. No reviewer, gateway, sandbox or credential configuration was changed.
