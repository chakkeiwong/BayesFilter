# Renewal of dense original-source comparisons

The complete dense fitter authority must execute the original numerical
dependency closure. Earlier comparisons used the original public and factor
modules but the current dense-score module. The repaired helper pins all three
to 3582b4ac. Preserve the earlier results as partial-reference evidence.

Renew the existing prepared-controller, validated-fitting and attempt tests on
CPU and GPU. Compare complete records, errors, callbacks, archives, changed
operands, frozen derivatives and owner collection using the unchanged tests and
tolerances. This is an authority repair, not a numerical or selection change.
Do not apply the pending clipping-count or condition-reporting proposals.

Use the stable campaign runner with `matrix --stage tests --test-batch
dense_reference_renewal_cpu --test-timeout-seconds 300` and the corresponding
GPU batch. Reserve at most 68 workers / 14400 charged seconds within the existing
32 CPU / 52 GPU process-hour caps. There are 32 groups per backend; the remaining
four workers allow localized harness repairs. Every launch writes a fresh
numbered directory in the master campaign root with exact source hashes,
commands, device/environment and test evidence. Sources stay frozen during each
matrix. GPU uses the established automatic non-desktop selector and verified
memory growth. CPU is an explicitly hidden-device reference.

A complete original-record mismatch vetoes that unit's qualification and stops
the matrix for attribution; it does not invalidate unrelated earlier tests.
Known nearly singular condition-reporting mismatches remain open and may not be
waived or repeatedly retried unchanged. Stop for invalid provenance, scope change
or exhausted budget. Fixture passes cannot establish actual DZ5 integration,
public default readiness, clean costs or scientific admission.

Review: copying only the public source would reproduce the same mixed-version
defect. The helper binds the original dense-score and factor callables, records
all three hashes, and inspected numerical dependencies resolve to TensorFlow,
TFP and standard-library code. Original diagnostic NumPy is kept solely in the
isolated test authority. The unchanged mass-artifact type does not participate
in these records. The seeded CPU renewal already passed 03538--03547; this unit
covers the older prepared-cloud and fitting authorities instead of counting
those seeded passes as their replacement.
