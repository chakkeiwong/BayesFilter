# Phase 8 Predictive Power Repair Native Review

Date: 2026-07-17

Verdict: `AGREE_GPU_SMOKE_ONLY`

The repair preserves the failed 448-draw nomination as the exact baseline and
uses a separate source-bound runner. It reads no retained G/H archive or
confirmation forecast. Pilot, passing smoke, failed nomination, and failed
runner identities are hard-bound.

The three prospective arms isolate interpretable mechanisms on shared
TensorFlow draws: 1984-draw precision only; precision plus midpoint margins;
and the same midpoint margins with an intersection-union TOST equivalence
bound. The midpoint margins tighten the mean equivalence region from `0.15` to
`0.10` and place both mean/log-variance margins halfway between zero and the
approved material anchors.

The Arm D mathematical split is valid for its declared roles. The all-features
equivalence null is a union of component non-equivalence nulls; rejecting only
when every level-`0.03` component TOST rejects controls the union null without
a multiplicity adjustment. The any-feature material claim is a union of
component detections and therefore continues to use the simultaneous
Bonferroni interval. Coverage is also assessed with the Bonferroni interval.

The two-arm influence scaling remains correct: concatenating `2*left` and
`-2*right` across eight pseudo-chains, followed by division by eight in the
long-run covariance helper, yields the sum of independent four-chain mean
covariances. B/C/D reuse the same generated paths and inferential tensors
within each family/replication. Selection order and futility logic are
prospective, incomplete runs cannot nominate, and smoke mode leaves selection
fields null.

Focused checks passed: `82` power-repair/controlled/predictive tests; Python
compilation; and `git diff --check`. The exact `[4,1984,2,10]` two-family GPU
smoke is the next admissible action. It cannot nominate an arm/tolerance,
authorize HMC acquisition, open validation, or support a G/H predictive claim.

## Nomination Review

Verdict: `AGREE_NOMINATION_ONLY`

The smoke passed every execution and boundary gate in `14.8747` seconds. Its
one-replication B/C/D statuses remain descriptive and are not used to alter
the prospective arm order, family set, thresholds, tolerance ladder, or fresh
seed. The material runner now hard-binds both the smoke receipt and the exact
historical runner hash recorded by that receipt.

All 13 families and 20 fresh replications are required for nomination. The
only early stop is mathematical futility; a successful nomination cannot stop
early. The `2400`-second cap is conservative relative to the exact-shape smoke
and covers the worst-case 260 family-replication evaluations. Focused tests
pass (`83`), including exact binding, null smoke selection, shared evidence,
selection ordering, incomplete-run rejection, and futility arithmetic.

No unresolved material issue remains for the bounded nomination. A pass would
show only that one controlled design remains viable for fresh validation; it
would not authorize HMC acquisition, validate the design, compare G/H, or
establish posterior correctness.
