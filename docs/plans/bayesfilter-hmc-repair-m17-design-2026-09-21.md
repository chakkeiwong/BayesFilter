# M17: difficult geometry, route composition and matched references

Refreshed after M16's terminal reconciliation. Seven independent route tests
passed under the engineering note. The existing ceilings are 12,000 CPU-reference
and 10,000 GPU worker seconds.

## Question and evidence contract

Does the repaired public procedure preserve candidate and target identity,
exclude adaptation and discarded draws, and report honest posterior outcomes
when geometry, transformations and proposal fields change? Assess posterior
quantities only after selecting the first verified candidate by identity.
Preserve every verified sibling as unassessed where the numerical allocation
funds one member. A posterior failure never removes or repairs a tuning member.

The engineering baselines are the already-passing public CPU integrations and
M14's short public matrix. Run fresh complete fits with longer posterior caps;
compare model quantities with independently coded analytic laws or pinned
posteriordb references. Descriptive posterior differences cannot rank algorithms
or coordinate choices across these single-fit cells. The primary pass criteria
are exact binding, retained-member reload, candidate inventories, independent
verification, correct model coordinates and warmup exclusion. Numerical
corruption, a mismatched reference or missing required provenance veto the
affected comparison and require repair. Empty candidate sets, missed modes,
and warmup/precision caps are scientific outcomes with explicit denominators.

Ordinary/fixed-transport posterior checks use inherited modern R-hat and ESS
requirements and lugsail mean precision plus median precision. Request absolute
MCSE .05, an explicit fixture-scale development hypothesis, not a universal
accuracy criterion. Warmup minimum 2000, recent window 1000, chunks 500, maximum
10,000; retained minimum 1000, chunks 500, maximum 10,000. The cauchy cell requests
only quantiles because its mean and variance do not exist. Exact truths and
reference draws are assessor-only and never enter tuning or member selection.

## Model and route matrix

Six geometry cells, on both devices:

| Cell | Route and model | Intended failure mechanism |
| --- | --- | --- |
| Rotated Gaussian | Ordinary, condition 100, angle .6 | Scale/rotation and metric evidence |
| Centered funnel | Ordinary, scale 3 | Varying curvature and tail exploration |
| Noncentered funnel | Ordinary, same model-coordinate law | Coordinate mapping and posterior tail diagnostics |
| Cauchy | Ordinary, independent two-dimensional Cauchy | Infinite-moment safeguards |
| Mixture, one-mode starts | Prepared identity metric, separation 5, left weight .3 | Locally favorable diagnostics with missed mass |
| Mixture, dispersed modes | Same prepared route, explicit four-chain mode bank | Persistent between-mode disagreement |

The mixture indicator 1[x<0] participates in posterior checks and mean
precision. Its exact expectation is .3 Phi(5) + .7 Phi(-5). A constant indicator
cannot establish precision. Compare observed global mass with this value after
sampling. The two starts are distinct development cases, not a statistically
powered paired experiment. A passed check cannot certify undiscovered modes.

Four transport cells on both devices: affine Gaussian, affine banana (bend .5),
fixed dense-IAF banana, and fixed dense-IAF Dirichlet with concentrations
[2,3,4]. The nonlinear map uses the existing nonzero-weight codec fixture,
whose value/Jacobian have independent tests. It is frozen before tuning and
is not trained. No transport-quality or learned-NeuTra claim follows. Preserve
the full model simplex coordinates in the last case. These routes use the
public fixed-transport tuner and exact transformed density including Jacobian.

Position-field cells use .8 times the exact potential gradient and the exact
endpoint potential, with identity parameter scales. Test Gaussian and
beta-binomial on GPU; test those plus LGSSM location and banana on CPU.
Use four supplied chains, public typed dispatch, pause/reload/resume, and
independent measurement/verification. These are conditional mechanics tests.
They must never acquire exact-score retained-member authority. The chosen
force is a declared perturbation, not a purported exact gradient.

CPU additionally runs a Student-t(df=5) ordinary fit to exercise a finite-variance
heavy tail separately from the Cauchy quantile case.

## Search, source and allocations

Ordinary geometry uses standard preparation, optional finite-window metric
evidence, startup backoff 20, metric probe 16, recovery 3, expansion 1, and the
native broad L search. These are M9--M14 hypotheses, not promoted defaults.
Prepared/transport routes use the broad L grid [3,5,9,13,18,25], initial epsilon
.5 with candidate-specific pilots, inherited evidence rungs and acceptance
bands. No geometry or epsilon is transferred between coordinate systems.
Position-field uses the same broad L grid with its typed preparation and the
existing overall [.65,.75]/per-chain [.55,.85] descriptive compatibility policy.

GPU ceilings: six geometry cells at 800 seconds (4800), four transports at 550
(2200), two position fields at 200 (400), and two matched-reference fits at
1000 (2000), leaving 600 for a localized repair. The public GPU pilots took
roughly 420--520 seconds for search; M13's regression fits took 672--762 seconds.
These caps include a measured margin but are not promises of completion.

CPU ceilings: six geometry cells at 600 (3600), four transports at 500 (2000),
two matched references at 900 (1800), four fields at 100 (400), Student-t at
600, existing integration preparation at 900, analysis at 900, and repair
reserve 1800, totaling 12,000. CPU paths deliberately hide GPUs and are
non-XLA reference exceptions. Serious GPU paths use trusted TF/TFP/XLA and
verified memory growth. Up to eight CPU or two GPU workers may run concurrently;
inspect available memory and device usage first. Charge tests and failed
attempts once. Resource retries preserve the source, data, seeds and criteria.

Root seed 2026092197 has separate device/cell/fit domains. Use one frozen
reviewed source at the phase refresh and unique output directories under
`artifacts/hmc-repair-master-2026-09-16/m17-r1/`. Preserve exact resolved JSON
designs, commands, environment/device records, inputs, source checksums,
candidate sets, verification receipts, exported members and posterior chunks.

## Matched reference cells and unavailable consumer evidence

Use pinned posteriordb commit 5545a1dd07ae297c36edecbcd82aa49097b4c385,
the noncentered eight-schools and sblrc-blr targets. Recheck laws, observed data,
coordinate Jacobians and each input against the pinned Git tree. Eight-schools
starts at zero in unconstrained coordinates. Regression uses the already
checked data-only stationary point; no reference draw initializes it and no
reference covariance supplies its mass. Use serious finite-window preparation
with the same startup/probe/recovery options.

The two posteriordb cells retain M8/M13's case-specific absolute mean MCSE
threshold .1 for their heterogeneous named quantities. This differs from the
.05 threshold of the small analytic model matrix above; neither is a general
BayesFilter default. Their actual accuracy screen is the separately declared
.25-reference-SD margin with uncertainty in both samples. Thus a mean-precision
pass alone cannot establish reference agreement.

After posterior sampling, compare each mean with ten saved thinned Stan chains.
Retain their finite-chain uncertainty: combine independent lugsail MCSEs and
use the inherited Bonferroni normal radius at family alpha .05. The M8/M13
development equivalence screen requires absolute difference plus this radius
to be at most .25 of the reference posterior SD, with separate reference and
BayesFilter posterior checks. This is a checked case-specific screen, not proof
of exact equality or nominal interval coverage. Failure is preserved.

The original MacroFinance typed-bridge memo is an API request whose engineering
repair is already tested. Recent daily/macro fit notes concern a different
campaign and do not supply an independently matched posterior reference for
the original case. Do not substitute generic regression, synthetic truth, or
those notes for that missing reference. Record this cell as unavailable unless
the exact target/data/coordinate and reference bundle can be bound locally.
Its absence does not stop the independent matrix and does not authorize
external messaging or spending MacroFinance's separate compute budget.

## Skeptical review for final refresh

Avoid a false baseline: a short successful tuning screen is not posterior
accuracy evidence. Keep ordinary automatic preparation distinct from supplied
identity geometry and from frozen transport. Do not call the force perturbation
an exact score. Heavy-tail moments and transformed coordinate names must match
their target laws. One pair of start regimes cannot establish comparative
mixing rates. The finite-mode fixture can expose a missed known mode, but cannot
prove unknown-mode discovery. Single-fit reference comparisons are development
evidence, with uncertainty in both chains. Time caps are resource limits;
posterior caps remain outcomes and never prompt seed replacement or threshold
relaxation. Complete this review using M15/M16 results, current measured costs
and the remaining ledger before launching the matrix.

At exit, reconcile every cell, classify each failure, repair any localized
implementation defect, and refresh M18's exact numerical parity and terminal
documentation/integration audit. Candidate rejection does not end the program.

### Completed skeptical refresh

M16 completed 576 fresh numerical boundary searches and 128 three-arm sequential
experiments per device, without invalid evidence. The reversed-ratio defect
meets its power criterion; three null-control intervals remain too wide for
the development size screen. Analytic controls show M15's 32-dataset/3-rank
SBC design has weak sensitivity to quarter/half-SD shifts. Accordingly M17
tests mechanisms and matched quantities without claiming that prior SBC
nonrejection establishes calibration. Single-cell outcomes remain descriptive.

The source remains M16's frozen `m16-r1/source-r1`, identity
`b5d2d662171ed61dab32a681693400abc1285ec5c13981a6118d1ce33624b54a`.
The only current-package difference is the later checkpoint-hash optimization;
M18 will test it separately. The matrix JSONs resolve to 6200 CPU and 7000 GPU
seconds; additional fields/references fit the allocations above. M16 leaves
86417.5601492732 CPU and 23255.818866750866 GPU seconds. No scientific threshold,
seed, target or prior is changed by this refresh. The exact MacroFinance
reference remains unavailable for its cell alone. The inspected scripts
validate source/device provenance and pinned input bytes. This design passes
the skeptical audit for bounded development execution.

Run the resolved `matrix-cpu_reference.json` and `matrix-gpu.json` through
`python -m bayesfilter.testing.inference_validation run` from that frozen
source, writing respectively `matrix-cpu-r1` and `matrix-gpu-r1` beneath M17.
Use eight CPU or two GPU workers. The separately metered `run_position_field.py`
and `run_posteriordb.py` take explicit source/device/case/seconds. Manifests
preserve the fully expanded commands. Resource failures trigger a same-source
bounded continuation or preserved incomplete result, never replacement seeds.

### Localized position-field harness repair

The four CPU field cells complete their conditional/resume checks, but every
observation is a pilot: the copied fixture sets `verification_repair_rounds=0`,
which disables same-L repair, so high pilot acceptance cannot propose a new
pair. This does not answer the planned measurement/verification question.
Preserve those pilot-only results and run a separate r2 design with three
bounded same-L repairs, using the same supplied targets, force, potential,
start policy and root seeds. Three is a development search allowance, not a
default. Keep the acceptance/health rules and all conditional authority limits.
The helper is copied to `run_position_field_r2.py` before execution so existing
manifests retain their script bytes. Allocate 400 CPU and 400 GPU seconds from
the existing local repair reserves; original GPU cells remain in the original
design for matched device coverage. Explicitly report executed stages and
whether any pair actually reaches fresh verification. A failure to find such
a pair remains uncovered verification, not an engineering pass for that stage.
