# Complete public sequential execution

The repair branch replaces the public sequential initializer's outer Python
controller with one XLA invocation. Localization, candidate selection, exact
row accounting, refinement, terminal geometry, movement summaries and mass
construction execute together. Host work validates configuration/input shapes
and serializes completed results. Progress follows the original values/order
after the numerical call; independent process deadlines remain necessary for
consumers that previously relied on live callbacks.

The numerical authority remains original `3582b4ac`. Tests compare complete
accepted/rejected payloads, exact discrete decisions, full target order/counts
and progress records at unchanged tolerances. The public call-chain check
inspects the owner actually used by the endpoint and its `_XlaMustCompile`
attribute. Changed inputs reuse the same trace; output preparation preserves
the original disconnected external derivative boundary. Unsupported callbacks
have no eager retry. Buffered locator overflow vetoes later target calls and
raises before partial progress is delivered.

CPU qualification passes 23 public checks in 02887–02905 and 56 existing
consumer/boundary checks in 02886, 02906–02911 and 02913. The 127 policy checks
pass in 02914. All23 public GPU checks pass in02915--02933 and all56 GPU
consumer checks pass in02934--02941. The actual block-coordinate caller passes
43 existing checks on each device in02942/02943. Its outer execution is a
separate open repair. The qualification receipt is
`artifacts/filter-gradient-repair-20260917/sequential-public-qualification-02943.json`
with its saved analyzer; runtime sources agree across these cohorts. The earlier internal controller has
23 passing checks on each device, including GPU 02854–02872. All commands,
source hashes, versions, exact fixtures, GPU UUID/memory policy, time and logs
are in numbered run manifests under the shared campaign artifact root.

Two legacy test fixtures injected values through helpers the new public
controller no longer calls. Their injections now occur at the executed native
fit boundaries. The first preserves identity precision and transactional
score-policy assertions. The second preserves rejected first-factor/usable
second-factor geometry, including the old fixture's absent cloud incumbent,
and uses the actual native factor-admission calculation. Assertions, target
evaluations and scientific criteria are unchanged.

The boundary group initially registered the retired `cfbc32d2` intermediate
reference. Run 02912 preserves its known eigensystem discrepancy and three
passing frozen-locator cases. The corrected group uses the existing original
authority adapter, with the same fixture and materializer; all four checks
pass in 02913. An exact-path regression prevents reintroducing that stale
reference. CPU evidence spans this recorded harness-only correction; the final
E6 suite still requires its own frozen-source qualification.

Review also found accidentally copied registration blocks in the runner's
explanatory-only list. Removing them restores every native/public sequential
correctness group as mandatory. The regression now requires textual reasons
for explanatory groups and explicit inclusion of both device matrices in the
terminal gate. No terminal gate was passed using the erroneous list.

The partial static guard now examines the entire public sequential module.
Its seven new exact iteration exceptions cover four configuration validations,
one completed-result validation and two JSON serializers. The numerical
controller has no Python-loop or NumPy exemption. Overall coverage remains
partial at 223 sources / 1325 exact exceptions.

| Decision | Primary criterion | Veto status | Main uncertainty | Next action | Nonclaim |
| --- | --- | --- | --- | --- | --- |
| Qualify the public endpoint |23 original/boundary,56 sequential/factor and43 block consumer checks pass on each device | Stale-reference failure preserved and corrected | External consumers/costs remain | Continue scoped ownership and ordered blocks | Not whole-repository completion |
| Keep the broader campaign open | Public endpoint invokes one compiled controller | Initializer rounding, nested callback ownership and external supervision remain | Complete cold/warm costs and native residency | Continue E3–E6 | No HMC or scientific readiness claim |

Post-run review: component existence does not qualify external consumers, and
deferred progress cannot enforce live deadlines. Root-only cache eviction still
leaves callback-bearing dependency caches alive. The separate posterior memory
study shows why Python collection and native executable residency must be
tested separately. Sequential full-call costs, ordered blocks, E4/E5 consumers,
terminal F01–F20 dispositions and main integration remain open.

The integration tranche used60/64 workers and2400.59/7200 charged seconds.
Focused Ruff, whitespace and223-source/1325-exception guard checks pass.
