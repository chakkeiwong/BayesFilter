# Uniform-cloud enclosing XLA repair

Continue the approved execution repair after dense-condition qualification.
The original `3582b4ac` uniform-cloud initializer, with its trust solver in graph
mode, is the numerical authority. Preserve Philox draws, ordered train/check
clouds, exact selection, replay, factor checks, trust-radius recurrence, statuses,
complete histories and physical-row accounting. Only the approved deficient-rank
condition definition may differ. No optimizer, threshold, model or seed changes.

Reuse the existing paired controller's round recurrence with a static choice of
probe generator and fitter. Share ordered partition evaluation; generate uniform
double draws with the repository Philox bit conversion so XLA keeps the original
seed-to-draw map. Bind dimensions/capacities/configuration at construction and
keep seed, center, scale and initial evidence as runtime operands. Preserve the
single-entry callback-identity cache. Reporting only materializes completed
histories; no host numerical loop may remain in uniform refinement.

Skeptical review: ordinary XLA uniform-double generation changes the stream;
reuse alone is not evidence of parity. Test frozen original draws and complete
records before public wiring. The shared paired controller must retain its own
tests. Invalid batches must preserve which target calls ran and which physical
rows were charged. Changed inputs must preserve one trace and unchanged HLO
without captured input values. Original XLA trust errors are not an authority
for candidate acceptance. Compilation can be expensive even when warm calls
are faster; record cold and warm costs separately. This is primary-agent review.

Require CPU/GPU D1/D3/D5 original comparisons across accepted, moving,
nonquadratic, rejected-model, failed replay, invalid initial/train/check/proposal,
round-limit and initial-evidence cases. Check partial final batches, negative
seeds, nondefault cloud size/width and configuration identity. Check complete
original numerical records at `1e-10` and exact decisions/shapes/counts. Keep the
approved deficient-rank normalization explicit, archiving original raw ratios.
Require stable signatures, loop bodies, absence of Python callbacks, single
trace, unchanged HLO/runtime operands, and downstream public consumer checks.

Dense numerical dependency costs must pass before public XLA integration.
The first fresh-process cost matrix identifies a D5 warm-time trigger relative
to the original specialized solver. Preserve the trigger and qualify its repeat
range and complete-controller impact before public integration. A diagnostic
native controller can be implemented and tested while this remains open; that
does not enable or admit the public default. Existing paired/trust eigensolver
errors also justify explicit dense spectral stress fixtures (zero, indefinite,
near-repeated, extreme scale and near-singular precision) against both the
original graph and independent eigenvalues before enclosing the dense fitter.
After controller checks, compare original/graph/XLA whole public refinement at
D3/D5 with three fresh processes per arm/device, fixed inputs and synchronized
all-output materialization. Record host RSS/VmHWM and GPU allocator current/peak,
compile/first-call cost, warm times and changed-input behavior. Investigate the
existing >20% warm-time or >256 MiB host-memory triggers without relaxing them.
Use bounded repeated calls to distinguish compilation retention from continued
warm growth. These descriptive measurements support no statistical ranking or
general leak-freedom claim.

Use the same campaign driver and output root, one numerical worker at a time,
300-second focused workers (900 seconds only when the registered bounded group
requires it), GPU3 idle preflight and verified memory growth. The shared caps
remain 32 CPU/52 GPU process-hours. Preserve failures and repair locally under
the same scope; stop launches for contention, exhausted budget or invalid source
provenance. A failed parity/cost check blocks integration, not further repair.
Runtime/tests/driver stay frozen during workers.

Passing this stage does not complete multistart composition, the separate
iterative/block/sequential controllers, external watchdog/DZ5 qualification or
the F01--F20 terminal merge gates.

## Spectral boundary investigation

Runs02173 CPU and02174 GPU3 each pass10 and fail4 new boundary cases.
Near-identity D3/D5 XLA raw eigenvalues differ from the original graph by
6.19e-9/7.45e-9. Reuse the existing residual-refined eigensystem already qualified
for paired fitting; this solves the same eigenproblem and retains TensorFlow's
derivative convention. Qualify full fields, consumers, derivatives and costs
after the localized repair. Do not change any tolerance.

The other two failures are huge-scale diagonal fits: tiny relative matrix
differences (about1.2e-16--3.6e-16 of a3e150 matrix on CPU) occur in nominally
zero off-diagonal entries whose original roundoff is itself about1e133--1e134.
Entrywise1e-10 comparison fails there. Preserve this distinct failure and its
raw records; it is not evidence that the near-identity eigenvalue defect is
harmless, and the factor-output approval does not apply to dense precision.
No extreme-scale comparison criterion is relaxed here. Follow up original
self-sensitivity and independent least-squares backward error before deciding
whether this is an implementation or comparison-contract issue.

After the localized eigensystem change,02175 CPU/02176 GPU3 pass12/14 cases;
both near-identity failures are repaired at unchanged tolerance. Only the two
huge diagonal precision comparisons remain failed.02177 passes the CPU
independent100/160-digit least-squares reference and original one-ULP sensitivity
diagnostic. These explanatory checks do not waive the original comparison.
The cost-attribution/derivative/consumer matrix also keeps runtime sources
frozen and every worker under300 seconds. No public controller has been wired.

02181 is a derivative-harness failure: the original dense fitter uses
`MatrixSolveLs(fast=False)`, which has no TensorFlow derivative. It cannot be a
VJP authority. Preserve original primal comparisons, compare current graph/XLA
derivatives, and check the independent full-rank least-squares pullback for
`0.5*||sym(C)||_F^2 = 0.5*sum(eigenvalues^2)`. For `C=(A'A)^-1 A'B`,
`G=sym(C)`, `Z=(A'A)^-1 G`, the reference uses `dB=A Z` and
`dA=(B-A C)Z' - A Z C'`. The tiny reference design condition is below2; NumPy
is confined to this independent diagnostic. This is a harness correction,
with no runtime change or weakened numerical tolerance.

Component attribution02179 CPU/02180 GPU3 identifies the tall design SVD as the
main D5 XLA cost: about2.22 ms on GPU versus0.63 ms for native CPQR/COD and
0.81 ms for the raw eigensolver. Separate launches are explanatory and their
times are not additive. Test the equivalent reduced SVD for tall static
designs: with `A=Q R` and `Q'Q=I`, `A'A=R'R`, so the singular values of `A`
equal those of `R`. Apply this only inside XLA; the original graph path remains
the reference. Keep the same numerical rank threshold, reported values and
consumer decisions. This changes execution of the same SVD diagnostic, without
changing the least-squares fit or filter algorithm. Require full original and
rank-boundary checks, changed-input HLO, consumer checks and fresh matched costs
before integration. Preserve every earlier failed/timing artifact.


## Continuation after reduced-SVD qualification

02190--02194 pass140 checks. Fresh matched costs02195--02230 preserve all
initial/changed records at1e-10 but retain D5 warm triggers:24.2% CPU and60.0%
GPU3 versus original medians. This optimization has not resolved the cost gate.
No memory trigger fires. A prelaunch GPU3 contention stop is preserved in
`dense-cost-preflight-pause-02212.json`; the same batch resumed after idle checks.
Investigate reduced QR-plus-SVD and the refined eigensolver separately; compare
each to its original graph quantity. These component timings are explanatory
and cannot replace whole-endpoint costs. The graph arm must remain truly graph
mode, including for the refined eigensolver comparison.

The owner approved the extreme-precision proposal on2026-09-21. The comparator
is now installed only in the huge D3/D5 spectral fixture, with exact schemas,
shapes/types, unchanged other fields, independent100/160-digit references and
response-residual checks. Run mutation tests first, then the complete dense
spectral/analytical-derivative/consumer and quadratic numerical suites on CPU
and GPU3 before further runtime changes. No source change during workers.

Primary-agent continuation review, 2026-09-21: the static paired/uniform choice
reuses one recurrence, preserves original role indices and adds uniform fit as
role6. Probe partitions keep the original anchor even if an earlier cloud
improves the incumbent. Float64 Philox conversion preserves the existing
non-XLA stream. Explicit graph mode covers fitter and trust dependencies.
The callback cache must distinguish family, row capacity, dtype/mode and
configuration while treating seed as a runtime input. Added family/config/seed
cache tests. No independent reviewer is claimed.

Fail any altered complete history, callback ordering, physical count, rank or
status. Keep the original graph trust solver as authority because its original
XLA spectrum is known wrong. The draft is not a public default until dense
spectral/derivative/cost checks and complete native-controller records pass.
Then qualify public consumers and full refinement costs before promotion.

The shared native controller draft is installed after checkpoint415eaa8f for
direct CPU/GPU qualification. Public uniform wiring remains unchanged. The
original full-record tests and changed-input/HLO checks are mandatory; dense
component and original self-sensitivity groups are explicitly explanatory.
No numerical runtime exemption is added to the source-policy allow list.

02247--02249 pass48 original CPU cases.02250 preserves two failures: D3's
harness incorrectly forbade the legitimate fixed target-matrix capture; D5
33-row/negative-seed design conditioning differs by5.24394084e-10. Correct the
capture assertion to bind that one exact matrix while requiring all runtime
operands and unchanged HLO. Repair SVD accuracy using XlaSvd at binary64 epsilon,
as already used by the repository condition-number helper; the default backend
stopping tolerance is insufficient. Retain the original values-only pullback
`U diag(ds) V'` (TF2.19 linalg_grad.py::_SvdGrad lines835--839) in an independent
function graph so resources are not retained by the custom gradient closure.
No rank threshold, acceptance criterion, seed, algorithm or comparison changes.
First check independent singular values, smooth pullbacks including repeated
values and design-condition derivatives, then failed complete records and
CPU/GPU resource lifetime. All affected costs need renewal after this repair.

02251--02257 pass135 focused CPU checks:10 independent SVD values/pullbacks,
4 changing-input/cache checks,1 resource-lifetime test,14 spectral boundaries,
6 dense precision/spectral derivatives,28 rank/consumers and72 policy checks.
The D5 error is fixed at the unchanged1e-10 gate and runtime HLO remains stable.
GPU3's preflight stopped the corresponding GPU batch before any worker launched
(samples8675MiB, utilization69/99/64/99/99/99%). The complete CPU uniform/paired
regression matrix is being renewed after the SVD repair. New condition-derivative
checks cover the full QR composition; component attribution includes the precise
reduced SVD with a genuinely non-XLA graph reference.

Full-controller cost interpretation: the dense D5 timing trigger remains a
required investigation. Standalone Jacobi SVD/Householder dispatch and the
corrected eigensystem can be slower than the original specialized Eigen CPU
call while an enclosing XLA loop avoids repeated host launches. Measure both
scopes and preserve both observations; do not subtract/add separate component
times or call the kernel regression repaired because the controller is faster.
Any explained residual tradeoff must state its size, numerical necessity and
complete-endpoint impact before public integration. All declared trigger
thresholds remain unchanged; no statistical ranking follows from three repeats.

After native CPU/GPU record and resource checks, measure the complete uniform
controller with all report fields retained (original/graph/XLA, D3/D5, one
fresh diagnostic process per arm/device). The registered `uniform_round_memory_cpu` and
`uniform_round_memory_gpu` batches use the same driver, limits, inputs, seeds
and output root; reserve `--repeat 1/2` for a triggered follow-up. The analysis script
`/tmp/analyze_uniform_round_costs_20260921.py` checks source/input identities and
both initial and changed full records before reporting. Native-controller costs
include its complete report formatting but omit the public validation/cache
lookup. After integration, require three fresh repeats through the public
endpoint at each arm/device/dimension. CPU is reference
only; GPU3 default-target qualification remains mandatory.

If host growth exceeds256MiB, attribute cold build/trace/first-call stages and
observe3000 alternating-input warm calls at round capacities1/4/8. Check graph
size, HLO, retracing and allocator current/peak along with RSS. Use900-second
registered workers only if those longer diagnostics need it. This changes no
budget or numerical gate. Python closure release is a separate observed check
and cannot establish native executable eviction or general leak freedom.

Review boundary: the September16 DZ5 handoff was reread during this continuation.
It distinguishes reusable BayesFilter target/score and exact transition-block
qualification from MacroFinance-owned model integration and hardware selection.
This uniform-controller checkpoint establishes neither the actual DZ5 route nor
full-chain compilation. Preserve those separate master obligations and the ban
on editing external source pins/environment during this campaign.

02258--02266 pass all187 complete CPU uniform/shared-paired/regression checks
with the SVD repair:48 uniform records,4 changed-input/cache checks,34 paired
records,26 paired probe cases,3 cache/state/resource tests and72 policy checks.
02267 adds2 nondefault batch/enclosure cases (B2/D3/three-row square design and
B8/D5/seven-row design). The original source remains3582b4ac graph trust; every
continuous field is1e-10 and every shape/count/decision exact. Additional dense
condition derivatives, component attribution and numerical checks are running
before native complete-controller costs. GPU remains unavailable at preflight.

CPU native-controller costs02272--02277 pass every initial/changed original
record. Warm original/graph/XLA times at D3 are436.89/45.96/25.68ms; D5
446.79/57.00/34.91ms. One process per arm is explanatory only. XLA observed host
peak increases517.8/517.6MiB, triggering the declared memory investigation.
Within XLA workers, factory construction adds~9MiB, tracing reaches~76MiB, and
first execution accounts for most of the~555/559MiB increase from input-ready
state. Cold execution takes2.52/2.61s, plus~0.28s build and1.31/1.32s trace.
Warm21-call growth is57/115KiB. Test3000 alternating calls and round capacity
before accepting a compile-retention explanation. The first diagnostic uses
D5/cap4 with a300-second worker; no cleanup is added to numerical runtime.
Analysis:`uniform-native-costs-02277.json`. GPU preflight again declined launch
with8675MiB occupied; no GPU numerical work ran.

CPU memory follow-up02278 passes3000 alternating-input calls at D5/cap4, with
complete original comparisons, one trace, unchanged HLO,3699 graph nodes and7
runtime operands (six scientific inputs plus the fixed callback matrix). RSS
observations after warm-up/1000/2000/3000 calls are1188999168/1189081088/1189154816/
1189158912 bytes; the last block grows4KiB. This bounds the observed warm-growth
question for one target and capacity, not native executable eviction or general
leak freedom. GPU3 finally admitted the next worker;02279 passes both nondefault
batch/enclosure cases. The remaining GPU SVD/controller matrix is active.

02279--02286 pass69 focused GPU checks (2 batch/enclosure,10 SVD,4 dynamic-input/
cache,1 resource,14 spectral,8 derivative,28 dense rank/consumer,2 attribution).
Policy02271 remains current source and was skipped rather than rerun. The
complete GPU original uniform/shared-paired matrix is now active.

GPU attribution02286: D5 precise reduced SVD2.715ms versus0.690ms graph, native
COD0.601ms XLA and refined eigenvalues0.988ms XLA. Ordinary tall SVD2.236ms and
ordinary reduced SVD2.275ms confirm that QR reduction alone does not reduce this
small GPU workload. D3 precise reduced SVD0.848ms. These separate call times are
not additive. The accuracy correction is needed for the preserved33-row failure;
using the faster inaccurate default cannot close the gate. Complete-controller
measurements will show the overall effect; the dependency slowdown stays explicit.


## Recovered continuation and public-boundary review

Recovery confirms session97853 continued the frozen native matrix.02287--02289
pass all48 D1/D3/D5 original uniform records on GPU3.02290 passes17 shared paired
D3 records; D5/probe/cache regression is still running. Focused Ruff passes for
all changed runtime and numerical tests. No runtime changes occurred during it.

Next execute registered native GPU costs, then D5 capacity1/4/8 memory checks
on both devices, reusing the already completed CPU capacity4 result02278.
The CPU capacity4 worker took121.72seconds, so retain the300-second bound first.
Renew the precise-SVD dense dependency costs; earlier cost artifacts precede
that accuracy repair and cannot be represented as the final implementation.
All runs retain the original graph trust authority and exact complete records.

The public-integration draft uses the already qualified paired reporter. Its
compatibility boundary is explicit: top-level numerical results remain tensors;
completed diagnostic records use ordinary Python scalars/lists, and payload()
retains the entire numerical schema. Test the public callback cache with a
changing tf.Variable target and changed seed/center/scale. Keep programming
exceptions visible and check malformed callbacks, rank rejection and downstream
initializer composition. Host reporting cannot update numerical decisions.

The old rank-deficiency fixture patches tf.random.stateless_uniform directly.
That now intercepts uint32 generation; instead patch the owned float64 uniform
helper to emit0.5, producing exactly zero offsets without changing the intended
rejection case. The existing anchor test should compare the diagnostic array
directly. Public original comparisons must explicitly use frozen graph-mode
trust and assert the new execution metadata for both families. No numerical
field is removed or relaxed. Extend the policy guard to the whole public module;
the only added exceptions may validate fixed configuration or serialize completed
records. The uninstalled draft has four such exact AST nodes and no numerical
loop exemption. Primary-agent review only; no independent review is claimed.

02287--02293 complete with111 checks passed:48 uniform,34 shared paired,
26 probes and3 cache/resource/state. Policy02271 remains current source. Native
GPU cost matrix42063 starts at02294. Charged through02293: CPU48110.53906388847
and GPU45288.76548697996seconds. No runtime change before the costs.


Native GPU costs02294--02299 pass all complete original/changed records.
Original/graph/XLA warm times are618.14/178.85/37.30ms at D3 and
627.65/204.19/54.98ms at D5. Host XLA RSS increases267.37/266.00MiB versus
original, crossing256MiB; versus graph it increases70.42/68.08MiB. GPU allocator
peaks decrease from8501504/8504832 to125696/160256bytes. XLA first execution costs
6.82/7.00seconds, excluding factory~0.28s and trace~1.30s. Preserve these cold
costs. Analysis`uniform-native-costs-02299.json` covers all12 CPU/GPU workers.
These are one-process descriptive costs; public three-repeat evidence remains
required. GPU capacity4/3000-call investigation starts at02300, session17345.


Analysis review found the first summary omitted the declared2x cold-cost trigger
from its trigger list, although it retained the component times. Preserve that
summary and use`uniform-native-costs-02299-v2.json`, which sums factory, trace and
first execution for the cold boundary. Original/graph/XLA total cold seconds are
0.497/2.846/4.113 (CPU D3),0.495/2.886/4.214 (CPU D5),3.465/7.408/8.389 (GPU D3)
and3.457/7.350/8.574 (GPU D5). Every XLA/original cold ratio triggers. This is an
analysis correction, not a numerical rerun or threshold change. Stage profiles
place the major host allocation in first execution. Single-target caching avoids
rebuilding a warm program; it cannot erase its first compile cost. Public repeated
costs must preserve both cold and memory triggers and test actual cache reuse.

02300 passes3000 alternating-input GPU calls at capacity4. RSS observations are
1466458112/1466515456/1466515456/1466515456bytes; live GPU allocator memory remains
7168bytes and its warm peak160256bytes. One trace and unchanged HLO retain all
runtime operands and complete original/changed records. Capacity8 is active at
02301; no broad leak-freedom or native executable eviction conclusion follows.


2026-09-22 capacity follow-up completes:02278 and02300--02304 coverD5 caps1/4/8
on CPU/GPU,3000 alternating-input calls each. All complete original records,
one trace and unchanged HLO/runtime operands pass. Graph nodes3695/3699/3699
and HLO~3.21/3.22/3.23MB remain bounded across the capacities. Final1000-call
RSS increments are4/4/0KiB CPU and12/0/0KiB GPU for caps1/4/8. GPU current is
7168bytes throughout; peaks increase with output-history capacity. Small residual
host growth is preserved, not rounded to zero. Stage/smaps observations place
most allocation in first execution and support near-flat warm reuse within
these scopes; they do not prove arbitrary lifetime bounds or executable eviction.
Analysis`uniform-capacity-memory-02304.json` and exact archived scripts retain
all numbers. No cleanup call was added to numerical runtime.

Renew36 dense dependency cost workers (three process repeats of each
original/graph/XLA arm atD3/D5 on both CPU/GPU3) beginning02305. The precise SVD
repair changed the measured code, so earlier ratios remain historical diagnostic
evidence. Interpret the required accurate SVD's small-kernel cost separately
from complete-controller dispatch savings. Preserve any20% warm,2x cold/device
or256MiB host trigger. Public wiring still awaits this disposition and all
public consumer/repeated-cost tests; no terminal finding is closed.


## Native qualification decision, 2026-09-22

02305--02340 complete all36 fresh-process dense costs, preserving every initial
and changed-input field. D3 original/XLA medians are0.563/0.550ms CPU and
2.103/1.758ms GPU. D5 is0.574/0.623ms CPU (+8.5%) and2.503/4.241ms GPU (+69.4%).
The GPU D5 repeat ranges2.458--2.507 versus4.223--4.276ms preserve a clear local
warm-time trigger. Standalone XLA adds226/230MiB observed CPU RSS and below1MiB
GPU-host RSS versus original. CPU total cold1.01/1.11seconds triggers2x versus
both original~0.075/0.072seconds and current graph~0.232seconds. GPU dense cold
ratios stay below2x. Analysis`dense-numerics-repeats-02340.json` and source are
archived. Ratios are descriptive three-process medians, not statistical ranking.

The known reduced-SVD/eigen/COD component runs explain the standalone small-D5
GPU cost. The accurate SVD is required by the preserved33-row failure at1e-10;
reverting its tolerance would recreate that numerical bug. Shared round control
removes repeated Python dispatch and the measured complete native XLA endpoint
warms at37/55ms versus618/628ms original on GPU. Separate component timings are
not additive, and these complete costs do not claim the dense penalty vanished.

Native qualification is sufficient to install the public wrapper for the next
bounded test phase. Retain the accurate solver and native recurrence as the
candidate, with their explicit compile/host costs and scoped cache. Public
consumer checks and three fresh-process public costs are still required before
accepting the public checkpoint. No terminal F01--F20 closure or merge follows.
This is an execution tradeoff within the approved algorithm/threshold/budget,
not a numerical criterion waiver or new owner decision.

| Decision | Primary criterion | Veto status | Main uncertainty | Next justified action | Not concluded |
| --- | --- | --- | --- | --- | --- |
| Install public candidate for qualification | Complete original CPU/GPU records, independent SVD/derivatives, resource and capacity checks pass | No unresolved numerical veto in this native scope; D5 standalone GPU and compile/host costs retained | Public reporter/cache behavior and repeated complete costs | Run public/consumer tests and three matched public repeats | No repository-wide repair, arbitrary lifetime memory bound, DZ5/HMC or merge readiness |

Primary-agent result review: the strongest alternative explanation for apparent
warm speed is excluding construction/reporting; public measurements therefore
include validation, cache lookup, full refinement and complete payload. The
native costs already materialize all histories, and cold profiles explicitly
retain factory/trace/compile. Capacity tests constrain only one target per
process; they cannot establish arbitrary target turnover. Actual external/DZ5
calls and broader controllers remain separate mandatory work. No subagents
or independent reviewer were used.


Public integration installed after02340 with no active worker. The uniform host
round/partition implementation is removed; both public families call the shared
controller. Diagnostic list semantics and complete graph escape hatch are now
documented in`docs/reference/paired-quadratic-refinement.md`. Top-level tensors
and serialized numerical fields remain preserved. Whole-module policy passes
204sources/1292exceptions; four public-module nodes only validate fixed options
or serialize completed records. Focused lint and whitespace pass.

02341 passes18CPU public original-record/cache/state tests;02342 passes51existing
quadratic-center tests, including rank-deficient zero-cloud rejection and
initializer composition. Paired/batch consumers and policy remain in the active
matrix. Repository call-site inspection identifies the two existing integration
scripts; both consume top-level tensors, scalar diagnostic counts or payload(),
with no diagnostic-leaf .numpy() dependency. Their external scientific claims
are not part of this consumer-type check.

02341--02345 finish204CPU checks (18public original/cache,51quadratic,38paired,
25batches and72policy). GPU public matrix99104 declined at preflight before
launch with8675MiB occupied; preserve`uniform-public-preflight-pause-02345.json`.
IndependentCPU public costs begin02346 under the same frozen source while GPU
availability is pending. No unrelated process or idle threshold changed.
