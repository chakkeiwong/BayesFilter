# Dense initializer controller qualification

Question: can the exact external2f386f75 initialize_dense_local recurrence run
through one reusable XLA invocation, retaining locator and attempt records,
partial cloud archives, strict incumbent selection and error/no-use semantics?
This unit composes the qualified reusable locator and prepared-cloud attempt.
It leaves seeded generation, external NPZ assembly, actual DZ5 callbacks and
public wiring to their declared subsequent units. No completed CDF campaign
will be restarted or used as a launch target.

The authority is the byte-frozen external function in
`tests/fixtures/filter_repair_dense_initializer_reference_2f386f75.py`.
A diagnostic AST adapter replaces only cloud RNG with explicit prepared offsets,
uses pinned original BayesFilter locator/fitter dependencies, and records the
policy migration from its false JIT flag to true. GPU resource counters use the
already qualified int64 accounting adaptation; CPU reference source is unchanged.
The actual original function still writes its NumPy NPZ archives. Tests read
those independently and compare every member against completed native tensors;
this is reference code only and never an admitted runtime dependency.

The controller owns one locator, one attempt program, invocation lock and fixed
capacity histories. An outer tf.while_loop carries center, exact rows, attempt
count and typed status. It checks locator acceptance and scaled-score cap before
any clouds, then preserves first-invalid early exit, strict cloud-center
promotion, attempt exhaustion, fit validation errors, fit rejection and finite
positive marginal scales. Return all partial archives, locator/fit histories,
computed score norms and objective improvements; the host must only decode
completed decisions. Apply stop_gradient to the completed outer return.

Pass criteria: complete source records and archives at unchanged comparisons,
exact callback order and batch extents, zero downstream calls after vetoes,
returned/no-use scale behavior, CPU/GPU compilation, stable changed-input HLO,
one trace and actual owner/callback/dependency graph collection. The narrow D3
condition-report proposal remains a separate decision and cannot be silently
applied to new fixtures. Include a well-conditioned analytically specified
one-factor target for the healthy D3 controller test; retain the independent
near-saturated fixture and its failure evidence.

Cases: healthy D1/D3; invalid locator and cloud; score veto; incomplete
stability caps; moved-center retry to success and exhaustion; invalid rank and
archive/error precedence; changed and return operands. Use a deterministic
row-independent target and retain source hashes for scripted branch fixtures.
A scripted branch probe cannot replace healthy real-locator qualification.

Reserve60 workers/9600 charged seconds within unchanged32CPU/52GPU campaign
hours. Ordinary cases120s, full original numerical checks300s. Run one registered
numerical worker at a time with source frozen. Artifacts use the campaign's
numbered directories, verified GPU growth, existing non-desktop selector,
recorded commands/environment and cumulative charges. Stop promotion on any
unresolved record, predicate, callback, shape, derivative, HLO or lifetime
mismatch. Stop execution only for invalid evidence, changed scientific contract
or exhausted budget. Local harness repairs can retry within the allocation.

Review: a correct cloud/fitter component does not prove a correct recurrence.
Saving only the final fit hides earlier attempts and partial invalid clouds.
A host format operation must not redo scale multiplication, norms or incumbent
subtraction; return those values from the compiled call. Locator resources must
reset on every call and cannot be concurrently shared by different owners.
Fresh-process containment remains necessary for native executable residency;
Python collection alone is not eviction evidence. This unit is an engineering
execution test and cannot promote posterior covariance, HMC or canonical LEDH.

Continuation after03469: D1 healthy/locator-invalid/cloud-invalid/score-veto/
fit-rejected checks pass03464--03468. D3 healthy fails only the factor_1
trace-normalized operator norm (about7.27e-9 versus1.01e-8); fitted precisions
agree to rounding. Before changing anything, compare original/current saved
matrix differences under identical XLA SVD, a magnitude-normalized XLA SVD,
and independent NumPy LAPACK diagnostic SVD. Include zero and scales1e-140,
1e-12,1,1e140. This is an explanatory test within the existing worker/time
reservation; it does not waive a gate. A successful localization can justify
repairing an inaccurate norm evaluation, followed by original full-record and
both-sides-of-threshold regressions. Preserve03469 and unchanged comparisons.

03470 confirms the suspected norm defect on identical saved inputs: unscaled
XLA SVD has about28% relative error; magnitude-normalized SVD has at most2.2e-13
relative error against independent LAPACK. Zero and the five scale examples
pass. Install homogeneous scaling only for the precision-difference operator
norm, preserving its mathematical definition and every configured cap. Qualify
independent scale/zero results, full original comparison records, threshold
decisions on both sides, existing stability/consumer tests and the full failed
D3 controller on CPU/GPU. This is a correctness repair, with no comparison
exemption. The two unrelated reporting proposals remain pending. The terminal
endpoint audit must additionally inspect other unscaled SVD consumers before
claiming this compiler sensitivity is fully covered.

03471 preserves a test adapter error (list precision lacked the internal fit
record's shape attribute);03472 passes45 norm/stability/boundary checks after
converting only that reference fixture to arrays. Review identifies a residual
case: unit-magnitude matrices can also have nearly equal singular values below
the default XLA convergence tolerance. Use the same100-iteration binary64
epsilon XLA SVD setting already used by the repository's condition diagnostics,
with a graph-reference branch and an analytic1+1e-7 singular-value regression.
This improves evaluation of the same norm; it does not change its threshold.

The unit's worker allowance is revised from32 to48 to include norm-localization
retries and explicit branch cases, with the9600-second unit ceiling and global
caps unchanged. The D1 recurrence uses a declared analytic polynomial with local
maxima at0 and1 and higher value at1; it tests movement/retry and exhaustion with
the real original locator and fitter. Other additions exercise a second invalid
cloud, invalid-rank fit errors and copied-partition errors. Compare exceptions
and the absence of an NPZ on exceptions; partial invalid-cloud returns must keep
the first two completed partitions. Strengthen collection to callback, attempt
and both dependency graphs. Retain all five original healthy/veto cases.

03474 passes full healthy D3 after the norm repair;03475--03482 pass the eight
new recurrence/error/archive cases. Install the completed host formatter and
use the qualified NumPy-free NPZ writer in every controller test. Re-read each
candidate archive with independent NumPy and compare every member against the
original archive and the emitted native tensors. Five exact host-reporting
exceptions cover JSON/history/member traversal; no numerical loop is exempted.
Renew the full CPU matrix and execute the corresponding GPU matrix. The maximum
worker count is60 to cover this report/archive renewal; the9600-second unit
ceiling and total campaign caps do not increase. Public argument wiring, seeded
controller/actual-consumer qualification and full cost/memory checks remain open.
