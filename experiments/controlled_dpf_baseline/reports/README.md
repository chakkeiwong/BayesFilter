# Controlled Baseline Reports

Reports record experimental-baseline results and their interpretation.  Use
labels such as `comparison_only`, `filter_consistency_evidence`, and
`sampler_not_justified`.
