# KDM auxiliary native execution repair

Recovery audit, September 26: the existing seed-131 auxiliary fixture fails
on the unchanged baseline (03912). Before another implementation comparison,
save its complete auxiliary/trace/kernel records and recompute each reset from
the saved operands. Separate canonical invalidity, component invalidity and
atom reconstruction tolerance. This explanatory diagnostic cannot waive the
actual endpoint's mandatory native-execution and numerical gates. If the
fixture's rejection is correct, preserve it as rejected-input evidence and
declare an additional valid control before executing it; never relax a gate.
The existing test settings are inherited diagnostic controls, not tuned LEDH
defaults. Up to four localization workers are included in the existing 20-worker
CPU allowance. Exact command uses the stable runner's
`test --group kdm_auxiliary_localization_cpu --device CPU --test-timeout-seconds 300`.
Artifacts are saved in its unique campaign run directory. Skeptical audit:
this is a comparison of execution and rejection semantics, so a failing
baseline is a repair trigger, not evidence against KDM or permission to skip
qualification. No numerical runtime is edited during localization.

03917 localizes the rejection: both reset factor-valid flags and all four
mixture-kernel flags are true, but first-reset column TV is 0.0005588870472403515,
above the unchanged 0.0001 gate. The second is 0.000004311410157331608. Canonical
validity stays false and returns `-inf`/zero; the reconstruction comparison then
correctly rejects. This is insufficient convergence of the inherited four total
Sinkhorn iterations, not evidence of ill-conditioning or a KDM derivative bug.
Preserve that exact fixture. The additional control uses the same seed, inputs,
model, corrections and gates with the existing executor's 8/8 Sinkhorn/balance
configuration (16 iterations). This is a separately declared diagnostic finite
program, not a changed default or tuned scientific scope. Qualify it against
the frozen original auxiliary before the native repair. If it fails, retain
the evidence and localize; do not search for a passing seed or relax tolerances.

03919/03920 pass native auxiliary graph and CPU XLA checks. Call-chain inspection
also finds the shared reset still unrolls the Sinkhorn/balance iteration count
in Python. Convert that same recurrence to `tf.while_loop`, preserving iteration
count (including zero), update order and every operand. Compare the shared
batched/directional outputs in FP64 and FP32 at `2e-10` / `1e-6` against frozen
`b052054f7` reset bytes, plus the full KDM records. The KDM reference now injects
those old reset bytes only in the independent diagnostic call, so it cannot
silently inherit the repair being measured. Keep any FP32 parity failure as a
veto. The full callback route remains diagnostic and no algorithm rebuild is
authorized. This localized native-loop change stays within the existing worker
and compute allowance. Direct reset plus actual auxiliary checks are required
before matched costs; component smokes alone cannot qualify this consumer.

03921 passes FP64 and misses the FP32 eager-reference gate by 1.3709068e-6.
Before attribution, record complete original eager, original graph/XLA and
native graph/XLA outputs under the same frozen operands. The actual KDM baseline
executes this reset inside its traced analytical step; direct eager reset is an
additional comparator, not that exact deployed execution. Preserve the initial
failure and unchanged tolerance while distinguishing a pre-existing compilation
effect from a loop-conversion effect. The recorder must save all outputs before
any assertion. No speed comparison is eligible while the discrepancy is unexplained.

03922 proves that the FP32 discrepancy is already present in the original XLA
reset: all original/native XLA outputs are bitwise equal, and graph same-mode
differences remain below the unchanged 1e-6 gate. Preserve the original-eager
cross-mode failure as its own mandatory terminal group; no automatic waiver or
tolerance change is authorized. FP64 complete public graph/XLA comparisons pass
again after loop conversion in 03923. FP64 costs can answer the execution-cost
question for that qualified diagnostic control, while broader FP32 cross-mode
qualification remains open.

The matched cost fixture is the qualified seed-131, N=4, D=2, T=2, FP64, 8/8
control. Three fresh-process repetitions per original/graph/XLA arm record
three public calls and, for native arms, separate owner construction/first call
plus five retained-owner calls. Original uses its actual default (eager outer
auxiliary, XLA mixture components), not an invented fully compiled baseline.
Report public and retained-owner costs separately, sampled RSS, process high
water and TensorFlow allocator current/peak; no process reservation/live-tensor
equivalence. Frozen-original comparisons execute after the measured phase.
Shared-device GPU observations exclude timing claims. These tiny descriptive
costs cannot establish target-capacity or production performance.

Localized test repairs add checks without enlarging the 7200 CPU / 3600 GPU
second unit ceilings or the global caps. Allocate up to 28 CPU / 16 GPU workers
inside those same time ceilings so preserved harness failures, reset attribution,
ownership regressions and three-repeat costs all remain budgeted. The original
20/12 worker reservation proved insufficient for that required diagnosis; this
is a reallocation within the already authorized compute, not a budget extension.

The three-repeat cost cohort (03929--03946) passes numerical checks but finds
higher XLA host memory and public-call compilation cost. Attribute that increase
with four fresh-process single-owner diagnostics (CPU/GPU, graph/XLA), recording
before/cold/1/5/20-replay/released RSS, native function count, allocator bytes,
trace count and weak-reference collection. Replays must remain exact; no-trace
growth and object release are engineering checks, not proof of native executable
eviction. Keep the same control and global/unit time ceilings. Native callback
ownership and public enclosing-XLA validity behavior remain required API checks.

03947/03948 show one trace and stable memory over 20 retained-owner calls, but
the traced graph survives owner deletion. Add one bounded trace-only registry
diagnostic using the already reviewed closure-path inspector to identify an
actual retaining edge. Do not call this a numerical leak or extrapolate capacity
from the tiny case; graph ownership and native executable retention are separate.
Preserve the complete public-call cost cohort before considering a lifetime fix.

Terminal API review found that the new factory's generic positive-dimension
validator would reject a zero-observation horizon accepted by the old public
recurrence. Preserve its neutral zero value/score and empty step tuple through
a nonnegative horizon validator and a frozen-original default-XLA regression.
This changes no measured positive-horizon numerical program. Permit up to
32 CPU workers under the unchanged 7200-second local ceiling for these localized
compatibility fixes and final verification; the 16-worker GPU ceiling is unchanged.

03954 passes the enclosing public GPU/XLA call but rejects the empty-horizon
compiler body: XLA checks an unreachable observation slice in the zero-trip
analytical loop. Return the exact neutral finite sum at the static zero-horizon
boundary inside the owned XLA function; every value/score is zero, steps are
empty, and validity is true exactly as the frozen original. Positive-horizon
numerics are unchanged. This is shape-boundary handling, not a second numerical
recurrence or a fallback execution mode.

The Younis KDM auxiliary is a diagnostic extension, not a canonical LEDH or
NeuTra training route. Its public callable currently invokes the analytical
score once and then uses a Python trace loop, calls `.numpy()` for model
validation, and creates two TensorFlow kernels for every observation. This
violates the repository execution policy even though the underlying mixture
kernels already have stable signatures and XLA options.

The repair preserves the same trace and KDM equations. Tests load the original
implementation from Git `ab431169d` as an independent diagnostic reference;
no runtime copy is retained. The public callable uses one fixed-signature
TensorFlow program. A private stacked-trace option in the shared analytical
executor avoids unpacking and repacking the time axis. Atom/KDM kernels are
built once per owner, and a `tf.while_loop` accumulates the same values, tangents,
validity flags and per-step diagnostics in the original arithmetic order.
An explicit factory enables repeated calls with changed tensor operands and
fixed model callbacks/options. The public compatibility wrapper builds an owner
per call, preserving mutable Python closure behavior rather than silently caching
those closures. Its per-call compile cost remains measured debt for callers that
do not retain an owner. No global cache is introduced.

Model mismatch is a tensor validity result in the numerical program; the eager
public wrapper raises its existing ValueError at the completed assertion boundary.
Inside an enclosing graph the false validity and NaN auxiliary outputs are the
authoritative rejection. The public tuple of complete step dictionaries and all
existing metadata remain unchanged. Their materialization is host presentation,
not a numerical recurrence. Default `jit_compile=True` follows repository policy;
`False` remains an explicit reference test arm.

Acceptance requires the existing auxiliary fixture to agree with the reference
at the current non-JIT test tolerances, exact replay and changed inputs,
canonical-value no-feedback fields, finite-difference agreement for the KDM
score, one trace, no host callbacks and a stable compiled graph. The complete
original fixture, changed operands, invalid model and ownership checks must pass
CPU graph/XLA before GPU renewal. Matched fresh-process costs cover original,
native graph and native XLA with the same fixture; report cold/public replay and
retained-owner reuse, RSS and allocator peaks separately. Three repeats are
required for descriptive medians; any numerical failure excludes speed ranking.
The route remains
diagnostic-only; no canonical LEDH admission, HMC, posterior, scientific or
performance claim follows.

Current unit budget: at most 32 CPU workers / 7200 charged CPU seconds and 16 GPU workers / 3600 GPU
seconds within the existing 56/52-hour campaign caps. Use the stable campaign
runner, explicit device, unique run directories and one numerical worker at a
time; freeze runtime/scripts/tests while a worker is active. Stop integration on
unexplained numerical failure, source drift, absent diagnostics or cap exhaustion.
No seed, numerical method, threshold or canonical algorithm rebuild is changed.

Skeptical review: moving the trace loop into XLA could change evaluation order,
so the reference comparison is mandatory. Prebuilding kernels avoids repeated
tracing but does not make an unsupported callback compilable; graph inspection
must reject host callbacks. An initial untested draft changed public step fields
and error behavior; it was removed before execution. The revised design preserves
the complete public records and classifies only completed formatting/assertions
as host operations. Compiled ownership freezes callbacks by explicit contract;
the public wrapper cannot assume captured Python cells are immutable. The
score's own analytical
trace remains the sole value/score authority, and KDM never feeds back into it.
