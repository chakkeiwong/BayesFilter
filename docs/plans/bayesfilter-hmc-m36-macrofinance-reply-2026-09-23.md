# BayesFilter HMC repair: consumer status

The shared candidate-set procedure and typed replay are retained. R-hat,
ESS and MCSE assess posterior sampling only; they do not reject or rank tuning
members. The current repair also makes validation batch/lugsail/readiness
settings explicit and rejects completed-fit reuse after those settings change.

The September 23 M36 inventory still cannot find the exact nine-parameter,
48-observation bootstrap request or its diagnostic directory under either
`/home/ubuntu/python/MacroFinance` or `/home/ubuntu/workspace/MacroFinance`:

* `docs/handoffs/one_country_affine_bootstrap_bayesfilter_repair_request_2026_09_22.md`
* `docs/plans/artifacts/one-country-affine-macro-option1-bootstrap-diagnostic-20260918-run01`

The earlier full-joint MIDAS reference is a separate missing dependency. The
September 18 recovery/correction documents are unchanged and describe a
fixed-loadings block model; those results cannot validate the full joint law.

A fresh integration needs the exact source revision and invocation, prepared
observations and their version, prior parameters, parameter ordering and
coordinate transformations, adapter value/score and status telemetry, numerical
mode, fresh start/seed policy, and a matching independent reference with its
Monte Carlo uncertainty. A missing posterior reference does not prohibit a
mechanics check, but prevents a posterior-correctness conclusion.

The consumed bootstrap run has not been rerun. R5 remains `awaiting_inputs`;
synthetic, block-model and supplied-map tests do not close it. The inventory is
`artifacts/hmc-repair-master-2026-09-16/m36-r1/input-inventory.json` in BayesFilter.
This reply is stored locally; no external message has been sent.
