# LEDH control diagnostics and safety calibration

This is the next executable prerequisite inside the score master. It follows the completed bounded iAPF mechanics repair and the nonlinear covariance pilot. The question is whether the numerical controls used by the canonical LEDH consumer materially distort otherwise healthy trajectories or conceal numerical failure. This phase may nominate fresh calibration ranges; it cannot promote a provider or a new default.

## Skeptical audit and intent

The previous pilot varied flow/reset effort and covariance-provider controls but inherited damping, coordinate caps, trust radius and ridge. Those values are hypotheses, not justified defaults. The canonical correction already computes feasibility margins, a scaled-system condition diagnostic, diagonal displacement and cumulant residuals; its calling code drops several of these. This prevents the planned diagnosis even when a command succeeds. Repair that observability gap first. Preserve the actual shared executor and its analytical derivatives; no reduced filter or correction fork is permitted.

| Role | Predeclared interpretation |
|---|---|
| Main question and mechanism | Do inherited numerical controls drive large correction distortions, poor finite-program accuracy or invalid arithmetic in weak, curved and concentrated observation regimes? |
| Engineering pass criterion | Trace-enabled and ordinary calls reach the same shared executor and agree on value and all tested analytical derivatives; all computed correction diagnostics reach the study artifact. |
| Safety evaluation | Healthy cases must retain values/scores within a measured floating-point envelope when merely adding diagnostics. Numerics-altering settings are compared explicitly; measure perturbations and boundedness before any selection. |
| Promotion criterion | None for a method/default in this screen. Later scope-specific calibration and untouched score-MSE comparisons remain required. |
| Promotion veto | Nonfinite output, failed fit/reset validity, or evidence that a proposed safety setting harms healthy cases. A weak score comparison cannot promote a setting. |
| Continuation veto | Broken same-program parity, wrong reference, source drift, malformed/missing diagnostics or exhausted allocation. These trigger repair of the affected run. |
| Repair trigger | Excessive numerical sensitivity, saturated cap, ill-conditioned correction, poor shape or failed candidate. Such results do not reject the research direction. |
| Explanatory diagnostics | Condition times machine epsilon, feasibility margins, cap displacement/derivative, ESS, covariance ranges, transport residuals, cumulant residuals, runtime and descriptive model-score error. No universal condition-number cutoff or MSE slope is assumed. |
| Nonconclusions | No model-score unbiasedness, statistical ranking, provider promotion, high-dimensional adequacy, HMC readiness or rejection of LEDH. Scalar fixtures cannot validate pairwise corrections. |

Implementation begins with opt-in compact trace summaries in the real Gaussian and nonlinear consumers. Preserve every scalar/vector/matrix diagnostic returned by the shared correction, reducing only over time/particle coordinates as explicitly named; do not copy its numerical algorithm. Keep source/filter dtype, TF32/XLA, finite checks and exact chunk policy visible. Tests must check actual wiring and equality with the ordinary call, including initial-law derivatives, covariance-provider routes and more than one state coordinate for pairwise diagnostics. CPU tests are reference exceptions. GPU evidence requires memory growth before initialization.

## Defaults and comparison design

The exploratory baseline is the preceding pilot's more resolved computation: N=128, T=4, flow substeps 8, Sinkhorn/balance steps 30/30, reset epsilon 2, reset ridge 1e-5, one diagonal correction of strength 0.2, LM damping 0.01 and scale floor 1e-4, diagonal trust radius 0.5, pairwise steps/strength/RMS cap 1/0.02/2, coordinate cap 0.4 and power 8. This is a reproducible baseline only. In scalar runs pairwise controls are structurally inactive and cannot be calibrated.

The baseline's coordinate cap is smaller than one standardized unit; it may materially change healthy points. Damping and ridge may also trade numerical stability against distortion. The earliest diagnostic is the actual pre/post displacement and derivative, followed by changes in the same finite score and distance to the independently refined model-score reference. Cap, damping and ridge tests must be reported as numerics-altering comparisons, not silent protections. Existing off/zero settings have no privileged status.

After the trace prerequisite passes, run single-factor curves around this baseline: coordinate caps 4 and 8; damping 0, 0.001 and 0.1; trust radii 0.1 and 2; reset ridges 1e-7 and 1e-3; reset epsilon 0.5 and 4; flow substeps 16; and Sinkhorn/balance 60/60. Together with baseline this is 14 arms. These are logarithmic or doubling perturbations chosen to reveal sensitivity, not optimized scientific defaults. Damping zero is an explicit historical numerical comparator with all validity guards, never an eligible new default. Shape and derivative diagnostics determine which settings merit a later fine curve. Fixed floors and correction strengths remain declared unresolved hypotheses if their behavior cannot be justified by this screen; this phase must not call the entire control family tuned.

Use fresh datasets 700 and 701, one shared particle stream, for each of the same weak/curved/concentrated regimes; master seed 92167. Never tune on opened pilot evaluation datasets 400–423. Reuse the refined FP64 reference construction and its tail/refinement veto from the prior pilot. Construct and evaluate four cheap adversaries (bootstrap PF: direct transition baseline; locally adapted PF: current-observation proposal; EKF: local linearization; UKF: deterministic moment approximation) on every condition. Their score error is a falsification diagnostic only and cannot select safety controls. With only two datasets, continuous differences are descriptive and no statistically supported ranking is possible. Include every failed candidate in the conditional table.

The 84 LEDH rows plus 24 heuristic rows give 108 numerical rows. A small GPU wiring smoke precedes that screen. The complete screen may execute only after this plan is refreshed with the exact generated study paths, source revision and command. It remains an exploratory calibration stage, not an untouched claim run. Adaptive iAPF, KDM/IWSG combinations and the other agents' regular/degenerate-transition score programs are outside this slice.

## Allocation, artifacts and repair

Allocate at most 60 CPU process-minutes for implementation tests and 45 GPU device-minutes including compilation, at most three GPU launches, and at most 120 numerical rows across smoke and screen. This later-phase allocation remains within the master's original eight GPU device-hours. Use the existing `tftwogpu` interpreter and RTX 4080 SUPER with trusted GPU access, TF32/XLA and verified memory growth. No package, hardware or data-boundary change is planned. Limit each launch with a timeout and save stdout/stderr. A local repair consumes this allocation; it does not authorize changing the scientific target or opening more data. Stop unchanged retries and reassess after three unsuccessful repairs for one cause.

Use fresh output directories under `docs/plans/artifacts/younis-kdm-score-master-20260914/run-20260914-140520-01/control-safety-*`. Freeze source before numerical execution; preserve exact source/command/environment/seeds/timings and failed rows. Output must include a machine-readable conditional heuristic screen and separate engineering, numerical-validity and interpretation decisions. A successful command with missing diagnostic fields is incomplete evidence.

Pre-mortem: tiny samples can make chance score differences look compelling; benign scalar models can conceal pairwise/high-dimensional problems; a cap may reduce a residual while making the next observation worse; a refined finite program is not the exact model. Diagnose these with explicit uncertainty limits, multidimensional wiring tests, retained pre/post quantities and the independent numerical oracle. At phase close, record the strongest alternative explanation, failed controls versus failed hypotheses, and the next fresh calibration scope. Do not carry over a setting as tuned for a different N, T, dtype or provider.

## Prerequisite result and exact execution refresh

Source is frozen at `41446e1e` in `.localresources/worktrees/younis-score-control-safety-20260916`. Seven focused CPU/XLA diagnostics tests pass, including actual shared-correction wiring, ordinary-call parity for all six parameter directions, the two-dimensional Gaussian pairwise route and all three nonlinear covariance consumers. Two existing canonical derivative/invalid-reset regressions and three mandatory oracle-contract checks also pass. The first test attempt failed before numerical execution because its import order initialized TensorFlow before runtime configuration; the corrected attempt is preserved separately. A sandbox-blocked Git index lock was retried with trusted permissions. Neither failure changes the scientific method.

The exact inputs are the five study JSON files and `bundle.json` in `artifacts/younis-kdm-score-master-20260914/run-20260914-140520-01/control-safety-config-01/`. `validation.json` records that every row is runnable: eight wiring rows and 108 sensitivity rows, 116 in total. The smoke uses N=128, T=4 for the three nonlinear consumers and N=16, T=2, d=2 for the Gaussian consumer. Each has an ordinary and diagnostic call with the same seeds. Its predeclared FP32 numerical envelope is `64 * epsilon_float32 * (1 + abs(ordinary_output))` for the value and every score coordinate; report actual differences. This is an engineering no-harm screen, not a model-score error tolerance. A failed smoke blocks the screen.

The command below runs from the frozen worktree. `TASK_ART` denotes the absolute directory `/home/chakwong/BayesFilter/docs/plans/artifacts/younis-kdm-score-master-20260914/run-20260914-140520-01`; actual command text and the external driver's SHA-256 are retained in the launch log and manifest.

```bash
CUDA_VISIBLE_DEVICES=GPU-68251639-fe82-8f81-3ccc-2953c32e805b \
TF_FORCE_GPU_ALLOW_GROWTH=true BAYESFILTER_PRELOAD_CUSTOM_OP=0 \
TF_NUM_INTRAOP_THREADS=1 TF_NUM_INTEROP_THREADS=1 OMP_NUM_THREADS=1 \
timeout 900 /home/chakwong/anaconda3/envs/tftwogpu/bin/python \
  "$TASK_ART/run-control-safety.py" "$TASK_ART/control-safety-config-01/bundle.json" \
  smoke "$TASK_ART/control-safety-smoke-01"
```

After the smoke passes, use the same environment/interpreter/driver and bundle, `timeout 1800`, stage `screen`, and fresh output `control-safety-screen-01`. Both runs use escalated GPU access. The preflight finds concurrent host research jobs and nonzero GPU utilization; exclusivity is not assumed and runtime comparisons are forbidden. Memory growth remains mandatory. The driver preserves candidate numerical failures and continues independent conditions; source drift, missing diagnostics, reference mismatch or an unclassified failure stop execution for repair. A post-run conditional table must include all 14 LEDH arms and four cheap adversaries, preserve the two datasets separately, and explicitly decline statistical rankings.

## Bounded repair after the first screen

The GPU smoke passed eight rows in 102.324 seconds. The first screen executed all 36 weak-regime attempts in 151.523 seconds: 32 completed, and four cap-4/cap-8 rows were rejected during tracing by `higher_moment_shape_jvp`'s validation. The driver stopped before the remaining conditions. This is an implementation-domain restriction, not numerical evidence against either cap. The pre-run audit missed the primitive's inherited upper bound; coordinator validation alone did not prove that the planned controls were callable.

The generic standardized cap is the smooth map `g_c(z) = z / (1 + (z/c)^p)^(1/p)` for finite `c > 0` and even `p >= 2`. Differentiation gives `g_c'(z) = (1 + (z/c)^p)^(-1/p-1)`, which is positive and at most one for every such `c`; `abs(g_c(z)) < c` for finite z. This derivation imposes no `c < 1` requirement. Its input is measured in standard deviations, not a bounded chart. The separate bounded-chart cap does need its chart-specific bound and retains it. The subsequent affine moment restoration can expand the cloud again, so the cap bounds its intermediate standardized coordinates, not final particles. Neither smoothness nor this derivative bound guarantees downstream score accuracy or numerical stability.

Repair only the generic standardized-cap validator to accept finite nonnegative settings (zero retains its explicitly disabled comparator meaning). Reject nonfinite values; retain the bounded-chart restriction and mutual exclusion. Parameterize the existing three-dimensional affine-moment and manual-JVP tests at caps 0.98, 4 and 8, and add validation tests. Autodiff remains a test reference only. Freeze a new source revision; do not edit the original snapshot. Existing successfully executed controls must have exactly unchanged arithmetic. The final report may combine their preserved results with repaired cap rows only after confirming that the sole numerical-module source difference is pre-trace validation and the old accepted inputs still run the same operations.

Resume into fresh output `control-safety-screen-02`: execute only four rejected weak rows plus the 72 unstarted curved/concentrated rows. The 32 completed weak rows remain in screen-01 and are not recomputed. This uses exactly 120 total attempts including smoke, remains within three GPU launches, and leaves 2,446 seconds of the 45-minute allocation after the first two launches. The retry timeout is 1,800 seconds. All three conditions are still reported with complete row/source provenance; incomplete or mismatched rows are a report veto. No range, target, comparator, dataset or promotion criterion changes in this repair.

Repair source `cbcfea1e` is frozen in `.localresources/worktrees/younis-score-control-cap-repair-20260916`. Thirteen coordinate-cap tests and three mandatory oracle-contract checks pass. An initial validation-test attempt omitted two required arguments; it failed before reaching the validator, was corrected, and is retained in the logs. `control-safety-repair-config-01/repair-audit.json` verifies equality of the entire numerical module's parsed syntax outside its pre-trace validation and the new standard-library import. The generated retry bundle contains exactly 76 runnable rows. Execute the same GPU command from this repaired worktree with `control-safety-repair-config-01/bundle.json`, stage `screen`, and output `control-safety-screen-02`; timeout remains 1,800 seconds. Final reporting must read both screen directories and validate every successful result digest and paired reference signature.
