# Filter repair remote integration

Date: 2026-09-25
Status: `VALIDATED_REPAIR_BRANCH_INTEGRATION`

Integrate remote main `5e16df06f` into repair checkpoint `3128687bd` without
losing native numerical recurrences or new remote functionality. This is a
repair-branch integration, not terminal promotion. The existing campaign
authorization and cumulative 32 CPU / 52 GPU-hour limits apply. At start,
11.620534 CPU / 31.404130 GPU hours remain. Reserve at most 24 workers / 7200
seconds for focused post-merge checks; local failures consume this allocation.
Use the stable `scripts/run_filter_repair_campaign.py` prefix, one numerical
worker at a time, CPU reference or available non-desktop GPU, verified growth,
numbered immutable run directories and existing source/provenance manifests.

Three conflicts require explicit disposition. Keep both ignore-rule additions.
Respect main's deliberate deletion of the obsolete GenUT bootstrap target and
its test (commit `43de3cb6c`); remove only obsolete guard entries and record
retired campaign groups without treating them as canonical replacement evidence.
For the Hermite KR sampler, retain main's reverse-conditioning and batched cores
and the repair branch's native coordinate loop in one shared implementation.
Pad heterogeneous ranks as before, broadcast only leading batch axes, and keep
CDF equations, bisection count, bracket/status reporting and order unchanged.
This is execution repair of an explicitly labeled extension; no Zhao-Cui
source-faithfulness claim or algorithmic redesign is made.

Pass criteria are complete original-record comparisons for the existing
proposal and frozen-main forward/reverse/batched inverse checks, changed operands,
stable enclosing XLA/HLO, no host callbacks, policy checks and focused Kalman,
analytical-score and canonical-IAF checks. Independent Gaussian/quadrature checks
remain numerical references. Timing is not a promotion criterion here. Any
numerical/status mismatch, unexpected import, source drift or budget exhaustion
stops the affected arm for localization. At most three localized harness retries
per unchanged fixture. No tolerance relaxation, stale admission refresh or
historical-map canonical promotion is allowed.

Skeptical review: selecting either conflict side would silently lose a capability
or reintroduce Python numerical loops. Batched right environments must retain
particle-specific suffixes and heterogeneous ranks; test both explicitly.
New remote files expand the audit surface beyond the existing partial guard;
they cannot inherit a clean repository verdict from the old source freeze.
Canonical IAF integration tests establish mechanics only, not learned-map quality
or HMC readiness. New target execution requires a fresh merged-commit snapshot.
Record exact test commands in run manifests and outcomes here; renew the concise
master/ledger checkpoint after this unit. No external code or environment edits.

Conflict review: six initial checks in 03763 pass for shared forward/reverse and
particle-specific inverse records, retirement and the internal warm-up JIT
default. Existing Hermite checks in 03764 pass. The old GenUT batch module's
deletion also orphaned one general-parity file, one shape-LM test, and the
`genut` / raw-barycentric `sinkhorn_jvp` measurement fixtures. Retire these exact
consumers; retain independent shape-LM checks. Their Git history and saved runs
remain historical. This does not establish any replacement canonical LEDH gate.
The partial policy guard now covers 237 sources / 1346 exact allowances:
two retired modules removed, three obsolete shape allowances removed, twelve
reviewed shape/signature/schedule constructions added. No numerical Python-loop,
NumPy or non-XLA exception was added. New remote algorithm modules remain an
explicit terminal audit obligation rather than inheriting prior qualification.

03765 passes 12 consumer checks and fails two concrete compatibility cases:
an empty suffix environment (valid for the first retained step), and eager
GradientTape through the default-XLA incomplete-Gram function, whose reverse
loop TensorList cannot cross the boundary. Return the unchanged suffix when the
core tuple is empty. For the Gram derivative use the fundamental theorem of
calculus: dM_ab(z)/dz = psi_a(z) psi_b(z) phi(z), with the same normalized
Hermite basis. A custom pullback contracts the cotangent with that derivative;
it changes no forward value, CDF rule or bisection. The existing polynomial-density
derivative check and original Gram comparisons are the immediate vetoes. This
localized integration repair remains inside the stated budget and method.

CPU matrix 03767--03773 passes 6 shared-inverse/retirement/default, 14 consumer,
6 canonical-IAF, 12 refined-eigen, 5 independent GenUT primitive, 87 SVD/filter/
derivative and 129 policy checks. GPU 03774 passes six shared-inverse checks.
03775 completes all 14 consumer assertions but fails the worker's provenance
check: two imported remote tests overwrite CUDA visibility with `-1`. Preserve
the invalid GPU record. Change only those test defaults to `setdefault`, so
standalone CPU behavior remains and explicit runner selection survives. Repeat
the GPU matrix under the unchanged numerical source and budget; no hardware or
numerical failure is inferred from this test-harness mutation.

All integration checks now pass: 162 per backend (shared inverse, actual
consumers, canonical IAF, refined eigenpairs, independent GenUT primitives,
Kalman/SRUKF/SVD derivatives and original Hermite records), plus 129 policy
checks. GPU runs 03776--03781 and 03784 use verified GPU2 with memory growth.
The only CPU/GPU source difference is the two test visibility defaults; numerical
runtime bytes match. 03785 refreshes the syntax inventory but still sees merge
index stages; repeat after resolving the index before reporting final counts.
Both preserved failures have bounded repairs and fresh passing counterparts.
Ruff, syntax and focused working-tree whitespace checks pass. The canonical transport core is byte
identical to remote main. This supports a repair-branch merge checkpoint;
public/DZ5, native capacity and terminal scientific/execution gates remain open.

| Decision | Criterion | Veto status | Main uncertainty | Next action | Not concluded |
|---|---|---|---|---|---|
| Commit integrated repair branch | Focused CPU/GPU values, derivatives and policy checks pass | Failed attempts preserved; their repairs pass | Wider new remote call chains await terminal audit | Fresh merged-source DZ5 snapshot and target checks | Main promotion or repository-wide completion |
| Preserve GenUT retirement | Deleted implementation and obsolete consumers remain retired | No old bootstrap admission survives | Canonical replacement has independent gates | Audit current canonical endpoints separately | Canonical LEDH readiness |
| Retain XLA defaults | Native recurrence and exact Gram pullback compile | No host callbacks in tested enclosing graphs | Full native-memory capacity is not tested here | Continue existing capacity plans | Memory eviction or performance superiority |

Terminal skeptical review: a passing module test could miss a new consumer or
confuse historical NeuTra facades with the canonical map. Actual reverse/batched
consumers and canonical-authority checks address the tested paths; the expanded
inventory remains a search lead for the full endpoint audit. No frozen old cost
cohort is silently relabeled as merged-source evidence. The weakest remaining
integration evidence is actual DZ5, which is the next execution step.

Final inventory 03786 runs after the merge index is resolved: 7,474 tracked
Python files, 7,473 parsed, and one unchanged historical vendor parse error.
The inventory is a search aid, not a compliance verdict. The integration unit
has consumed its 24-worker allocation and 1,077.729097 charged seconds, within
the 7,200-second ceiling. Cumulative charges are 73,975.777126 CPU and
74,613.162079 GPU seconds; 11.451173 CPU / 31.274122 GPU hours remain.
The final inventory archive and checksums are recorded in
`artifacts/filter-gradient-repair-20260917/remote-integration-verification-03785.json`.
The next numerical work uses the separate existing E5 DZ5 allocation.
