# Zhao-Cui integrated audit: fresh-session brief

Continue the owner's latest scientific request: thoroughly analyze the code,
LaTeX, and mathematical algorithm; propose a coherent set of fixes; use
MathDevMCP to establish mathematical assertions. Runtime fixes are proposals
until that analysis establishes how they fit together. The earlier isolated
aggregate-guard repair brief is superseded for this audit.

Research checkout: `/home/chakwong/BayesFilterZhaoCui`, branch
`zhao-cui-tt-regression-20260908`, base
`47176bce7cdaa91ddd4466c39f90a11ad005c800`, plus preserved uncommitted work.
Notes workspace: `/home/chakwong/BayesFilter`. Verify current differences before
using previous evidence. Global context-discipline policy is already installed.
Do not load saved conversations, incident reports, or policy-installation
backups into this research session.

Read the 66-line plan `docs/plans/zhao-cui-integrated-audit-2026-09-11.md` in
the notes workspace. Its scientific contract and bounded diagnostic/proof
allowances remain the governing plan. The stalled integrated stage used no
numerical invocation or proof attempt: it called MathDev `doctor` and inspected
source. The planned `artifacts/zhao-cui-integrated-audit-20260911-01/` directory
was still absent at recovery; create a new versioned directory if it now exists.

Start with stage 1: build a compact, source-anchored inventory of the active
Algorithm 3 manuscript's assertions and the corresponding preparation,
sampling, value, analytical-score, and consumer paths. The manuscript is
`docs/benchmarks/artifacts/c2_completion_20260824/attempt05/attempt05_n4_failure_analysis.tex`
in the research checkout. Read its relevant sections and cited dependencies
selectively. Audit Zhao-Cui paper and author-code anchors before calling any
route source-faithful. Save exact source hashes and assertion/proof obligations
before proceeding to bounded mathematical verification.

Earlier stage-03 findings are unresolved, not a completed overall verdict:
rank initialization can remain rank one on a rank-two target; invalid aggregate
output can be accepted; frozen-path and fixed-uniform derivatives differ;
numerical-CDF and smooth-TT proposal laws differ; consumer wiring is incomplete.
Consult selected fields or exact sections of
`docs/plans/artifacts/zhao-cui-audit-20260911-03/stage-result.md` only when needed.
Treat earlier proof and diagnostic claims as checked only after inspecting their
specific evidence and confirming unchanged source. A successful MathDev health
check is not a proof.

For every `functions.exec` call, explicitly set an outer output allowance
normally no greater than 4,000 tokens, and keep the sum of command allowances
within it; ordinarily use at most 2,000 per command. Reserve room for wrappers.
For example, start the tool script with
`// @exec: {"max_output_tokens": 3500}` and keep all command limits below that
combined allowance.
Use file-name/count searches, narrow reads, selected JSON fields, and compact
MathDev verdicts with evidence saved on disk. Never print entire tool registries
or nested results. Truncation requires narrowing the next query.

At each substantive result, replace the active checkpoint with the current
question, checked finding, evidence path, remaining allowance, and exact next
action. Finish the current bounded stage and persist its result before starting
another large stage. Continue through fresh stage conversations while remote
compaction is unreliable; do not launch other agents autonomously. No GPU,
training, HMC, scientific comparison, or environment installation is authorized
by this audit brief.

## Current bounded checkpoint (2026-09-11)

Stage 01 source inventory is complete at
`docs/plans/artifacts/zhao-cui-integrated-audit-20260911-01/stage-01-source-inventory.md`.
The paper and author MATLAB anchors are hashed there. The structural route is
anchored, while local chart construction and numerical CDF handling are
classified as extensions or fixed adaptations. The active unresolved gaps are
rank activation, fail-closed aggregate checks, numerical-CDF law wording, and
claim-bearing consumer wiring.

Next exact action: bounded proof checks for the squared-TT marginal, upper
conditional, physical Jacobian, Algorithm 3 weight identity, and frozen score;
save compact verdicts before any runtime edit or larger diagnostic.

Stage 02 is now recorded at
`docs/plans/artifacts/zhao-cui-integrated-audit-20260911-02/stage-result.md`.
SymPy certified four bounded scalar identities; the full TT, conditional, and
recursive claims remain manual/source-anchored. The next action is a small
deterministic CPU diagnostic pass for the rank, aggregate-finiteness, numerical
CDF-law, and consumer-wiring blockers.

Stage 03 reconciled the prior CPU evidence to the current source at
`docs/plans/artifacts/zhao-cui-integrated-audit-20260911-03/stage-result.md`.
The repair proposal is saved at
`docs/plans/artifacts/zhao-cui-integrated-audit-20260911-04/repair-proposal.md`.
No runtime code has been changed during this continuation. The audit is ready
for a bounded repair decision, with the fail-closed aggregate guard as the
smallest justified first implementation and rank initialization requiring a
separate Class C calibration.
