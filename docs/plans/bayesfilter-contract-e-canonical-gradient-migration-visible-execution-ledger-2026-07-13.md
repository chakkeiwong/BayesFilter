# Visible Execution Ledger: Contract E Canonical LEDH Gradient Migration

Date: 2026-07-13

Program ID: `contract-e-canonical-gradient-migration-20260713`

Status: `BLOCKED_HUMAN_DECISION_AND_IMMUTABLE_CAMPAIGN_BUDGET`

## Roles And Boundaries

Codex is supervisor and executor. Claude is a read-only reviewer. Execution is
visible in the current conversation. No detached agent, package installation,
destructive git action, public release, or model-file edit is authorized.

## Campaign Clock

- Maximum visible campaign duration: eight hours.
- Clock basis: current Contract E migration launch in this conversation.
- Scientific thresholds are independent of this operational limit.

## Initial Skeptical Audit

Status: `PASS_WITH_PHASE_GATES_AND_DEFERRED_MARGIN_DECISION`.

- Exact Kalman and same-scalar FD answer different questions and remain separate.
- Covariance restoration is a veto diagnostic, not sufficient promotion evidence.
- The existing dense Contract E matrix interface cannot scale to `N=10000`.
- The streaming design must expose row mass and compose a cloud-level Contract E
  pullback with the streaming transport pullback.
- The existing score schema omits reset semantics and can admit a raw route.
- Historical `1%` Kalman-gradient tolerance is not reused without justification.
- Dirty work exists in the repository; the initial planning set is additive.

### 2026-07-13 - Program - PRECHECK

Evidence contract:

- Question: can the migration be planned without repeating the raw-route
  substitution or creating an unscalable dense path?
- Comparator: current schemas, adapters, transport helpers, Contract E helper,
  LaTeX chapters, and prior LGSSM evidence.
- Primary criterion: master, Phase 0 subplan, runbook, ledger, stop handoff, and
  bounded review bundle exist and survive skeptical audit.
- Promotion vetoes: missing reset identity, invented oracle threshold, dense
  production assumption, or review/policy ambiguity.
- Continuation vetoes: owner directive ambiguity, unexpected concurrent changes,
  missing authority, or review nonconvergence after five rounds.
- Nonclaims: no algorithm, gradient, covariance, production, HMC, or leaderboard
  correctness is established by planning.

Actions:

- Read repository policy, Claude review guide, visible-runbook template, current
  route/schema code, Contract E code/evidence, and LaTeX anchors.
- Found that the prior demotion separated compact versus full-history gradients
  but did not bind raw versus Contract E reset semantics.
- Found the current Contract E helper's dense matrix input and planned a
  cloud-level streaming composition.

Artifacts:

- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-master-program-2026-07-13.md`
- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase0-policy-route-freeze-subplan-2026-07-13.md`
- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-visible-gated-execution-runbook-2026-07-13.md`
- this ledger;
- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-visible-stop-handoff-2026-07-13.md`;
- `docs/reviews/bayesfilter-contract-e-canonical-gradient-migration-master-review-bundle-iter1-2026-07-13.md`.

Gate status:

- `IN_PROGRESS`: local checks and bounded material review remain.

Next action:

- Run local planning checks, then invoke the bounded Claude review gate.

### 2026-07-13 - Program - REVIEW_ITERATION_1_REPAIR

Evidence contract:

- Question: do the repaired phase order and gates prevent forged route identity,
  partial Contract E derivatives, post-result threshold selection, and nominal
  but infeasible production execution?
- Comparator: iteration-1 fresh Codex read-only findings after the platform
  blocked external Claude review.
- Primary criterion: all nine findings are visibly bound into the master,
  Phase 0 subplan, and runbook before execution.
- Promotion vetoes: implementation before normative math; caller-stamped route
  identity; omitted direct moment/weight adjoints; deferred raw admission;
  missing dense/cloud parity; unspecified replication/FD/feasibility/veto gates.
- Continuation vetoes: unexpected concurrent edits or five nonconvergent material
  review rounds.
- Nonclaims: repaired planning does not establish Contract E correctness.

Actions:

- Recorded that the Claude path was rejected by the platform external-disclosure
  boundary and will not be retried or bypassed.
- Reordered the program so checked normative mathematics precedes schema and
  implementation.
- Required immediate Phase 0 raw/v1 admission revocation, a factory-issued
  non-overridable route identity, complete direct-plus-transport pullback,
  two-stage small-`N` parity, frozen statistical/FD/veto designs, and measured
  production feasibility.

Artifacts:

- repaired master program, Phase 0 subplan, runbook, and stop handoff;
- `docs/reviews/bayesfilter-contract-e-canonical-gradient-migration-master-review-bundle-iter2-2026-07-13.md`.

Gate status:

- `IN_PROGRESS`: focused checks and fresh bounded Codex review remain.

Next action:

- Run structural checks, then request the iteration-2 fresh Codex review.

### 2026-07-13 - Program - REVIEW_ITERATION_2_CONVERGED

Actions:

- Structural checks and `git diff --check` passed for the repaired planning set.
- A fresh Codex reviewer read only
  `docs/reviews/bayesfilter-contract-e-canonical-gradient-migration-master-review-bundle-iter2-2026-07-13.md`.
- Review result: no material findings; `VERDICT: AGREE`.
- Reviewer noted that any precision-target stopping rule must preserve interval
  coverage; that is binding input to the Phase 1 design freeze.

Gate status:

- `PASSED`: the master program may launch Phase 0.

Next action:

- Perform Phase 0 dirty-file/source inventory, then apply the minimal fail-closed
  v1/raw admission revocation, policy, manifest, tests, result, and Phase 1 handoff.

### 2026-07-13 - Phase 0 - PRECHECK

Evidence contract:

- Question: can the owner route decision be made mechanically fail closed before
  canonical implementation begins?
- Comparator: current v1 forward/score validators, emitters, adapters,
  aggregators, leaderboard consumers, raw route symbols, and dense Contract E
  reference.
- Primary criterion: every v1/raw artifact is historical and cannot obtain
  admission/default/leaderboard/HMC status, even with forged Contract E metadata;
  all routes/consumers have exact manifest anchors.
- Promotion vetoes: any admitted v1 output, caller-controlled canonical identity,
  missing consumer, changed numerical threshold, or overwritten dirty work.
- Continuation vetoes: unclassifiable active route, unexpected concurrent edit,
  or an authority conflict.
- Nonclaims: Phase 0 does not compute or validate a likelihood or gradient.

Skeptical audit:

- The v1 schemas have no reset identity; therefore accepting an admitted v1
  payload would treat missing semantics as evidence. Rejecting admitted v1 status
  is the smallest correct emergency action.
- Tiny/blocked v1 payloads may remain readable as historical diagnostics.
- A caller-added `reset_contract_id` cannot repair a v1 artifact because the
  executed callable is not mechanically bound to that field.
- Central validator rejection must be supplemented by reachability tests for the
  shared emitter and inclusive aggregator so no consumer trusts the raw status
  string without validation.

Gate status:

- `IN_PROGRESS`.

### 2026-07-13T18:58:35+08:00 - Phase 0 - ASSESS_GATE

Actions:

- Added the scoped Contract E repository policy without changing the existing
  Academic Research Governance Profile.
- Froze route/factory/derivative identities and inventoried raw, reference,
  schema, consumer, harness, transport, and documentation anchors in JSON.
- Revoked v1 forward/score canonical admission centrally while preserving
  historical reads and all structural checks.
- Added forged-metadata, validator, emitter, and inclusive-aggregator fail-closed
  tests.
- Repaired early-veto error ordering after the first full central suite exposed
  that malformed v1 payloads were being rejected for policy before their actual
  structural defect.
- Updated clean central tests whose old success claims were wrong relative to the
  owner-selected canonical target.

Artifacts:

- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase0-route-freeze-manifest-2026-07-13.json`;
- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase0-policy-route-freeze-result-2026-07-13.md`;
- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase1-normative-mathematics-subplan-2026-07-13.md`;
- focused code and tests named in the result.

Checks:

- final CPU-hidden focused suite: `75 passed, 2 warnings`;
- Python compile: passed;
- JSON parse/semantic/path checks: passed;
- policy preservation and dirty-file hash checks: passed;
- `git diff --check`: passed.

Gate status:

- `PASSED_PENDING_HANDOFF_REVIEW`.

Next action:

- Run the bounded fresh-Codex Phase 0 result/Phase 1 handoff review, repair if
  material, then begin Phase 1.

### 2026-07-13 - Phase 0 - REVIEW_AND_CLOSE

Actions:

- Fresh read-only Codex reviewed the exact Phase 0/Phase 1 handoff packet.
- Review returned no material findings and `VERDICT: AGREE`.

Gate status:

- `PASSED_CLOSED`.

### 2026-07-13 - Phase 1 - PRECHECK

Evidence contract:

- Question: what exact finite Contract E--Chol program and total derivative will
  schema v2 and production code implement?
- Comparator: independent derivation, dense helper, TensorFlow float64 autodiff,
  directional FD, and the two existing LaTeX chapters.
- Primary criterion: all forward conventions, direct-plus-transport pullbacks,
  ridge semantics, active sets, numerical vetoes, FD safeguards, and paired
  uncertainty design are frozen before implementation or promotion outputs.
- Promotion vetoes: copied-but-unchecked helper math, omitted direct moment/weight
  path, ambiguous denominator/orientation, stopped adaptive ridge, arbitrary
  threshold, or invalid sequential coverage.
- Continuation vetoes: inconsistent target, no definable ridge chart, unresolved
  owner-required threshold decision, concurrent edit, or budget exhaustion.
- Nonclaims: no implementation, production, Kalman, HMC, nonlinear, or
  leaderboard correctness follows from the specification alone.

Skeptical audit:

- Existing LaTeX and helper are candidate references, not authority.
- The helper's residual injection uses an `N/(N-1)` scaling and a ridged
  Cholesky chart; both require explicit algebra before adoption.
- The helper already exposes direct source moment/weight VJP terms, but streaming
  composition must preserve them rather than replacing them with `Y+` only.
- Existing fixed one-step FD tests are insufficient for the new normative gate;
  a step ladder and active-set report are required.
- Threshold values will be derived before canonical promotion outputs are run.

Gate status:

- `IN_PROGRESS`.

### 2026-07-13 - Phase 1 - REVIEW_AND_CLOSE

Actions:

- Derived and checked the finite Contract E-Chol forward program, complete
  direct-plus-transport pullback, row quotient, population covariance,
  Cholesky orientation, fixed residual design, and prepared ridge semantics.
- Removed arbitrary numerical thresholds and preserved unresolved adequacy
  requirements as explicit later-phase blockers.
- Bounded scientific review converged after three iterations.
- Final focused suite: `14 passed`.

Gate status:

- `PASSED_CLOSED_MATHEMATICS_AND_DESIGN_ONLY`.

### 2026-07-13 - Phase 2 - REVIEW_AND_CLOSE

Actions:

- Added schema-v2 candidates and a factory-bound immutable semantic identity.
- Kept the production factory empty and every v2 artifact unadmitted.
- Repaired nested mutability, validator aliasing, and replaceable registries.
- Final Phase 0/2 compatibility suite: `104 passed, 2 warnings`.

Gate status:

- `PASSED_CLOSED_SCHEMA_ONLY_NOT_ADMITTED`.

### 2026-07-13 - Phase 3 - PRECHECK_AND_IMPLEMENT

Evidence contract:

- Question: does the owned cloud primitive implement the fixed-ridge Contract E
  equations and complete local derivatives without a dense transport matrix?
- Comparator: frozen rational certificates, dense fixed-ridge helper,
  TensorFlow autodiff, and JVP/VJP duality.
- Primary criterion: bounded exact-chart bitwise equality for all five inputs,
  noncommuting and transported-covariance coverage, XLA-on public wrappers.
- Promotion blockers: residual centering, mean, ridged-identity kernel error,
  raw ridge bias, conditioning/downstream error, and ridge-domain adequacy.
- Nonclaims: not general parity, streaming/full-filter correctness, GPU
  feasibility, Kalman/HMC/nonlinear/leaderboard readiness, or admission.

Actions:

- Added the owned TensorFlow cloud reset and focused tests.
- Implementation review iteration 1 found commuting-only factor coverage;
  repaired with an exact noncommuting chart.
- Iteration 2 found the `plus_cov -> gap_chol` branch inactive; repaired with an
  exact nonzero transported-covariance chart.
- Iteration 3 returned `VERDICT: AGREE`.

Gate status:

- `IN_PROGRESS_CLOSEOUT_REVIEW`.

### 2026-07-13 - Phase 3 - CLOSEOUT_REPAIR

Actions:

- Fresh bounded closeout review returned `VERDICT: REVISE` for missing evidence,
  not a cloud formula defect.
- Added certificate hash/status/dyadic/binary64 checks, every shared dense
  forward intermediate, a `B=2` exact check, explicit ridged-identity operation
  scale, and a persisted recomputable general-chart diagnostic artifact.
- Preserved the general chart as
  `INCONCLUSIVE_GENERAL_CHART_NO_JUSTIFIED_FORWARD_ERROR_BOUND`.
- Final Phase 0-3 compatibility suite: `134 passed, 2 warnings in 9.93s`.

Artifacts:

- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase3-cloud-reset-result-2026-07-13.md`;
- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase3-parity-diagnostics-2026-07-13.json`;
- Phase 3 manifest/log and review records;
- `docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase4-streaming-composition-subplan-2026-07-13.md`.

Gate status:

- `EXACT_ENGINEERING_CERTIFICATE_PASSED_GENERAL_PARITY_AND_PROMOTION_BLOCKED`.

Next action:

- Obtain bounded review of the repaired Phase 3 result and exact Phase 4
  handoff. Do not edit streaming code until that review converges.

### 2026-07-14 - Phase 4 - REVIEW_AND_CLOSE

Evidence contract:

- Question: does finite streaming compute `Y=Q/M` and the complete local
  Contract E direct-plus-transport derivative without dense production state?
- Comparator: exact quotient identities, same finite dense program, TensorFlow
  autodiff, JVP/VJP duality, CPU-XLA, and bounded trusted-GPU forward/VJP
  feasibility.
- Primary criterion: quotient and coordinate identities pass, mass derivative
  paths are present, no dense production path exists, and selected
  production-shape graphs execute without hard-veto failure.
- Promotion blockers: row/Sinkhorn/chunk adequacy plus all six inherited reset
  requirements.
- Nonclaims: no general numerical parity, full-filter/Kalman/HMC/nonlinear,
  admission, leaderboard, or release claim.

Actions:

- Repaired the missing row quotient by streaming augmented payload
  `[particles,1]`, carrying `Q,M,dQ,dM,barQ,barM` without a floor.
- Split generic transport geometry and payload dimensions.
- Composed direct source/probability-weight and transport
  source/normalized-log-weight paths using `w * G_w_probability`.
- Passed `134` Phase 0-3 compatibility checks, `7` Phase 4 eager/reference
  checks, and `1` isolated CPU-XLA wrapper check.
- Ran trusted RTX 4080 SUPER float32/TF32/XLA preflights at
  `B=1,N=10000,d=3`, chunks `1024`: forward and final analytic VJP passed valid
  chart/finite hard vetoes with peak GPU allocator approximately `64.8 MiB` and
  `80.9 MiB` respectively.
- The first compiled VJP aborted in XLA GEMM-fusion autotuning. A narrow,
  algebraically identical VJP-local broadcast-reduction repair passed full
  revalidation and bounded review before the second/final attempt passed.
- Wrote the result, manifest, focused log, complexity/graph audit, failure/
  repair records, and bounded review records.

Gate status:

- `ENGINEERING_COMPOSITION_AND_PRODUCTION_SHAPE_FEASIBILITY_PASSED_NUMERICAL_SCIENTIFIC_PROMOTION_BLOCKED`.
- Fresh bounded Phase 4 closeout review: `VERDICT: AGREE`.

### 2026-07-14 - Phase 5 - PLAN_REVIEW_AND_BUDGET_HANDOFF

Actions:

- Drafted a dedicated Phase 5 one-graph value/JVP/FD subplan.
- Iteration 1 review found an under-specified scalar, missing non-arbitrary
  derivative gate, unspecified `p` axis/aggregation, and wrong initialization
  test; repaired all four.
- Iteration 2 found implicit initial weights and a cleanup/certification handoff
  contradiction; froze `logw_{-1}=-log(N)` and split certified versus
  cleanup-only handoffs.
- Iteration 3 returned `VERDICT: AGREE`; the subplan is `REVIEWED_ACTIVE`.

Budget decision:

- Campaign birth: `2026-07-13T18:19:26+08:00`.
- Eight-hour ceiling: approximately `2026-07-14T02:19:26+08:00`.
- At the decision point (`2026-07-14T01:28+08:00`), approximately 51 minutes
  remained.
- The reviewed Phase 5 evidence contract requires a frozen fixture, new owned
  solve-based primal/JVP module, exact microcertificates, `0 ULP` same-core
  forward-autodiff gate, same-callable FD, compatibility, CPU-XLA, artifacts,
  and implementation/handoff reviews. That work cannot be completed validly in
  the remaining interval.

Gate status:

- `BUDGET_CONTINUATION_VETO_FIRED_BEFORE_PHASE_5_IMPLEMENTATION`.
- No Phase 5 implementation file was started. No evidence criterion was
  weakened.

Next action:

- Resume from the reviewed Phase 5 subplan in a new owner-authorized campaign;
  first freeze the additive `B=2,N=4,T=2` exact fixture before reading outputs.

### 2026-07-14 - Phase 5 - RESUMED_AND_CLOSED

Actions:

- Resumed under the owner-authorized continuation clock beginning
  `2026-07-14T01:32:19+08:00`.
- Implemented the owned canonical LGSSM finite value/manual-JVP graph.
- Passed the checked tiny manual-versus-forward-autodiff gate at `0 ULP` and a
  same-concrete-function CPU-XLA FD identity certificate.
- Final Phase 0-5 union: `150 passed, 2 warnings`.

Gate status:

- `CANONICAL_ONE_GRAPH_ZERO_ULP_ENGINEERING_CERTIFIED_NUMERICAL_SCIENTIFIC_PROMOTION_BLOCKED`.

### 2026-07-14 - Phase 6 - CLOSED

Actions:

- Required explicit diagnostic opt-in at every inventoried raw execution root.
- Demoted all successful v1/raw outputs to
  `historical_raw_barycentric_diagnostic_only`.
- Preserved structural validation ordering and removed obsolete admission
  branches without changing protected numerical kernels.
- Inventory ended with zero unclassified hits and exact kernel-hash parity.
- Six representative raw route records remained bitwise identical.
- Final focused union: `427 passed, 2 warnings in 403.26s`.

Gate status:

- `HISTORICAL_RAW_ROUTES_FAIL_CLOSED_AND_BITWISE_PRESERVED_CANONICAL_SCIENTIFIC_PROMOTION_BLOCKED`.

Next action:

- Execute the active Phase 7 LaTeX reconciliation subplan. Do not start a
  material Phase 8 oracle run until its equivalence margin is justified and
  frozen or explicitly resolved by the owner.

### 2026-07-14 - Phase 7 - CLOSED

Actions:

- Reconciled both named Contract E chapters with the normative Phase 1
  mathematics and checked owned TensorFlow code.
- Made transition-first timing, population moments, `Y=Q/M`, fixed prepared
  residual/ridge inputs, row-oriented Cholesky restoration, direct-plus-
  transport pullback, normalized-log-weight composition, and active/inactive
  carry semantics explicit.
- Classified raw compact/manual routes as historical-only with no fallback.
- Repaired two closeout notation ambiguities: locally self-contained row-vector
  moments and separation of OT settings from the frozen Contract E `rho=1`.
- Built the full 387-page monograph successfully and recorded pre-existing
  duplicate-label/bibliography warnings separately from Phase 7 changes.

Gate status:

- `NORMATIVE_DOCUMENTATION_RECONCILED_AND_MONOGRAPH_BUILT_SCIENTIFIC_PROMOTION_BLOCKED`.

Next action:

- Review and refine the active Phase 8 statistical-design subplan. Design and
  lower-rung harness work may proceed, but the final multi-seed oracle run is
  blocked until the owner freezes the practical gradient-equivalence margin,
  near-zero scale, simultaneous interval rule, and minimum replication.

### 2026-07-14 - Phase 8 Rung 0A - DTYPE REPAIR CLOSED

The canonical full-filter graph now uses one shared float32/float64
primal/manual-JVP core. Dtype-independent operation-order repairs aligned affine
flow assembly, geometry division, and the mandatory row quotient.

Gate status:

- exact tiny derivatives, CPU-XLA repeatability, branch identity, finiteness,
  source binding, and historical-route preservation pass for the stated checks;
- fresh float64 artifacts reproduce the Phase 5 v2 objective/score hex values
  and branch hash;
- the Phase 1 seven-step FD plateau/error-bound subgate remains
  `INCONCLUSIVE_BLOCKED` because callable-specific error bounds are undeclared;
- primary-shape execution remained blocked at this close pending the owner
  statistical-design amendment and then-unresolved formal FD disposition.
  This chronological statement is superseded by the representable-step close:
  the FD-only heuristic passed and the rigorous certificate is unsupported.

Result:
`docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase8-rung0a-dtype-repair-result-2026-07-14.md`.

### 2026-07-14 - Phase 8 Rung 0B - CLOSED

A dedicated canonical/Kalman tiny-fixture harness passed its finite, chart,
repeatability, and one-callable checks. A direct joint-Gaussian likelihood
independently matched the Kalman value exactly and gradient to roundoff. All
canonical-versus-Kalman differences are descriptive only.

The preparation audit found no defensible target-scale default for residual
design, ridge, reset policy, or transport settings. Target-prefix Rung 1 remains
blocked until the dedicated preparation/telemetry subplan converges; no
historical `1e-5` ridge or RNG salt was inherited.

Gate status:
`TINY_ORACLE_HARNESS_PASSED_DESCRIPTIVE_COMPARISON_ONLY_TARGET_PREPARATION_BLOCKED`.

Result:
`docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase8-rung0b-result-2026-07-14.md`.

### 2026-07-14 - Phase 8 Rung 1 Preparation And Telemetry - CLOSED

The owned preparation builder now freezes PHILOX key/domain/axis/dtype identity,
constructs the required centered population-scaled residual design, and requires
explicit ridge/reset/transport inputs. The canonical primal emits the declared
quotient, moment, covariance, Cholesky, ridge, and active-mask telemetry without
changing the finite scalar or derivative.

Evidence:

- preparation plus canonical union: `19 passed, 2 warnings in 102.86s`;
- fresh float64 manual JVP versus forward autodiff: zero ULP;
- fresh float64 CPU-XLA reproduces the Phase 5 v2 objective/score hex and branch
  hash exactly;
- fresh float32 CPU-XLA is finite, repeatable, branch-identical, and uses one
  concrete value-and-score callable;
- compilation, scoped diff validation, source hashes, focused-check record, and
  run manifest pass.

The tiny Claude health probe returned `CLAUDE_PROBE_OK`, but the substantive
one-path review was denied by the platform private-workspace disclosure
boundary. No bypass was attempted. The fresh Codex read-only substitute found
four execution-contract defects in the smoke draft: chart-failure handoff,
executable hard-check definitions, CPU/XLA provenance, and output/attempt
budget. These are same-plan repair triggers; no target result has been run.

Gate status at close (historical; superseded for FD disposition):
`PREPARATION_AND_TELEMETRY_INFRASTRUCTURE_CLOSED_TARGET_RIDGE_AND_FORMAL_FD_BLOCKED`.

Next action:
repair and re-review the CPU-hidden `T=1,N=4` target-prefix wiring smoke. No
target numerical hypothesis, `T=10`, GPU, or primary-shape run is authorized
by this close.

### 2026-07-14 - Phase 8 Rung 1 Target-Prefix Smoke - CLOSED

The reviewed CPU-hidden float64/XLA `T=1,N=4` smoke used dataset seed `81100`,
estimator seed `81120`, active-all reset, and the explicitly noncandidate
transferred settings ridge `4`, epsilon `1/2`, scaling `3/4`, two Sinkhorn
steps, and chunks `2`.

Evidence:

- harness tests: `6 passed, 2 warnings in 5.51s`;
- one authorized attempt completed in `15.169998470999417` seconds with only
  `/device:CPU:0` visible and XLA compiled for Host;
- all 18 hard checks passed, including frozen observation identity, one
  concrete callable, two-call serialized equality, all chart histories, and
  complete finite telemetry;
- no retry or numerical-setting change occurred.

Descriptively, Contract E versus Kalman value difference was
`0.17616214556824517` (`0.025200467300378712` relative to `abs(Kalman)`). These
and all score differences are explanatory only. The ridged covariance identity
residual was approximately `2.19e-15`, while the raw covariance residual norm
was `2.80` and matched the exact ridge prediction to approximately `1.23e-15`.
This distinguishes correct ridged algebra from an intentionally material raw
covariance shift under ridge `4`; it does not nominate another ridge.

Gate status at close (historical; superseded for FD disposition):
`TARGET_PREFIX_WIRING_SMOKE_PASSED_DESCRIPTIVE_ONLY_NUMERICAL_DESIGN_AND_FORMAL_FD_BLOCKED`.

Next action:
review the instrumentation-only target numerical-design subplan. Do not execute
another target value/gradient until its parameter-domain, bias, conditioning,
transport, chunk, seed, and budget requirements are frozen.

### 2026-07-14 - Phase 8 Rung 1 Numerical-Design Instrumentation - CLOSED

The reviewed instrumentation slice added pre-ridge plus moments, covariance
gap/eigenvalues, and full final-coupling row/column marginal diagnostics. The
column-mass reporting pass retains O(N) vectors and fixed blocks, not an N-by-N
coupling. No new target-prefix value or score ran.

Evidence:

- final instrumentation/compatibility bundle: `19 passed, 2 warnings in
  53.82s`;
- tiny dense row/column coupling definitions match the streaming reports;
- allocation audit passes;
- fresh aggregate/per-batch manual JVP agrees with forward autodiff at zero ULP
  on the frozen fixture;
- fresh float64 CPU-XLA objective/score hex, branch hash, center replay,
  endpoint charts, and one-callable identity reproduce Phase 5 v2 exactly.

Three initial local assertion/reporting defects were repaired and the unchanged
bundle rerun. They did not change the coupling, quotient, reset, derivative, or
target configuration.

Gate status:
`NUMERICAL_DESIGN_INSTRUMENTATION_PASSED_TARGET_NUMERICAL_REQUIREMENTS_BLOCKED`.

Next action:
obtain owner scientific direction for the parameter domain, fixed-ridge/raw-
bias and conditioning budgets, transport/chunk convergence budget, lower-rung
design, and complete primary-shape statistical amendment. The later
representable-step close retired formal FD certification as unsupported and
passed the FD-only heuristic, so formal FD is not a current blocker.

### 2026-07-14 - Phase 8 Same-Program FD Reclassification - CLOSED INCONCLUSIVE

The rigorous callable-error-bound FD certificate is unconditionally
`unsupported`; the required TensorFlow/XLA absolute forward-error bounds do not
exist in checked evidence. A separately reviewed seven-step FD-only heuristic
ran on the frozen Phase 5 fixture.

Attempt 1 failed before endpoint output due to a localized `tf.one_hot` API
call and was preserved. The single authorized retry completed all 35 records.
Center, prepared input, source, one-callable, branch, chart, CPU-hidden, and XLA
identity passed. Only 13 endpoint pairs had bitwise-symmetric representable
steps; all 13 passed with maximum relative error approximately `1.15e-9` versus
the FD-only threshold `0.1118`. The other 22 pairs were invalid solely for
actual plus/minus step asymmetry.

Gate status:
`FORMAL_FD_CERTIFICATE_UNSUPPORTED_HEURISTIC_SCREEN_INCONCLUSIVE_INVALID_ENDPOINT`.

Next action:
review the separate nearest-dyadic representable-step repair. Do not select the
13 valid pairs or run a third version of the closed ladder.

### 2026-07-14 - Phase 8 Representable-Step FD Repair - CLOSED

The reviewed nearest-dyadic base `2^-17` repaired only endpoint
representability. All 35 nominal steps (`2^-14` through `2^-20`) matched both
actual plus/minus steps bitwise, all branch/chart/identity and denominator
checks passed, and every FD-only relative error passed `0.05*sqrt(5)`.

Maximum relative error was `1.9172384855228604e-8` versus threshold
`0.1118033988749895`. The complete Phase 8 focused union then passed
`42 tests, 2 warnings in 213.04s`.

Gate status:
`SEVEN_STEP_FD_HEURISTIC_SCREEN_PASSED_FORMAL_CERTIFICATE_UNSUPPORTED`.

The rigorous callable-error-bound certificate remains unsupported and is not
required for the explicitly heuristic FD screen. This gate is not Kalman,
ridge, transport, HMC, or statistical evidence.

### 2026-07-14 - Phase 8 Owner-Decision Proposal - REVIEWED

The remaining Phase 8 scientific boundary was reduced to a concrete proposal
without observing another Contract E target result. Repository evidence fixes
the authoritative `benchmark_box`, transformed HMC coordinates, center, row
transport baseline, and legacy leaderboard seeds. The proposal separates the
center-scoped leaderboard question from full-box HMC readiness, defines a
center-local global-oracle-scale gradient contribution metric without invented
near-zero floors, freezes a finite staged lower-rung graph, and separates
lower-rung, calibration, and audit estimator seeds.

The first bounded scientific review returned seven material findings. Two
follow-up rounds found four and then three remaining definition/consistency
defects. All were repaired in the same proposal. The final bounded Codex
substitute review returned `VERDICT: AGREE`; Claude had returned no substantive
one-path review output despite a passing health probe.

Status:
`BLOCKED_HUMAN_DECISION`.

Next action:
the owner must approve or amend the reviewed scientific proposal, including the
gradient metric and `delta_grad`, scope separation, numerical stability design,
lower-rung graph/budget, seed split, fixed audit count, and Student/Bonferroni
model. Approval does not itself launch a command; the exact lower-rung harness
subplan must then pass skeptical audit and bounded review.

### 2026-07-14 - Kalman-Only Margin Decision Support - CLOSED

A reviewed CPU-hidden, non-JIT comparator-only exception evaluated the frozen
`T=50` Kalman oracle once without importing or loading the canonical Contract E
dependency closure. Preflight passed `5 tests`; the one no-retry command
completed in `7.4s`; all identity, finiteness, transform, chain-rule engineering,
dependency, and manifest checks passed.

All five HMC-coordinate oracle components are nonzero. This makes a floor-free
center-componentwise relative-gradient criterion executable and reveals that
the proposal's global scale is materially more permissive for the weak `phi3`
and `q_scale` oracle contributions. The result recommends the componentwise loss
for owner consideration but selects no margin.

Result status:
`KALMAN_ONLY_DECISION_SUPPORT_PASSED_OWNER_MARGIN_STILL_REQUIRED`.

Terminal bounded review: `VERDICT: AGREE`.

Result SHA-256:
`608a93f3fcf7c38a451fcb507123aa870b0d8858f0c2e9e31a662a1ab1382f1d`.

Current status remains `BLOCKED_HUMAN_DECISION`; no Contract E numerical arm is
authorized.

### 2026-07-14 - Current Campaign Terminal Veto

Final handoff consistency review returned `VERDICT: AGREE`. The current
eight-hour campaign cannot launch the reviewed lower-rung graph: its explicit
`14*300=4200` second worst-case node envelope exceeds the time remaining before
the immutable `2026-07-14T09:32:19+08:00` deadline, even before exact harness
drafting/review. The owner has also not yet selected the gradient loss/margin,
fixed audit count, or future continuation budget.

Terminal status:
`BLOCKED_HUMAN_DECISION_AND_IMMUTABLE_CAMPAIGN_BUDGET`.

This is a true continuation veto, not candidate rejection or program
completion. The canonical implementation, instrumentation, FD heuristic, and
Kalman decision support remain preserved; Phase 9, leaderboard regeneration,
HMC, release, and post-run completion audit remain unexecuted.

### 2026-07-14 - Post-Crash Recovery And Test-Isolation Repair

The three terminal-status documents passed `git diff --check` and remained
aligned on `BLOCKED_HUMAN_DECISION_AND_IMMUTABLE_CAMPAIGN_BUDGET`. Recovery did
not reset or extend the original campaign clock.

A stale handoff paragraph incorrectly stated that a center-scoped Phase 8 arm
must already prove full-domain ridge validity. It was reconciled with the
reviewed amendment: center-scoped Phase 8 may defer full-box HMC readiness, while
any later full-box/HMC-ready claim still requires the same fixed ridge to remain
valid over its declared domain. A bounded one-path Claude review returned
`VERDICT: AGREE`.

The mixed Phase 8 test union then exposed an order-dependent comparator-test
defect. The Kalman-only loaded-module audit was reading the parent pytest
process, where unrelated Phase 8 tests had legitimately imported canonical
modules. The test now executes that audit in a fresh CPU-hidden child process,
matching the actual comparator harness boundary without weakening the
standalone runtime guard. Focused comparator tests passed `5/5`; the exact
six-file mixed union passed `32/32` with two dependency deprecation warnings.

No Contract E candidate was evaluated, no `delta_grad` was selected, and no
runtime, Phase 9, leaderboard, HMC, release, or completion claim follows. The
remaining continuation gate is unchanged: owner approval of the center scope,
gradient loss/effect-size margin, lower-rung budget, and fixed audit design,
followed by a fresh bounded campaign.

### 2026-07-14 - Owner-Directed Phase 8 Continuation - ACTIVE GATE CLOSED

The owner approved center-scoped Phase 8, selected `delta_grad=0.05`, selected
audit count `16`, and authorized a fresh two-hour continuation beginning
`2026-07-14T11:55:26+08:00`. Full-box HMC readiness remains deferred. FD is a
separate implementation screen at `0.05*sqrt(5)`; the LGSSM value boundary is
`0.001`; Kalman remains the scientific oracle.

The initial `T=1,N=32` lower-rung design was found non-identifying because the
canonical program accumulates the only likelihood increment before the same-
time reset. It was repaired without tuning to `T=2,N=32`. The selected lower-
rung tuple passed engineering/FD checks but failed the Kalman screen. At
`T=2,N=32`, value relative error was about `1.3647%`; HMC-gradient component
relative errors were about `18.37%`, `9.63%`, `126.25%`, `2.84%`, and `4.53%`.

A one-seed `N=32,64,128` reset/no-reset scaling diagnostic was nonmonotone and
inconclusive. The frozen paired 16-seed audit at `T=2,N=128` was
`mixed_or_inconclusive`; both arms failed all six small-shape Kalman intervals.
Only `r_scale` showed a supported small reset-specific worsening under the
predeclared model. Explicit float64 Student recomputation changed the critical
value from `3.0362837314605713` to `3.036283222821165` and changed no
classification.

The decisive fixed-particle follow-up found no shared flow/weight wiring bug:
the affine conditional correction equals the analytic predictive density, its
manual JVP equals autodiff, normalization ties out, and an independent `T=2`
optimal-proposal sequential-importance recurrence matches canonical no-reset
value/autodiff/manual JVP to float64 roundoff. This shifts the leading
explanation toward finite-particle estimator bias/variance, without proving it
is the only cause.

Gate status:
`COMMON_PATH_IDENTITIES_PASSED_FINITE_PARTICLE_ACCURACY_GATE_REMAINS_BLOCKED`.

Next action: under a fresh bounded campaign, freeze and run a larger-`N`, multi-
seed finite-particle study with uncertainty before inspecting its output. Keep
the production factory empty; Phase 9, leaderboard regeneration, HMC, default,
release, and completion claims remain blocked.
