# Costs and capacity after the remaining SVD consumer repair

This E6 unit follows numerical, derivative and ownership qualification in
filter_gradient_remaining_svd_scale_unit_20260924.md. It asks whether the
magnitude-normalization repair changes compilation cost, warm execution or
host/device memory for the affected fitting consumers. The earlier48-worker
SRUKF cost cohort does not answer this question.

Bind the before implementation and its complete numerical dependency closure
to9696666e4; bind the after implementation to one frozen candidate source set.
Use the same deterministic orthogonal-frame quadratic designs as the scale
diagnostic, with7x3 and11x5 shapes, separated and nearly tied spectra, and
magnitudes1,1e-4,1e-10,1e4. First retain the original/candidate numerical status
for every design. A failed original XLA arm remains failure evidence and cannot
support a speed ratio. Exact precision, rank, status and condition checks retain
the scale diagnostic's1e-10 comparisons; no threshold or ridge changes.

Measure one representative separated-spectrum magnitude1 design at each shape
in fresh processes for before graph, before XLA, after graph and after XLA,
with three repetitions on CPU and GPU:48 workers. CPU and graph are explicit
references. GPU cohorts need one physical UUID, verified growth, uncontended
preflight and clean sampled sharing observations. Preserve the complete
returned records and verify synchronized replay before interpreting costs.

Record construction/tracing, first execution,20 synchronized warm calls,
changed operands and return-to-original replay. Measure observed process RSS,
mapping count, TensorFlow allocator current/peak and process GPU reservation
separately; reservation is not live tensor memory. Verify one trace, stable HLO
for changed operands, no host callbacks, and owner release. Retained native
executable memory requires a separate capacity/process-lifetime disposition.
Report ratios only for accurate arms with matched provenance, hardware and
execution settings. These repeated observations remain descriptive, not a
statistical superiority claim. A20% cold/warm cost or host/device memory increase
triggers attribution and a documented capacity disposition, never a silent waiver.

Reserve at most52 workers /7200 charged seconds, including four localized
harness/attribution attempts, inside the existing32CPU/52GPU-hour campaign caps.
Register bounded300-second workers under the same approved campaign runner.
Run one numerical worker at a time, freeze sources, write fresh numbered
artifacts and preserve all failures. Stop for invalid provenance, sharing,
scope change, exhausted unit/global budget or unexplained numerical failure.
Do not execute until the preceding consumer and ownership tests pass.

Review: comparing only SVD primitive times would miss fitting decisions and
materialization costs, so the measured program uses the actual dense, block,
sequential and quadratic consumers plus the COD condition helper. The
unchanged sequential consumer is included to keep the original diagnostic
composition fixed. Independent exact-quadratic checks prevent speed ranking
an inaccurate baseline. These are kernel-composition costs; final public and
actual-DZ5 capacities remain separate requirements. No default readiness,
posterior, HMC or final-merge claim follows from this unit alone.


Preparation checkpoint while the qualification source set is frozen: an
uninstalled harness draft is `/tmp/test_filter_repair_remaining_svd_cost.py`.
It uses FrozenCheckpoint to load the9696666e4 numerical dependency closure,
records every scale result, times the actual five-consumer composition, and
separates host mappings/RSS, TF allocator and process GPU reservation. The
code passes selected Ruff checks only; it has not run and is not qualification.

Pre-execution review still needs to resolve two harness details. First, direct
composition of the private fitting kernels may itself register caller graphs
through the existing COD custom gradient; observing Python-function collection
alone cannot be represented as graph/dependency collection. Use the already
qualified public ownership boundaries or explicitly attribute this diagnostic
composition before changing the ownership criterion. Second, the numerical
screen, sharing/provenance checks and saved raw arrays must be independently
validated by an analyzer before any old/new ratio is admitted. The draft is
not registered and no matched cohort should run until these points are fixed.

Pre-execution review revision after03708: the installed harness now traces a
shape-only five-consumer primitive before creating its caller. Offsets and
precision are explicit operands; the primitive has zero captured tensors.
The owning endpoint reads precision from a callback's resource. Changed-input
checks change both offsets and resource contents, then restore and replay.
Require collection of the complete caller function, FuncGraph, callback, owner
and resource. Record primitive graph collection separately: inherited COD
custom-gradient registry retention belongs to this deliberately reusable,
shape-only diagnostic primitive and is bounded by the fresh worker lifetime.
This does not prove release of that primitive or native executable eviction,
and cannot replace the already completed public ownership checks. Both before
and after arms use the identical boundary; construction includes both traces.

The new independent analyzer recomputes every scale/precision/rank/condition
screen from saved raw arrays using diagnostic NumPy SVD, verifies fixture
spectra, changed resource values, raw replay, complete owner collection, HLO,
source/environment/physical-device matching and observed sharing. Adverse
checks cover corrupt values/status/rank, omitted/duplicated scales, false flags,
retained dependencies, stale baselines, host callbacks and sharing. Run those
checks before the first cost pilot. The first D3 prior-graph and candidate-XLA
workers are discriminating pilots; stop on any unexplained failure. A passing
pilot can enter the cohort only if its complete source set remains unchanged.
The52-worker/7200-second allocation is unchanged and includes harness attempts.
No numerical runtime source or comparison tolerance changes in this revision.

03709 passes25 analyzer/preflight rejection checks. Pilot03710 fails before
numerical construction because the pinned fixed-center dependency imports
`bayesfilter.runtime.stable_config_hash`; the older reference loader supports
module files but not that lazy package export. The local cost loader now executes
the exact9696666e4 runtime package table and binds its lazy import resolution to
the same frozen loader. Both package and resolved runner bytes enter the source
hash closure. No current helper is substituted. Preserve03710 as a harness
failure and retry under the unchanged contract and allocation.

03711 reaches the completed numerical report but its diagnostic JSON writer
rejects a NumPy int32 scalar from tensor materialization. Preserve the partial
file and failed run. Materialize diagnostic outputs uniformly as NumPy arrays
so the existing reference-only cleaner handles scalar and matrix leaves; no
arithmetic, comparisons or runtime serialization path changes. The cohort has
not started and no cost claim is made from this failed artifact.

2026-09-25 interruption recovery:03712--03727 completed two CPU repeats.
The first GPU worker03728 wrote passing JUnit and complete numerical data, but
its parent did not record termination. No numerical process remains. Preserve
its running manifest and all files, exclude it from cost analysis and charge
the full300-second reservation under the existing runner. Recovery receipt:
`interrupted-cost-recovery-03728.json`. Retry in a new numbered directory.
The local worker allocation becomes54 (48 accepted cohort workers plus six
harness/recovery/final-check attempts), retaining the7200-second ceiling and
unchanged global32CPU/52GPU-hour caps. This is a localized supervision recovery,
not a changed target, numerical method, comparator, hardware class or gate.

The September25 canonical NeuTra directive does not change these deterministic
SVD/fitting kernels. They use no neural transport, train no map and cannot
establish canonical architecture or trained-map readiness. Historical transport
imports in the33-file baseline closure are dependencies only; no legacy recipe
executes in the measured program. Continue this exact frozen numerical cohort.


Final matched cohort through03760: CPU runs03712--03736 and GPU runs03737--03760
provide three complete repeats per arm/dimension. Analyzer reports are
`remaining-svd-cost-cpu-03736.json` and `remaining-svd-cost-gpu-03760.json`; the
immutable receipt is `remaining-svd-cost-receipt-03760.json`. Prior XLA fails
independent numerics and is excluded from all speed ratios. Candidate graph
parity is eligible. Candidate XLA has CPU cold ratios2.997/3.154 and host-RSS
increases344.1/354.7MiB (D3/D5), GPU cold ratios1.865/2.072 and host-RSS
increases20.3/20.6MiB, and a D5 GPU warm ratio1.509. These are attribution
triggers, not waivers. Caller/resource collection passes in all workers; the
shape-only primitive retention is explicit and bounded by process lifetime.
See `filter_gradient_remaining_svd_cost_result_20260925.md` for the decision
table and nonclaims. Public/actual-DZ5 capacities, native lifecycle and terminal
F01--F20 audit remain open.
