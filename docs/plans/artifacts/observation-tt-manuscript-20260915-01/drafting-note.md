# Phase 10: SGQF initialization, safety and independent filtering

Authority: the master program's phase 10, within the existing five-hour budget.
This is a bounded extension of the existing mathematical report, not a new
numerical protocol. Baseline TeX/PDF and SHA-256 are preserved in baseline.json.
Human reading feedback remains pending; this draft is provisional.

Reader: the research owner, comfortable with Gaussian filtering and linear
algebra, asking why a TT fitted with SGQF guidance can underperform SGQF. The
reader should be able to reconstruct the joint Gaussian, understand what
initialization preserves or loses, and distinguish a validation safeguard from
a population performance guarantee without searching another document.

Narrative: start with the already available SGQF joint. Derive its backward and
forward conditionals, then show the measured cost of polynomial conversion and
refinement on the same target. Explain why a joint defensive fraction can have
a larger conditional effect and derive the particle-level calibration. Carry
the remaining fitting/coverage uncertainty into independent filtering, adding
the latter's actual results only after the campaign completes.

Preserve the earlier equations, source classifications, original negative
findings, corrected Gram identity, and limits of theorem/gradient evidence.
Historical conclusions should be dated and linked to the new continuation,
not silently rewritten as if the earlier protocol had included these methods.
The new SGQF initialization and selector are extensions; do not claim author
source faithfulness, scalable high-dimensional initialization, HMC readiness,
or filter superiority from fit diagnostics.

Skeptical audit: the comparator must be the correlated analytical SGQF joint;
the old standalone current marginal is a separate weaker-information proposal.
Use the identical frozen fitting target for A04/A05 comparisons and identify
recursive incoming TT error. Keep safety calibration separate from target-error
tuning. A06 downstream comparisons require reference validity, regime coverage
and sequence-level uncertainty. No new default or scientific contract changes
are made in drafting.

Verification: compile the full document, inspect new rendered pages, compare
with the protected baseline for removed mathematical/source content, and record
all unresolved findings. An illustration of conditional mixture fractions may
be added from the stated formula; it is a deterministic explanatory plot, not
another numerical experiment or parameter-selection procedure.
