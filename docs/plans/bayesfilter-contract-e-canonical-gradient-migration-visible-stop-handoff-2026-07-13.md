# Visible Stop Handoff: Contract E Canonical LEDH Gradient Migration

Date: 2026-07-14

Status: `BLOCKED_HUMAN_DECISION_AND_IMMUTABLE_CAMPAIGN_BUDGET`

Detailed status:
`PHASE_8_KALMAN_DECISION_SUPPORT_PASSED_FUTURE_CONTINUATION_DESIGN_REVIEWED`.

## Current State

Phases 0-7 are closed at narrow policy, mathematics, schema, cloud reset,
streaming composition, tiny canonical-graph, historical-cleanup, and normative
documentation gates. The current continuation clock began at
`2026-07-14T01:32:19+08:00` and ends at approximately
`2026-07-14T09:32:19+08:00`; do not reset it.

The decision-ready amendment review closed at approximately
`2026-07-14T08:47:20+08:00`, still inside that original boundary. Execution
stopped for the owner scientific decision below, not because the clock expired.

Phase 5 status:
`CANONICAL_ONE_GRAPH_ZERO_ULP_ENGINEERING_CERTIFIED_NUMERICAL_SCIENTIFIC_PROMOTION_BLOCKED`.

Phase 6 status:
`HISTORICAL_RAW_ROUTES_FAIL_CLOSED_AND_BITWISE_PRESERVED_CANONICAL_SCIENTIFIC_PROMOTION_BLOCKED`.

Phase 7 status:
`NORMATIVE_DOCUMENTATION_RECONCILED_AND_MONOGRAPH_BUILT_SCIENTIFIC_PROMOTION_BLOCKED`.

Phase 8 Rung 0A status at close (historical; superseded for FD disposition):
`DTYPE_SHARED_CORE_REPAIR_PASSED_FORMAL_PHASE1_FD_SUBGATE_INCONCLUSIVE_PRIMARY_SHAPE_BLOCKED`.

Phase 8 Rung 0B status:
`TINY_ORACLE_HARNESS_PASSED_DESCRIPTIVE_COMPARISON_ONLY_TARGET_PREPARATION_BLOCKED`.

Phase 8 Rung 1 preparation/telemetry status at close (historical; superseded
for FD disposition):
`PREPARATION_AND_TELEMETRY_INFRASTRUCTURE_CLOSED_TARGET_RIDGE_AND_FORMAL_FD_BLOCKED`.

Phase 8 Rung 1 target-prefix smoke status at close (historical; superseded for
FD disposition):
`TARGET_PREFIX_WIRING_SMOKE_PASSED_DESCRIPTIVE_ONLY_NUMERICAL_DESIGN_AND_FORMAL_FD_BLOCKED`.

Phase 8 Rung 1 numerical-design instrumentation status:
`NUMERICAL_DESIGN_INSTRUMENTATION_PASSED_TARGET_NUMERICAL_REQUIREMENTS_BLOCKED`.

Phase 8 same-program FD status:
`SEVEN_STEP_FD_HEURISTIC_SCREEN_PASSED_FORMAL_CERTIFICATE_UNSUPPORTED`.

## Supported

- Contract E normative finite mathematics and complete local pullback;
- checked cloud and streaming composition engineering certificates;
- checked tiny canonical LGSSM manual JVP matching forward autodiff at `0 ULP`;
- shared float32/float64 full-filter cores with fresh CPU-XLA certificates and
  exact tiny JVP equality;
- source-bound PHILOX target-prefix preparation with no hidden ridge/reset/
  transport defaults, plus scalar-preserving canonical telemetry;
- CPU-hidden/XLA `T=1,N=4` target-prefix wiring, exact two-call replay, valid
  chart histories, and complete telemetry under a noncandidate transferred arm;
- pre-ridge gap/eigenvalue and full row/column marginal telemetry with tiny
  dense parity, frozen scalar/score preservation, and O(N) allocation audit;
- seven-step same-program FD-only heuristic on 35 exact representable endpoint
  pairs, with all pairs passing; formal callable-error-bound proof unsupported;
- v1/raw routes fail closed as explicit historical diagnostics;
- protected raw numerical kernels and six representative records unchanged;
- the two Contract E chapters agree with the normative mathematics and checked
  code, and the 387-page monograph builds;
- v2 remains unadmitted and the production factory remains empty.

## Not Supported

- general row/Sinkhorn/chunk/ridge/reset/conditioning adequacy;
- full `T=50,N=10000` canonical GPU/XLA feasibility;
- rigorous callable-error-bound FD proof; unsupported and not claimed, while
  the owner-directed heuristic implementation screen has passed;
- Kalman value/gradient equivalence or a justified equivalence margin;
- nonlinear Contract E validity or physical-support safety;
- v2 admission, HMC, leaderboard, default, release, or scientific readiness;
- post-run integrity clearance.

## Resume Point

Read the reviewed owner-decision amendment proposal for Phase 8. Do not execute another
target-prefix value/gradient, `T=10`, GPU, a new numerical arm, or the final
multi-seed result until the required parameter-domain, raw-bias/conditioning,
transport/chunk, seed, and budget requirements are frozen.

## Terminal Review And Checks

The bounded Claude one-path review returned no substantive output after the
earlier health probe had passed. Under repository review proportionality, a
fresh bounded Codex substitute reviewed the two terminal Phase 8 result files.
Its first verdict identified two scientific-wording defects; both were repaired,
and the follow-up verdict was `VERDICT: AGREE` with no material findings.

The previously completed CPU-hidden focused union passed `42 tests, 2 warnings
in 213.04s`. Final Python compilation, JSON parsing, scoped `git diff --check`,
status consistency, and source/artifact SHA-256 checks passed after the
documentation repair. The three canonical core source hashes and the
representable-step result hash remain unchanged from the recorded evidence.
After the decision-ready proposal converged, the authoritative row-contract
test passed `10 tests in 0.03s`; final proposal/handoff diff and status checks
also passed.

## Forbidden Shortcuts

- Do not reinterpret v1/raw artifacts as canonical or add a raw fallback.
- Do not register/admit the canonical route in Phase 8.
- Do not invent or silently reuse an LGSSM equivalence margin.
- Do not treat `0.05*sqrt(p)` as a general or Kalman gradient tolerance.
- Do not run HMC, nonlinear promotion, leaderboard regeneration, or release.

## Safest Next Action

Ask the owner to approve or amend
`docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase8-owner-decision-amendment-proposal-2026-07-14.md`.
The reviewed proposal removes the need for an arbitrary near-zero floor and for
primary-shape tuning, but the owner must still choose the scientific gradient
loss/margin (`delta_grad`), center-scoped versus full-box Phase 8 scope, lower-
rung design/budget, fixed audit count, and inferential model. Approval does not
launch a command; an exact lower-rung harness subplan remains required. The
formal rigorous FD certificate is unsupported and must not be revived without
actual callable error bounds.

Kalman-only decision support now shows that every frozen-center HMC oracle
gradient component is nonzero. The recommended loss is floor-free componentwise
relative error; the owner still must choose `delta_grad`. See
`docs/plans/bayesfilter-contract-e-canonical-gradient-migration-phase8-kalman-decision-support-result-2026-07-14.md`
for exact values and illustrative absolute budgets.

At `2026-07-14T09:08:59+08:00`, the remaining original clock was shorter than
the reviewed lower-rung proposal's `4200` second worst-case node envelope, even
before exact harness drafting/review. The lower rung is therefore not launchable
inside this campaign. Owner approval can freeze a future continuation design,
but cannot reset or extend the current eight-hour boundary.

## 2026-07-14 Continuation Handoff

The stale owner-decision block above was resolved in a fresh continuation. The
owner selected center scope, `delta_grad=0.05`, audit count `16`, and a two-hour
budget. The continuation ran a valid `T=2` lower rung, an `N`-scaling
diagnostic, a paired 16-seed audit, and fixed common-path identity checks.

Current Phase 8 status:
`COMMON_PATH_IDENTITIES_PASSED_FINITE_PARTICLE_ACCURACY_GATE_REMAINS_BLOCKED`.

Supported now:

- same-program FD-only screen passes separately;
- exact conditional affine proposal/weight value and derivative identities;
- independent `T=2` no-reset recursion value/autodiff/manual-JVP tie-out;
- 16-seed reset-versus-no-reset result is mixed/inconclusive, with only a small
  supported `r_scale` worsening;
- explicit float64 Student reporting correction changes no classification.

Still unsupported:

- the owner-selected `5%` Kalman-gradient equivalence criterion;
- the `0.1%` Kalman-value criterion at the tested small shape;
- primary `T=50,N=10000`, GPU/XLA, factory admission, nonlinear Phase 9, HMC,
  leaderboard, default, release, or program completion.

Safest next action: write a fresh bounded finite-particle study plan that freezes
larger `N` values and seed count before output, preserves the same oracle and
boundaries, and reports simultaneous uncertainty. Do not promote the route or
launch Phase 9 before that study passes its predeclared gate.
