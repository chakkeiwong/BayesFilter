# Complete paired quadratic refinement controller

Question: can the paired-local initializer's initial evaluation, replay,
ordered probes, model/factor decisions, trust proposals and bounded fit-round
recurrence execute in one stable TensorFlow/XLA program? This continues the
authorized execution repair; uniform-cloud dense fitting and its singular
condition-report mismatch are separate open work.

The numerical authority is the complete dependency closure at `3582b4ac`,
with its trust solve explicitly in ordinary graph mode because the original
XLA eigensolver has demonstrated errors. The current qualified paired probe,
batch evaluation, fitted precision and refined trust programs are building
blocks, not substitutes for the complete original public record. Preserve
the exact analytical target callback, seeds, probe stream, widths, all
thresholds, trust update, replay tolerance, callback order/counts, padding,
strict selection, failed-proposal retention, anchor freshness, optional
initial evidence and result/rejection precedence. No optimizer or method
change, ridge, score substitution or tolerance change is included.

Use fixed-capacity tensor histories sized from the existing planned-row and
round limits, with explicit active counts and field-presence masks. Only
completed records may be materialized on the host. The native while loop
must stop at the same round and callback as the original. A plain user target
may be called while tracing but no PyFunc, pfor or Python numerical recurrence
may run inside the compiled program. Static configuration and output-schema
construction are allowed; numerical decisions are tensors. This first stage
qualifies an isolated complete controller before public replacement.

The primary gate is every original value, score, selected index, physical/
padded/invalid row count, status, proposal, round model, factor and history
field at atol=rtol=1e-10; discrete decisions agree exactly. Assert and compare
execution metadata separately where a single compiled boundary necessarily
changes trace reporting. No numerical field can be dropped. Cover D1/3/5,
one/four rounds, centered/moving/nonquadratic targets, initial and per-design
invalidity, replay mismatches, proposal failure, model rejection and optional
initial evidence. Compare original callback rows and invocation order too.
Changed center, scale, seed and evidence must preserve one trace and all
runtime HLO inputs without data specialization. An enclosing-XLA call must
return the same complete numerical state. Unused history slots cannot affect
results or create target calls.

After correctness, compare complete original/controller graph/XLA costs in
fresh processes at D3/5, identical input/output lifetimes, twenty warm calls.
Include build/trace/cold/warm/materialization times, graph/HLO sizes, sampled
RSS/VmHWM and GPU allocator current/peak. Investigate >20% warm regression,
>256 MiB host increase, >2x device peak, materially higher cold cost or
continuing allocation growth. Cost diagnostics are descriptive, not a
statistical ranking or full composed locator/initializer speed claim.

Use only the existing absolute campaign driver with registered groups,
sequential CPU reference/GPU2 workers, GPU idle preflight and verified memory
growth. Bound each correctness group to 300 seconds and each cost process
to 120 seconds initially; a ceiling failure prompts splitting/inspection,
not altered data or iteration limits. Each launch has a fresh numbered
directory under the shared filter-gradient-repair-20260917 artifact root.
All attempts count against the unchanged 32 CPU / 52 GPU process-hour caps.
Do not edit source/tests/driver while a worker is active. Stop for broken
source isolation, missing required observations, uncontrolled growth or
budget exhaustion. Numerical or cost failures reject the candidate and
trigger localized repair; they do not authorize merge or scientific claims.

Skeptical review: tracing all conditional branches can change trace metadata
without changing executed calls, so callback counters and full original
histories are mandatory. Graph assertions can disappear under XLA; the
wrapper keeps static/initial-input validation and the controller carries
explicit tensor status for dynamic failures. Buffer capacity is derived from
the existing declared physical bound, not a convenient smaller fixture.
Recomputing reports or calling the target after the numerical return would
invalidate the claimed boundary and comparison. A passing paired controller
cannot close the uniform dense fitter, original D5 sequential mismatch,
external watchdog/DZ5 evidence, or terminal F01--F20 gates. Primary-agent
review accepts this bounded continuation; independent terminal review remains
pending. No external source/pin or environment changes are included.
