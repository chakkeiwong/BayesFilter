# Uniform-cloud enclosing XLA repair

Continue the approved execution repair after dense-condition qualification.
The original `3582b4ac` uniform-cloud initializer, with its trust solver in graph
mode, is the numerical authority. Preserve Philox draws, ordered train/check
clouds, exact selection, replay, factor checks, trust-radius recurrence, statuses,
complete histories and physical-row accounting. Only the approved deficient-rank
condition definition may differ. No optimizer, threshold, model or seed changes.

Reuse the existing paired controller's round recurrence with a static choice of
probe generator and fitter. Share ordered partition evaluation; generate uniform
double draws with the repository Philox bit conversion so XLA keeps the original
seed-to-draw map. Bind dimensions/capacities/configuration at construction and
keep seed, center, scale and initial evidence as runtime operands. Preserve the
single-entry callback-identity cache. Reporting only materializes completed
histories; no host numerical loop may remain in uniform refinement.

Skeptical review: ordinary XLA uniform-double generation changes the stream;
reuse alone is not evidence of parity. Test frozen original draws and complete
records before public wiring. The shared paired controller must retain its own
tests. Invalid batches must preserve which target calls ran and which physical
rows were charged. Changed inputs must preserve one trace and unchanged HLO
without captured input values. Original XLA trust errors are not an authority
for candidate acceptance. Compilation can be expensive even when warm calls
are faster; record cold and warm costs separately. This is primary-agent review.

Require CPU/GPU D1/D3/D5 original comparisons across accepted, moving,
nonquadratic, rejected-model, failed replay, invalid initial/train/check/proposal,
round-limit and initial-evidence cases. Check partial final batches, negative
seeds, nondefault cloud size/width and configuration identity. Check complete
original numerical records at `1e-10` and exact decisions/shapes/counts. Keep the
approved deficient-rank normalization explicit, archiving original raw ratios.
Require stable signatures, loop bodies, absence of Python callbacks, single
trace, unchanged HLO/runtime operands, and downstream public consumer checks.

Dense numerical dependency costs must pass before public XLA integration.
The first fresh-process cost matrix identifies a D5 warm-time trigger relative
to the original specialized solver. Preserve the trigger and qualify its repeat
range and complete-controller impact before public integration. A diagnostic
native controller can be implemented and tested while this remains open; that
does not enable or admit the public default. Existing paired/trust eigensolver
errors also justify explicit dense spectral stress fixtures (zero, indefinite,
near-repeated, extreme scale and near-singular precision) against both the
original graph and independent eigenvalues before enclosing the dense fitter.
After controller checks, compare original/graph/XLA whole public refinement at
D3/D5 with three fresh processes per arm/device, fixed inputs and synchronized
all-output materialization. Record host RSS/VmHWM and GPU allocator current/peak,
compile/first-call cost, warm times and changed-input behavior. Investigate the
existing >20% warm-time or >256 MiB host-memory triggers without relaxing them.
Use bounded repeated calls to distinguish compilation retention from continued
warm growth. These descriptive measurements support no statistical ranking or
general leak-freedom claim.

Use the same campaign driver and output root, one numerical worker at a time,
300-second focused workers (900 seconds only when the registered bounded group
requires it), GPU3 idle preflight and verified memory growth. The shared caps
remain 32 CPU/52 GPU process-hours. Preserve failures and repair locally under
the same scope; stop launches for contention, exhausted budget or invalid source
provenance. A failed parity/cost check blocks integration, not further repair.
Runtime/tests/driver stay frozen during workers.

Passing this stage does not complete multistart composition, the separate
iterative/block/sequential controllers, external watchdog/DZ5 qualification or
the F01--F20 terminal merge gates.

## Spectral boundary investigation

Runs02173 CPU and02174 GPU3 each pass10 and fail4 new boundary cases.
Near-identity D3/D5 XLA raw eigenvalues differ from the original graph by
6.19e-9/7.45e-9. Reuse the existing residual-refined eigensystem already qualified
for paired fitting; this solves the same eigenproblem and retains TensorFlow's
derivative convention. Qualify full fields, consumers, derivatives and costs
after the localized repair. Do not change any tolerance.

The other two failures are huge-scale diagonal fits: tiny relative matrix
differences (about1.2e-16--3.6e-16 of a3e150 matrix on CPU) occur in nominally
zero off-diagonal entries whose original roundoff is itself about1e133--1e134.
Entrywise1e-10 comparison fails there. Preserve this distinct failure and its
raw records; it is not evidence that the near-identity eigenvalue defect is
harmless, and the factor-output approval does not apply to dense precision.
No extreme-scale comparison criterion is relaxed here. Follow up original
self-sensitivity and independent least-squares backward error before deciding
whether this is an implementation or comparison-contract issue.

After the localized eigensystem change,02175 CPU/02176 GPU3 pass12/14 cases;
both near-identity failures are repaired at unchanged tolerance. Only the two
huge diagonal precision comparisons remain failed.02177 passes the CPU
independent100/160-digit least-squares reference and original one-ULP sensitivity
diagnostic. These explanatory checks do not waive the original comparison.
The cost-attribution/derivative/consumer matrix also keeps runtime sources
frozen and every worker under300 seconds. No public controller has been wired.

02181 is a derivative-harness failure: the original dense fitter uses
`MatrixSolveLs(fast=False)`, which has no TensorFlow derivative. It cannot be a
VJP authority. Preserve original primal comparisons, compare current graph/XLA
derivatives, and check the independent full-rank least-squares pullback for
`0.5*||sym(C)||_F^2 = 0.5*sum(eigenvalues^2)`. For `C=(A'A)^-1 A'B`,
`G=sym(C)`, `Z=(A'A)^-1 G`, the reference uses `dB=A Z` and
`dA=(B-A C)Z' - A Z C'`. The tiny reference design condition is below2; NumPy
is confined to this independent diagnostic. This is a harness correction,
with no runtime change or weakened numerical tolerance.

Component attribution02179 CPU/02180 GPU3 identifies the tall design SVD as the
main D5 XLA cost: about2.22 ms on GPU versus0.63 ms for native CPQR/COD and
0.81 ms for the raw eigensolver. Separate launches are explanatory and their
times are not additive. Test the equivalent reduced SVD for tall static
designs: with `A=Q R` and `Q'Q=I`, `A'A=R'R`, so the singular values of `A`
equal those of `R`. Apply this only inside XLA; the original graph path remains
the reference. Keep the same numerical rank threshold, reported values and
consumer decisions. This changes execution of the same SVD diagnostic, without
changing the least-squares fit or filter algorithm. Require full original and
rank-boundary checks, changed-input HLO, consumer checks and fresh matched costs
before integration. Preserve every earlier failed/timing artifact.


## Continuation after reduced-SVD qualification

02190--02194 pass140 checks. Fresh matched costs02195--02230 preserve all
initial/changed records at1e-10 but retain D5 warm triggers:24.2% CPU and60.0%
GPU3 versus original medians. This optimization has not resolved the cost gate.
No memory trigger fires. A prelaunch GPU3 contention stop is preserved in
`dense-cost-preflight-pause-02212.json`; the same batch resumed after idle checks.
Investigate reduced QR-plus-SVD and the refined eigensolver separately; compare
each to its original graph quantity. These component timings are explanatory
and cannot replace whole-endpoint costs. The graph arm must remain truly graph
mode, including for the refined eigensolver comparison.

The owner approved the extreme-precision proposal on2026-09-21. The comparator
is now installed only in the huge D3/D5 spectral fixture, with exact schemas,
shapes/types, unchanged other fields, independent100/160-digit references and
response-residual checks. Run mutation tests first, then the complete dense
spectral/analytical-derivative/consumer and quadratic numerical suites on CPU
and GPU3 before further runtime changes. No source change during workers.
