# M19 result: integration and repair-plan audit

Local HMC work was committed as `066deb5ce`; remote `3582b4ac5` was merged in an
isolated checkout and integrated into local main as `e6701cbf6`. The ordinary
public entry still rejects historical campaign overrides and uses the shared
candidate-set controller. The merge preserved extracted preparation and remote
filter recurrence changes. Git push was launched; the remote receipt is recorded
separately when it completes. No force push or history rewrite was requested.

The single textual conflict combined stage-specific evidence lengths with
issued-work identity checks. Testing found additional environment-sensitive
issues: a hardcoded original checkout path, Python 3.13 omitting empty AST
fields, and a non-package-relative NeuTra test import. These are repaired.
Five merged eigensolver loops were inspected and classified as static
permutation-index construction; actual matrix rotations remain in TensorFlow.
The guard still rejects a newly injected numerical loop. Initial missing local
custom-op build output and failed tests remain in the attempt records.

The final inventory has **478 named passing tests**, using the latest completed
report for each test. This includes candidate execution, historical checkpoint
compatibility, public dispatch, preparation, numerical Kalman/UKF checks,
scale-aware transport engineering smokes and seven phase-refresh tests. It is
not a whole-repository or posterior-validity claim. The phase-refresh helper now
updates the overall program status when it starts the next phase; stale terminal
timestamps cannot make an active continuation look complete. Tests cover active,
terminal and actual-blocker behavior.

All 25 tracked files dirty before main's fast-forward were preserved byte for
byte, including unrelated q20 work. New/raw prior campaign data were not removed.
The earlier publication inventory explains which compact records are in Git and
which 33 GB of raw historical evidence remain local.

## Scientific plan corrections

The saved-evidence audit establishes that centered-funnel preparation completed;
the observed failure is candidate search with nonfinite proposals and conflicting
acceptance evidence. The GPU attempt has no valid directional parent from which
existing failed-interval refinement can proceed. M20 therefore compares fresh
declared smaller-epsilon hypotheses under the same frozen geometry before
proposing more preparation changes.

M17's one-member precision results do not classify unassessed siblings. For the
rotated Gaussian, exact marginal scale makes the independent-draw median MCSE
planning approximation exceed 0.05 at 40000 total draws. Funnel child variance
is `exp(9/2)`, also making 0.05 absolute mean precision demanding. These facts
do not prove that correlated HMC cannot meet the target; they identify a scale
and budget explanation that must be distinguished from poor mixing. Original
precision gates and failures are retained. M20 predeclares multiple member
identities and records all unavailable groups and caps.

The refreshed master resolves all nine gaps into M19--M24, with actual repair
continuations, calibration requirements and a 48 CPU/24 GPU hour replacement
allowance. Old unused funds are not added. M19 charged 692.3060097771231 CPU
worker-seconds, including failed test invocations; its terminal ledger leaves
172107.69399022288 CPU and 86400 GPU worker-seconds. Ordinary read-only inspection
and editing are not numerical-worker charges.

| Decision | Primary criterion | Veto status | Main uncertainty | Next action | Limit |
| --- | --- | --- | --- | --- | --- |
| Integrate merged source | 478 named checks pass | All observed engineering failures repaired | Broader model behavior on merged filters | Fresh M20 source binding | No global posterior or whole-repo claim |
| Continue difficult-model repairs | Failure provenance checked; M20 resolved and audited | No shared numerical corruption found | Smaller epsilon may still fail in the funnel neck | Execute matched and fresh geometry probes | No default change |

| Inference status | Finding |
| --- | --- |
| Hard veto screen | M17 invalid proposals and posterior caps remain rejected |
| Statistically supported ranking | None |
| Descriptive differences | Member MCSE and acceptance histories only |
| Default readiness | Unchanged; optional settings remain hypotheses |
| Next evidence | M20 same-geometry search and predeclared posterior-member diagnostics, then fresh confirmation where justified |

Post-run review: passing engineering tests could conceal target-specific filter
or sampler failures, so fresh current-source integrations remain required. The
weakest scientific evidence is still replication/calibration. The master gives
that separate work rather than equating a completed matrix with closure.

Artifacts: `artifacts/hmc-repair-master-2026-09-16/m19-r1/`, especially
`saved-failure-audit.json`, `final-test-inventory.json`,
`reconciliation-terminal.json` and the preserved attempt XML/logs.
