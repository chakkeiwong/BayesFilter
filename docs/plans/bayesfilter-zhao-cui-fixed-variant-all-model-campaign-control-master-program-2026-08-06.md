# Zhao-Cui Fixed-Variant All-Model Campaign Control Master Program

Date: 2026-08-06

## Status

`HISTORICAL_CONTEXT_AFTER_METHOD_A_POLICY_RESET`

## Primary Mission

This master program is now historical context only.

It recorded the Austria-first Method B recovery block from the prior campaign state.
Under the active Method A policy reset, the immediate mission is no longer Method B
recovery; it is to reclassify Method A so that admission requires manual or
analytical score provenance.

## Scientific Target

Establish a value/score route that is useful for later parameter-estimation work.
This master program does **not** claim that the current campaign proves exact
physical-likelihood correctness, posterior correctness, or HMC readiness.

## Campaign Target

Show that tangent/interpolation extensions can be made same-scalar coherent and
replayable on top of fixed-variant parents.

For this execution block, the target is specifically:

1. recover Austria SIR Method B under the current source tree,
2. rerun the multimodel campaign with live Austria Method B rows,
3. preserve explicit blocked statuses for every remaining missing Method B row.

## Current Phase Target

Recover the Austria SIR Method B artifact chain under the current source closure
and rerun the fixed-variant multimodel campaign with fresh Austria Method B
paths.

The intended immediate sequence is:

1. fresh T1 score/tangent artifact,
2. fresh T1 training-JVP artifact,
3. fresh T2 training-JVP child artifact,
4. multimodel rerun with updated Austria Method B paths,
5. updated reset memo and result note.

## Inherited State

The active campaign surfaces are:

- `docs/plans/bayesfilter-fixed-variant-value-score-multimodel-plan-2026-08-04.md`
- `docs/plans/bayesfilter-fixed-variant-value-score-multimodel-target-registry-2026-08-04.json`
- `docs/plans/bayesfilter-fixed-variant-value-score-multimodel-reset-memo-2026-08-04.md`
- `docs/plans/bayesfilter-fixed-variant-value-score-multimodel-result-2026-08-04.md`
- `scripts/run_fixed_variant_value_score_multimodel_20260804.py`

Current scientifically relevant inherited facts:

- Method A is executed and same-scalar validated at the campaign baseline points
  across LGSSM, KSC SV, actual SV, predator-prey, and Austria SIR.
- Austria SIR is the only model with real in-tree Method B infrastructure.
- The current Austria Method B blockers are mechanical and narrow:
  - T1 score artifact stale under current strict source closure.
  - T2 extension row points at the wrong parent path and lacks a current JVP
    artifact path.
- Existing T1 parent and selected T2 parent remain compat-loadable under current
  source; the stale component is the T1 score/tangent artifact, not the whole
  Austria family.

## Governing Authority Order

When campaign artifacts disagree, use this authority order:

1. newest reviewed reset memo or master-program artifact for this campaign;
2. newest fixed-variant multimodel result note produced by this campaign;
3. current fixed-variant multimodel plan and registry;
4. Austria Method B loader/issuer code and current artifact manifests;
5. older historical Austria Lane-B artifacts as historical evidence only.

No lower-ranked artifact may silently override a higher-ranked mission or phase
statement.

## Explicit Goal Ledger

| Layer | Active statement |
| --- | --- |
| Primary mission | Make Method B real and reproducible, starting with Austria SIR. |
| Scientific target | A value/score route useful for later parameter-estimation work. |
| Campaign target | Same-scalar coherent and replayable Method B extensions on top of fixed-variant parents. |
| Current phase target | Recover Austria SIR Method B artifacts under current source closure and rerun the multimodel campaign with live Austria Method B rows. |

## Not The Goal

The following are **not** the goal of the current phase and must not displace it
by convenience:

- broad Method A expansion or fresh Method A redesign;
- source-native Zhao-Cui reimplementation;
- exact physical-likelihood adjudication;
- new neural or kernel surrogate families;
- cross-model Method B symmetry before Austria is recovered;
- broader HMC, posterior, or production-readiness argument;
- treating historical blocked Austria Method B rows as sufficient completion.

## Research Question Guardian

| Field | Contract |
| --- | --- |
| Main question | Can Austria SIR Method B be reissued under the current source tree so that its fixed-variant child rows execute again inside the multimodel campaign? |
| Baseline / comparator | Current Method A-admitted Austria row plus the historical Lane-B artifact chain and current strict loaders. |
| Promotion criterion | Fresh Austria Method B artifact chain loads strictly, returns finite value/score rows, and is consumed by the multimodel runner without manual patching of semantics. |
| Promotion veto | stale source-closure failure, missing artifact chain, wrong parent identity, non-finite child value/score, or rerun result note that silently changes Method A semantics. |
| Continuation veto | no current-source Austria Method B chain can be reissued within the trusted GPU budget, or loader/source-closure contracts are mathematically inconsistent. |
| Repair trigger | localized path, closure, issuer, JVP, selection, serialization, or runner wiring defect that preserves the current campaign target. |
| Explanatory only | wall time, allocator bytes, pilot ranking, validation metrics, historical arm differences. |
| Forbidden conclusion | Austria Method B recovery does not establish exact physical likelihood, posterior correctness, HMC readiness, or cross-model Method B availability. |

## Stage Gates

1. **Method-A-admission gate**
   - Already passed. No further work in this block should reframe Method A as the
     campaign destination.

2. **Austria-first gate**
   - Because Austria is the only model with real Method B infrastructure already
     in-tree, Austria recovery is the next required phase.

3. **Live-Austria-before-LGSSM gate**
   - LGSSM Method B may be considered only after Austria Method B rows are live in
     the multimodel result.

4. **No silent blocker reinterpretation gate**
   - Artifact staleness and missing JVP chains must be repaired or reported; they
     must not be reinterpreted as proof that Method B is not the goal.

5. **Result-artifact-before-advance gate**
   - No phase advance without an updated result artifact or reset memo recording
     what changed.

## Explicit Anti-Drift Vetoes

- `METHOD_A_GATE_TREATED_AS_DESTINATION`
- `AUSTRIA_METHOD_B_BLOCKER_REINTERPRETED_AS_MISSION_CHANGE`
- `SOURCE_NATIVE_ZHAO_CUI_REDIRECT`
- `EXACT_LIKELIHOOD_DEBATE_REPLACES_CURRENT_PHASE`
- `PHASE_ADVANCE_WITHOUT_RESULT_ARTIFACT`
- `GPU_REISSUE_WITHOUT_TRUSTED_EXECUTION`
- `RUNNER_REPAIRED_BY_WEAKENING_LOADER_SEMANTICS`
- `BLOCKED_METHOD_B_ROW_SILENTLY_DROPPED`

## Current Execution Program

### Phase 0 — Anti-drift document and campaign links

Create this master-program document and then link it from the current multimodel
plan, reset memo, and result note as the anti-drift authority.

Gate: mission / target ledger / not-the-goal / stage-gate / nonclaim sections are
all explicit.

### Phase 1 — Austria T1 Method B recovery

Reissue a fresh T1 score/tangent artifact under the current source closure using
existing Lane-B infrastructure.

Required outcome:
- `load_t1_score_artifact(...)` passes on the fresh artifact.
- The child returns finite value and score at the campaign theta.

### Phase 2 — Austria T1 training-JVP recovery

Issue a fresh T1 training-JVP artifact so the T2 training-JVP issuer can consume
it under current source.

Required outcome:
- `load_t1_training_jvp_child(...)` passes on the fresh issuer.

### Phase 3 — Austria T2 Method B recovery

Use the current compat-loadable selected T2 parent together with the fresh T1
issuer to issue a fresh T2 training-JVP child artifact.

Required outcome:
- `load_selected_t2_parameter_parent_compat(...)` passes.
- `load_t2_training_jvp_child(...)` passes on the fresh issuer.
- The T2 child returns finite value and score at the campaign theta.

### Phase 4 — Multimodel rerun

Update Austria Method B artifact paths in the multimodel runner to the fresh
artifacts and rerun the CPU-only multimodel summary.

Required outcome:
- Method A rows unchanged.
- Austria SIR Method B T1 row becomes `VALID`.
- Austria SIR T2 extension row becomes `VALID` if the full chain is recovered.
- Non-Austria Method B rows remain explicitly blocked unless new persisted routes
  actually exist.

### Phase 5 — Handoff artifact refresh

Update the fixed-variant multimodel result note and reset memo with:
- the anti-drift authority link,
- fresh Austria Method B artifact identities and paths,
- or the exact remaining blocker if recovery is incomplete.

## Why This Is Next

Austria is the only model with genuine Method B infrastructure already in the
repo. Recovering it is therefore the shortest path from the current state to the
campaign target. Any attempt to broaden to new models, new mathematical routes,
or new surrogate families before Austria recovery would be convenience drift.

## Execution Boundaries

- CPU-hidden runs are appropriate for multimodel summary generation and read-only
  diagnostics.
- Austria Method B artifact reissue requires trusted GPU execution and must keep
  the current strict loader/closure semantics.
- No package installation, environment mutation, or source-native Zhao-Cui route
  creation is authorized by this master program.

## Nonclaims

This execution block does **not** conclude:

- exact physical observed-data likelihood correctness,
- exact posterior correctness,
- HMC readiness,
- production/default readiness,
- source-faithful Zhao-Cui parameter inference,
- cross-model Method B availability beyond what is actually reissued.

## Required Handoff Fields

Any updated result/reset artifact produced under this master must state:

- which phase completed,
- current Austria Method B status,
- fresh artifact paths and identities,
- whether the multimodel runner now consumes live Austria Method B rows,
- exact remaining blocker if not,
- and the next permitted action under this master program.
