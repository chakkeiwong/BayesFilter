# Capacity and normalization mechanics

This continues the authorized master within the existing slice: at most 56 further numerical rows, 100 attempts, two additional GPU launches, and the remaining two-hour CPU/three-hour GPU slice envelopes. The parent budget is unchanged. Unique run directories preserve every attempt. Stop a dependent run for nonfinite values, invalid covariance/normalizer, missing derivatives, mismatched data/source identity, resource exhaustion, or the first budget limit. Failed settings trigger a bounded repair; descriptive failure to improve does not terminate the research direction.

## Capacity check

Question: do the actual shared consumers execute at moderately larger state dimension on GPU, and what compilation and allocator costs do they incur? Run Kalman, canonical LEDH, SGQF covariance and persistent-mixture covariance at d=4 and d=12, observation dimension 2, N=64, T=3, one dataset/stream each: eight rows. Use the frozen a99a1c55 numerical source, RTX 4080 SUPER, FP32/TF32/XLA and verified memory growth. Each dimension gets its own process with a 900-second timeout. Record kernel calls, traces, wall time and allocator peak; a single first call mixes compilation and execution and cannot establish throughput. Kalman supplies an exact affine comparator; the other cheap heuristics are not run because this is solely a resource/consumer check, with no quality comparison or promotion permitted.

Defaults audit: d=4/12 and N=64 are deliberately bounded engineering steps above the scalar pilot, not a high-dimensional research benchmark. Observation dimension 2 checks a non-scalar observation call chain. Existing flow/reset controls, SGQF level 2 and mixture within-fraction .5 are unpromoted mechanics choices. A failure may reflect conditioning or controls, not an inherent method limitation. All control values remain in the study. No new clipping, damping or ridge is introduced. The transported cloud uses the repository chunk selector K=N.

## Normalization and consistency check

Question: can the master distinguish mean finite-program log-score, ratio of mean numerator/normalizer, and particle-count consistency without silently granting unbiasedness? Use affine scalar N=8,16,32, T=4; prior SIS and canonical LEDH; two datasets, four particle streams: 48 rows. CPU FP64/XLA is an explicit small reference exception. Each particle count has a unique run directory and a 600-second timeout. Source, settings, partition and data identities must match before counts are joined. Kalman likelihood and all-six-parameter score accompany every row; numerical reference error therefore does not enter this fixture.

Write U=Zhat/Z and V=U*shat. This defines V from the same finite-program derivative; it does not independently prove that it estimates the likelihood derivative without bias. Report mean U, mean V, mean shat, (mean V)/(mean U), empirical bias and variance, denominator CV, reciprocal-normalizer tails and sample covariance of V with U. The exact empirical identity is mean(V) - mean(U)*mean(shat) = sample covariance with divisor R. The first-order ratio-of-means bias expansion is (E[V] Var(U)/E[U]^3 - Cov(V,U)/E[U]^2)/R under adequate moments and a controlled Taylor remainder. Treat its plug-in value as descriptive, especially with four replicates. It is distinct from the bias of each V/U.

Prior SIS has a smooth, fixed base-noise construction with no discrete resampling; differentiation under the expectation needs integrability/dominating conditions and is not inferred from a numerical pass. LEDH/OT has no assumed unbiased normalizer or derivative. All six initial-law terms remain present. Reciprocals, expansions and correlations are diagnostics, never unbiasedness or promotion certificates.

Across N,2N,4N report score-difference norms and their descriptive association with oracle score error. Common seed labels preserve each requested marginal; changed stateless tensor shapes are not asserted to provide prefix nesting or optimal coupling. Reject inconsistent data, scope, candidate configuration or replicate coverage. Zero variance yields undefined correlation, not zero. Four replicates and two datasets cannot calibrate a useful bias surrogate or establish consistency. Later Phase 4B must use independent calibration/evaluation and enough replicates before applying a combination selected using that measure.

Skeptical audit: these runs answer implementation/resource and report-semantics questions only. They cannot promote a provider based on covariance accuracy, a method based on a ratio identity, or a high-dimensional route from d=12. Existing nonlinear heuristic losses remain visible. Focused tests must distinguish individual ratios from ratios of means, verify ratio identities against exact finite arrays, and reject mismatched joined evidence before any pilot is interpreted. Artifacts: versioned states, normalization.json per run, consistency.json across counts, and a result/refresh note under this campaign root.
