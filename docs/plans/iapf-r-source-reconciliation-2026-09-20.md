# R iAPF source and fitting reconciliation

2026-09-20. The owner requests continuous execution until a real blocker.
This is the next task in the authorized independent R first-study replication.

Question: can primary author or supplementary material identify the actual
equation-15 optimizer, constraints, positive floor and controller convention,
and can the present R reference implement those choices without silently
changing the method? The successful log-quadratic variant remains a distinct
reference. It cannot close equation-15 conformance by repeated simulation.

Execute in sequence without a new permission checkpoint:

1. Inspect cached provenance and the final-paper/source locations. Use bounded
   publisher, author, DOI and code-repository lookups. Preserve fetched sources,
   timestamps, status codes and hashes. Do not contact authors or install code.
2. Reconcile each disputed operation against primary technical text/source.
   Record explicit instructions, local derivations and unknowns separately.
3. If a source-grounded localized correction exists, implement it, test the
   actual consumer call chain and run the smallest numerical discriminator.
   If source choices remain unknown, test any mathematically decidable
   identity or obstruction locally rather than relabeling a different method.
4. Refresh the master, record dispositions, and automatically continue eligible
   repairs. Pause only at a true unresolved dependency or resource boundary.

Evidence contract: exact comparison is equation 15 in the preserved GJL2017
paper and any verified author implementation. The primary pass condition is
operation-by-operation agreement with checked sources, with explicit unknowns.
A changed objective, proposal normalization or estimator target vetoes an
equivalence claim. A broken identity or corrupted source invalidates dependent
experiments. Metadata, citation counts, optimizer success codes and small
likelihood error cannot establish source identity. No method ranking, full
paper replication, or TF/LEDH/KDM/score/HMC promotion follows from this audit.
Stochastic experiments require a refreshed evidence contract before launch;
the earlier Kalman and conditional heuristic criteria are not relaxed.

Budget: reassign the preceding closed stage's unused 479.734033 CPU worker
seconds to at most 8 bounded diagnostic launches, including retries. This is
not additional campaign compute. At most 20 bounded source requests, each
with at most 30 seconds of network waiting; preserve failures and change the
retrieval method after repeated infrastructure errors. Source inspection and
ordinary edits continue without numerical workers. No paid compute, package
mutation or GPU work. Deliberate CPU references hide CUDA and use one BLAS/
OpenMP thread. Ordinary focused tests use the existing mechanics allowance.

Artifacts: docs/plans/artifacts/iapf-r-source-reconciliation-20260920-01/.
Every numerical launch needs a unique directory and command/environment,
source hashes, seeds (or deterministic designation), time, status and result.
Source requests and claim dispositions use a compact JSON/Markdown ledger.

Default audit: the cached accepted manuscript is a primary technical source
but publication-version parity must be checked; public R code has unverified
author provenance; log_quadratic/power8 is a frozen optional reconstruction;
finite optimizer bounds and floor definitions are hypotheses unless recovered
from source. Inspect the cheapest formula/call-chain check before any replay.
Do not select fresh claim seeds or tune using completed confirmation data.

Skeptical pre-execution audit: PASS for this bounded reconciliation. It directly
addresses the missing method identity, uses the actual paper rather than the
successful alternative as baseline, has finite requests/compute, preserves
unknowns and forbids another replication batch from masquerading as a source
repair. Missing original seeds may limit exact figure replication without
invalidating a transparently reconstructed study. A failed network request is
repairable; missing scientific specifications may block only dependent claims.

## Deterministic equation-15 discriminator

Before launch: use a fixed 5-by-5 grid on [-2,2]^2 and a Gaussian target with
unit marginal variances and correlation .6. Compare the actual profiled R
equation-15 objective with direct density arithmetic at diagonal covariances
(.64*s^2)I, s in {1,2,4,16,256,4096,1000000}. The correlated Gaussian is a
tractable target excluded by the diagonal fitting family. Its log-density
mixed contrast at (0,0),(1,0),(0,1),(1,1) equals .6/(1-.6^2), whereas every
diagonal Gaussian has zero contrast, independently of mean and scale.

Pass for demonstrating the obstruction: the direct and actual code losses
agree; all entries remain finite with no underflow flag; the last absolute
loss is less than 1e-12 times the first while its relative shape residual is
larger; the latter converges to 1-(sum y)^2/(N*sum(y^2)); and an exactly
representable diagonal Gaussian control retains zero loss to roundoff. No
optimizer search, data selection or stochastic ranking is involved. These
values are algebraic fixtures, not proposed numerical defaults. This test
answers whether the written global minimization is well-posed on all valid
fitting clouds, not whether a particular author's local optimizer failed.
The diagonal/full-covariance distinction is explicit; the heuristic comparison
gate is not a method-ranking question here. Timeout 30 seconds, charged to
the 479.734033-second diagnostic allowance. Pre-launch skeptical audit: PASS;
it tests the executed objective without changing or selecting a fitter.
