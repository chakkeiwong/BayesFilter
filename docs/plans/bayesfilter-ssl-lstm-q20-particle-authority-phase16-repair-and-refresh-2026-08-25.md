# Phase 16 Repair and Refresh Note

Status: `PASS_HARD_GATES_ROLE_LIMITED_INTERACTION_REPLICATED_REFRESHED_PHASE17`

The phase preserves the Phase 15 comparator and adds one independent
representation seed. It cannot promote an arm or coordinate system by itself.

| Failure | Classification | Repair |
|---|---|---|
| input/hash/mode-axis mismatch | harness/input | fail closed; refresh from the Phase 8 receipt |
| GPU memory/XLA/batch failure | infrastructure | repair launch or device setup; rerun in a fresh root |
| target/status/parity failure | implementation | isolate the affected coordinate route |
| absent/non-finite full-bank metrics | diagnostic harness | repair the metric path before interpretation |
| replicated poor residuals | candidate/evidence | retain the negative result and refresh Phase 17; do not stop the program |

After both receipts, record exact commands, hashes, device policy, wall times,
full-bank metrics, paired sign consistency, uncertainty limitations, and the
next subplan. A persistent infrastructure failure counts as a real blocker only
after three focused repairs under the program's global definition.

Both receipts passed. The sign of the arm-by-coordinate interaction replicated
across the two training seeds. This is descriptive evidence, not a universal
preconditioning result. Refresh Phase 17 toward source-faithful ETPF/GenUT/
LEDH contract checks, retaining the coordinate routes as diagnostic comparators.
