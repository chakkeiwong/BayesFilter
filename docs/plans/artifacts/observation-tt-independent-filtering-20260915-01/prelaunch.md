# A06 prelaunch record

The protocol was reviewed AGREE before implementation; the master and checkpoint
index that decision. Synthetic smoke-01 completed all eight methods, T=3 d=1,
with no method failure, in 19.711 seconds including startup/compilation. Its
observations use a separate mechanics seed and are not claim data. Compilation
dominates this small smoke; fresh d4 costs remain uncertain but the fixed
2400-second limit and 2700-second total numerical cap remain active.

Inference regression initially exposed a real harness issue: a sparse,
ineligible regime caused empty bootstrap cells to abort otherwise eligible
comparisons. The corrected inference excludes ineligible regimes from the
simultaneous maximum and emits no interval for them. This implements the
predeclared inconclusive rule without deleting an eligible comparison. The
paired-sequence/coverage regression passes on retry; both logs are preserved.

The data-generator graph is created once per dimension and reused with a
stable seed signature. The smoke already exercised that numerical program;
moving graph construction outside the sequence loop does not change its law.
Baseline proposal records now retain charts as well as coefficients.
No untouched observation data were generated before controls were frozen.
