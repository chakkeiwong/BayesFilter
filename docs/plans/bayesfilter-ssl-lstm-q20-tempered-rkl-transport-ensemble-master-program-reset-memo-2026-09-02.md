# Reset Memo: q=20 Tempered Transport Master Program

Date: 2026-09-02 (M3-C closeout refresh)  
Status: `MASTER_M3_TERMINAL_M3C_CLOSED_N3_BLOCKED_PHASE9B_BLOCKED`

## Active authority

The governing program is:

`docs/plans/bayesfilter-ssl-lstm-q20-tempered-rkl-transport-ensemble-master-program-2026-09-02.md`

The latest subordinate continuation is:

`docs/plans/bayesfilter-ssl-lstm-q20-performance-whitening-next-result-2026-09-02.md`

The 2026-08-25 particle-authority master remains historical for its completed
phases and direct LEDH blocker. The 2026-08-28 implementation plan remains the
detailed mathematical/engineering design. Neither independently authorizes a
future phase.

## Current state

- Phases 0--7 mechanics foundations are complete for their stated gates.
- Phase 8 C0--C5 is complete and freezes the K=2 compact-high L3 pure-
  continuation protocol for tuning only.
- The chart-1/beta-0 Phase 9A program repair passed localized mechanics with
  fresh v4 seeds. It is not robust tuning, whitening, posterior, or HMC
  evidence.
- The first canary exposed a launcher-root defect and a resource stop; the
  launcher was repaired and the focused regression passed.
- The corrected canary retry reproduced the resource stop under the unchanged
  cap. M3 is terminal under
  `docs/plans/bayesfilter-ssl-lstm-q20-phase9a-full-replay-performance-result-2026-09-02.md`.
- The full six-scope Phase 9A replay and Phase 9B retained sampling remain
  blocked.
- The bounded M3-C performance/whitening continuation is closed under its own
  budget and fresh artifact root. It did not reopen the M3 replay or Phase 9B.

## Historical pre-terminal entry

The pre-terminal ordered work was M3/R0--R1: audit timing evidence, add the new
`phase9a_full_replay_v1` source-owned profile and exact launcher, instrument
compile versus steady-state cost, and prove any execution-path repair
numerically equivalent before a GPU canary. The subsequent R2 canary has a
1,800-second cap; the later R4 six-scope replay has a 7,800-second cap. Those
ceilings are not current execution authorization.

That work is complete and superseded by the terminal closeout below. After
every stage, preserve the attempt, classify the failure, run the focused
regression, repair without changing frozen identities, and refresh the next
subplan. High acceptance, seed sensitivity, poor ESS, or poor whitening is a
repair trigger. It does not by itself stop the program or open Phase 9B.

## Terminal refresh

Attempt-02 used the repaired source-owned launcher root and a fresh output
directory. It completed 21 calls in `1645.967775` seconds before the fixed
`1800` second cap terminated the next call; outer wall time was
`1852.926691` seconds including termination grace. This was the second bounded
resource failure after R1, so `BLOCK_M3_CANARY_RESOURCE_VETO` is active.

No six-scope replay or Phase 9B command is valid under the current program. A
future continuation requires a new reviewed plan that explicitly changes the
performance design or budget; it may not widen the cap or reuse partial calls.

The new M3-C continuation satisfied that requirement. Its exact grouped-
transition and target-specific training commands were governed by the closed
subordinate plan and are now recorded in the result named above.

## Nonclaims

There is no current evidence for IID-Gaussian whitening, exhaustive mode
discovery, convergence, posterior correctness, sampler superiority,
high-dimensional scaling, production readiness, or a default HMC route.

## M3-C refresh

The predecessor diagnostic completed with finite target batching and score
authority checks but no valid grouped-HMC equivalence and no whitening. The
next plan therefore began with a source/seed audit, tested exact grouped
transition semantics, and ran a disjoint three-seed training ladder. Its
pre-execution review verdict was `PASS_M3C_BOUNDED_CONTINUATION`. The first
GPU attempt exposed an arm-dependent validation-bank defect; it is preserved
but quarantined for cross-arm interpretation. The harness was repaired to
share one held-out validation seed per seed index, and the second GPU attempt
completed the valid comparison.

The scalar and explicit row-loop HMC controls were exactly equivalent, but the
fast grouped TFP transition was not. The ladder produced 9/9 finite candidates
with 12/12 valid updates each, yet no arm met the two-of-three-seed, 10%
score-RMS nomination rule. M3-C is therefore closed without a nomination; N3,
M4, and Phase 9B remain blocked. The detailed evidence is in
`docs/plans/bayesfilter-ssl-lstm-q20-performance-whitening-next-result-2026-09-02.md`.
