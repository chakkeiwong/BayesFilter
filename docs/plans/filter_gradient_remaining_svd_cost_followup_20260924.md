# Costs and capacity after the remaining SVD consumer repair

This E6 unit follows numerical, derivative and ownership qualification in
filter_gradient_remaining_svd_scale_unit_20260924.md. It asks whether the
magnitude-normalization repair changes compilation cost, warm execution or
host/device memory for the affected fitting consumers. The earlier48-worker
SRUKF cost cohort does not answer this question.

Bind the before implementation and its complete numerical dependency closure
to9696666e4; bind the after implementation to one frozen candidate source set.
Use the same deterministic orthogonal-frame quadratic designs as the scale
diagnostic, with7x3 and11x5 shapes, separated and nearly tied spectra, and
magnitudes1,1e-4,1e-10,1e4. First retain the original/candidate numerical status
for every design. A failed original XLA arm remains failure evidence and cannot
support a speed ratio. Exact precision, rank, status and condition checks retain
the scale diagnostic's1e-10 comparisons; no threshold or ridge changes.

Measure one representative separated-spectrum magnitude1 design at each shape
in fresh processes for before graph, before XLA, after graph and after XLA,
with three repetitions on CPU and GPU:48 workers. CPU and graph are explicit
references. GPU cohorts need one physical UUID, verified growth, uncontended
preflight and clean sampled sharing observations. Preserve the complete
returned records and verify synchronized replay before interpreting costs.

Record construction/tracing, first execution,20 synchronized warm calls,
changed operands and return-to-original replay. Measure observed process RSS,
mapping count, TensorFlow allocator current/peak and process GPU reservation
separately; reservation is not live tensor memory. Verify one trace, stable HLO
for changed operands, no host callbacks, and owner release. Retained native
executable memory requires a separate capacity/process-lifetime disposition.
Report ratios only for accurate arms with matched provenance, hardware and
execution settings. These repeated observations remain descriptive, not a
statistical superiority claim. A20% cold/warm cost or host/device memory increase
triggers attribution and a documented capacity disposition, never a silent waiver.

Reserve at most52 workers /7200 charged seconds, including four localized
harness/attribution attempts, inside the existing32CPU/52GPU-hour campaign caps.
Register bounded300-second workers under the same approved campaign runner.
Run one numerical worker at a time, freeze sources, write fresh numbered
artifacts and preserve all failures. Stop for invalid provenance, sharing,
scope change, exhausted unit/global budget or unexplained numerical failure.
Do not execute until the preceding consumer and ownership tests pass.

Review: comparing only SVD primitive times would miss fitting decisions and
materialization costs, so the measured program uses the actual dense, block,
sequential and quadratic consumers plus the COD condition helper. The
unchanged sequential consumer is included to keep the original diagnostic
composition fixed. Independent exact-quadratic checks prevent speed ranking
an inaccurate baseline. These are kernel-composition costs; final public and
actual-DZ5 capacities remain separate requirements. No default readiness,
posterior, HMC or final-merge claim follows from this unit alone.


Preparation checkpoint while the qualification source set is frozen: an
uninstalled harness draft is `/tmp/test_filter_repair_remaining_svd_cost.py`.
It uses FrozenCheckpoint to load the9696666e4 numerical dependency closure,
records every scale result, times the actual five-consumer composition, and
separates host mappings/RSS, TF allocator and process GPU reservation. The
code passes selected Ruff checks only; it has not run and is not qualification.

Pre-execution review still needs to resolve two harness details. First, direct
composition of the private fitting kernels may itself register caller graphs
through the existing COD custom gradient; observing Python-function collection
alone cannot be represented as graph/dependency collection. Use the already
qualified public ownership boundaries or explicitly attribute this diagnostic
composition before changing the ownership criterion. Second, the numerical
screen, sharing/provenance checks and saved raw arrays must be independently
validated by an analyzer before any old/new ratio is admitted. The draft is
not registered and no matched cohort should run until these points are fixed.
