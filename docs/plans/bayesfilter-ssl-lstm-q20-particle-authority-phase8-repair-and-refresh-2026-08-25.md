# Phase 8 Repair and Refresh Note

Status: `PASS_MEASURE_AUDIT_REFRESHED_PHASE9`

Classify failures as input/metadata, receipt hashing, tensor shape/finite,
stage ledger, terminal-weight identity, acceptance accounting, mode-axis
alignment, and only then scientific interpretation. The auditor must not edit
or repair input receipts in place.

Record the legacy negative-control result separately. A missing `mode_axis`
field is an expected rejection for a legacy artifact, not a real blocker. A
repaired metadata-bound artifact must pass the exact same audit before it can
feed a downstream NeuTra rerun.

The next subplan must state whether any remaining issue is a ledger mismatch,
an input-authority gap, a training/tuning gap, or a target/transport
contradiction. HMC remains outside this phase and the entire current program.

## Executed disposition

The first audit attempt failed only because the auditor duplicated a
repository-relative receipt prefix; that harness issue was repaired. The
second audit on the metadata-bound N=300 bank passed all gates, including a
`4.34e-18` terminal-weight residual and zero mode-axis residual. The legacy
negative control was rejected for missing `mode_axis` as predeclared. Refresh
Phase 9 for a corrected mode-axis NeuTra rerun; keep the finite ledger
role-limited and keep HMC deferred.
