# A10 terminal review

Verdict: the requested documentation, mathematical audit, implementation and
bounded experiment are complete. Accept the localized numerical repairs and
the result record. Do not promote a filtering default or claim an analytical
total filter derivative. This is executor self-review; no independent terminal
approval or formal proof certificate is claimed.

## Scientific question and interpretation

The target remains the stated stochastic-volatility filtering distribution.
The actual consumer samples the physical mixture and evaluates its complete
density at the sampled point, using the exact transition-times-likelihood
importance numerator. Guide repair and independent coordinates change the
proposal approximation, not that numerator. These are authorized extensions,
not evidence of Zhao--Cui source faithfulness.

The final comparison uses the frozen A09 TT baseline, guide repair, independent
charts and charts with the transition mixture, together with four cheap
proposal comparators. No SGQF representation-loss criterion has been reinstated.
Filtering MSE is the primary quality measure. ESS, weight concentration and
fixed-order timings remain descriptive. The declared conditional heuristic
screen is a promotion veto, not a continuation veto or a statistical ranking.
Every repaired candidate loses descriptively to a cheap proposal in at least
one declared observation regime; that finding is prominent in the result.

All TT methods complete all 24 fresh cases without nonfinite, inverse-CDF or
log-evidence failures. One d4 reference exceeds its precision budget and is
excluded from admitted accuracy summaries for every method. The d4 comparisons
therefore have only eleven matched sequences and no complete-panel confidence
intervals. The scalar full arm's 19.0% lower MSE has the declared exploratory
paired interval below zero; no ranking between repaired arms is established.
The d4 full arm's 13.9% higher MSE is descriptive. Fixed rank and degree on a
broader chart are a plausible explanation, not a checked causal conclusion.

## Engineering and numerical checks

The implementation audit follows the actual fitting and particle-consumer call
chains. The 38 focused tests in tests-05.log pass, including both mixture
branches, importance identities, scale guards and signed initialization.
Smoke-03 reproduces the original negative least-squares scale at the actual
fit endpoint; healthy positive-scale arithmetic is unchanged. Calibration-02's
complete parsed selection ledger equals calibration-01. All earlier failures
remain preserved and are included in the attempt accounting.

The final terminal-checks.json records matching archived source hashes,
disjoint sequence seeds and 1,920 checked mixture steps. The minimum observed
log-density-bound margin is 1.6688872506165353e-11. Three unresolved guide
updates explicitly use the predictive Gaussian fallback. These finite checks
support the bounded mechanism result; they do not prove a realized ESS floor,
universal guide accuracy or absence of rare future numerical failures.

GPU/XLA, float64, memory-growth and device provenance are in the run manifests.
The final confirmation's source snapshot was made before plan closeout edits;
the archived plan, rather than a later edited working copy, is the run protocol.
Reference checking is intentionally a diagnostic role. No new default, HMC
consumer, package/environment change or production source-route claim is made.

## Mathematics and manuscript

Section 17 contains nine propositions with proofs. MathDevMCP inspected the
new equation targets; its derivation/formalization coverage was incomplete.
math-review.md records the findings, repaired assumptions and abstentions.
The sign-scaling proposition uses the fixed reference measure of the density
(Gaussian for the implemented polynomial TT). Magnitude scaling with additive
defense mass requires the stated homogeneity; global sign invariance does not.
The mixture second-moment bound is conditional and supplies no finite-sample
ESS guarantee. The smoothness result assumes fixed smooth component densities
and does not cover fitting, adaptive choices or particle resampling.

Final LaTeX passes 6 and 7 succeed and produce a 64-page PDF; pass 7 has no
undefined references, LaTeX warnings or overfull boxes. The final rendered
pages 61--64 and the preceding proposition pages were visually inspected.
The protected-baseline comparison records one 610-line insertion, with no
removed or replaced baseline lines. Human prose acceptance remains pending;
neither compilation nor model review certifies a human voice.

## Remaining uncertainty and next action

The strongest alternative explanation for the d4 error is inadequate capacity
or calibration in independent coordinates, with reference precision an
additional limitation. The next amendment should first define a more precise
reference and a bounded capacity diagnostic on fresh calibration data, then
freeze candidates before fresh confirmation. It must preserve the filtering
target, signed-scale fix, explicit fallback reporting and exact mixture
weights. No additional experiment was launched after the seven A10 attempt
slots were consumed. The master program and sole budget ledger carry closeout.

Evidence: ../../observation-aware-tt-robust-guide-20260916-result.md;
attempt-confirmation-02/comparison.json;
attempt-confirmation-02/terminal-checks.json;
manuscript-preservation.json; math-review.md; implementation-audit.md;
latex/build-pass7.log; latex/observation-aware-tt-a10.pdf.
