# M31--M38 terminal checkpoint

The owner-authorized funded roadmap has reached terminal M38. No worker is
running. The complete result and remaining requirements are in
`bayesfilter-hmc-m31-m38-result-2026-09-23.md`; the master and machine progress
carry the same disposition. Do not restart M31 or append an automatic M39.

Implemented: validation batch/lugsail/readiness options, matching fixed/stopped
estimation, source/design/data/stream-bound resume before cached returns, and
an independent normal-endpoint diagnostic with complete-denominator rules.
R-hat/ESS/MCSE remain posterior-only; all verified tuning candidates are retained.

Executed: baseline lifecycle tests (220 passed, one optional reference module
unavailable); three exact static/dynamic GPU model pairs; four paired GPU
normal-target activation controls; actual global-indicator tests; banana and
mixture CPU/GPU training-freeze-retune composition; final affected tests and
574-page official book build/render inspection. Test suite counts overlap.
Both tiny learned-map posterior runs failed and cannot support map quality.

Scientific status: R2/R3 confirmation underfunded at the reviewed design cost;
R4 global exploration and R7 serious learned-map quality open; R5 exact consumer
bundles absent. The optional ArviZ reference module requires matplotlib.
Legacy-module maintenance debt remains recorded. No numerical default changed.

Baseline: e9fee584704a465fc6ab984e3a8cc53980f335d0. M32/M34 GPU evidence uses
m38-r1/source-final-r2. The final host-side source-identity guard was tested on
r4; M37 and final book use r5. No historical evidence was relabeled. Numerical
snapshots exclude unrelated Q20/training, AGENTS, chapter26b and bibliography
edits. Preserve those worktree changes during selective publication.

Reconciliation: 2480.699 CPU and 1167.560 GPU worker-seconds charged;
71946.615 CPU and 74041.939 GPU worker-seconds remain. Receipts, classifications
and phase ledgers are under artifacts/hmc-repair-master-2026-09-16/m31-r1
through m38-r1. The reconciliation script charges each outer receipt once.

The owned code/tests/guide/plans and compact evidence form the publication
set. Git history records the publication commit. Remote main matched the
planning baseline at the pre-publication fetch; unrelated work is preserved.
