# Seeded enclosing dense initializer qualification

E5 asks whether the preserved external TensorFlow seed mapping, complete
attempt recurrence and completed JSON/NPZ records execute as one reusable XLA
calculation. The prepared-cloud controller and RNG components pass separately;
neither establishes that their composition preserves strict decisions.

The authority is the actual frozen MacroFinance `2f386f75` initializer, with
its original RNG statements intact and the pinned original BayesFilter locator
and fitter. The reference's false JIT locator flag is explicitly migrated to
true for this comparison, as in the prepared-cloud unit. Preserve the original
target, random stream, configuration and all comparison tolerances. Do not apply
either pending reporting proposal. Passing fixture tests does not qualify the
actual DZ5 target or a new public default.

Compose the two existing programs without copying their numerical algorithms.
Initial position, scale, seed and radius are operands; all shape/configuration
choices are construction metadata. Serialize calls using the same locator owner
lock, and synchronize before releasing it. Precomputing all bounded clouds is
permitted: it consumes no target rows and must not change callback order. Output
derivatives remain frozen. Keep file writing and completed record traversal on
the host with no numerical feedback.

Compare full original results, every NPZ member, exact callback counts/extents
and ordered positions in healthy D1/D3 and invalid-locator, invalid-cloud,
score-cap and fit-rejection cases. Three calls use original, changed and original
seed/radius/center/scale operands. Require unchanged HLO, one trace, exact same-
backend replay, frozen derivatives and collection of callback, owners and every
dependency graph. Existing prepared-cloud tests remain the recurrence/retry and
error-precedence authority. A new seeded mismatch blocks promotion and triggers
attribution; no tolerance or decision change is implied.

Reserve 26 workers / 4800 charged seconds within unchanged 32 CPU / 52 GPU
process-hour caps. Use the stable campaign runner with registered
`dense_seeded_cpu` and `dense_seeded_gpu` matrices and 300-second test timeouts.
One numerical worker runs at a time with sources frozen. Use new numbered
artifacts with commands, source hashes, seeds, environment, HLO, full records,
device identity and verified growth. Foreign load permits correctness checks
only; matched memory/performance measurements remain separate. Stop execution
for invalid evidence, changed scientific scope or exhausted budget.

Review: component RNG tolerances could conceal a changed strict incumbent or
curvature selection; only full source records can answer this. Reusing prepared
clouds in the authority would miss the composition question, so the reference
must execute its actual RNG. Callback and complete archive comparisons prevent
a passing final scale from hiding changed data or early-exit behavior. The
original and changed seeds are fixed probes, not a statistical quality claim.

The initial unregistered isotropic D3/D23 trial is preserved as exploratory
evidence only (see the 03535 checkpoint). Before qualifying seeded composition,
run a registered explanatory attribution on both the original frozen clouds and
the generated clouds. Evaluate original and current public fitters at identical
center/score/cloud inputs and compare each precision to the exact identity.
Retain raw complete records, family selection and factor rank/anchors; this
diagnostic cannot waive their differences. The qualifying healthy D3 fixture
uses the same specified one-factor covariance as the prepared-cloud matrix;
that does not close the distinct isotropic finding. Save evidence before
assertions so failures remain inspectable. Use at most three attribution workers
within the same 26-worker/4800-second allowance.

Attribution 03536 completes on original/generated clouds and finds the same
D3/D23 differences in standalone fitting: the current D23 precision equals the
identity, while the old-reference factor fit has up to 1.63e-7 error, zero
optimizer iterations, and rank46 versus current45. Raw factor anchors differ.
These are unexplained original-comparison differences, not qualification.

Reference audit then found `original_fitter` loaded the old public fitter and
factor module but resolved `fit_dense_score_precision_tf` from current code.
Pin that third numerical module to3582b4ac before further qualification. Its
original dependency closure is TensorFlow/stdlib; the unused mass-artifact type
does not enter these records. Renew attribution and all affected complete-record
qualification before terminal closure. Preserve prior evidence as partial
reference-isolation evidence, not a fully isolated original authority. This
repair changes neither runtime nor tolerance and uses the existing allowance.

The third attribution worker replays the original weighted fit, factor initial
state and loss gradient on archived 03537 clouds. It tests whether the inherited
loading floor at repeated correlation eigenvalues explains the returned precision
and zero optimizer iterations. This is explanatory only and cannot waive complete
record comparisons. Renewed CPU seeded cases 03538--03547 all pass; GPU remains.

Reporting follow-up: source review finds that the shared completed fitter
formatter hard-codes jit_compile=True even when an internal graph-reference
owner is explicitly configured False. Reproduce this using a D3 seeded graph
reference, preserving its full report. Bind the completed raw execution flag to
the controller's actual configuration and propagate it into fitter diagnostics;
keep default True and every numerical operation/threshold unchanged. Reserve
at most three workers for reproduction, repair check and default renewal within
the same4800-second/global caps. The unchanged seeded and prepared comparisons
then verify default records during the reference renewal. This is execution
provenance, not authority to admit graph references as default evidence.

Review: deriving the flag from a caller's reporting guess would retain the
bug. The numerical owner must issue the flag, and formatting must consume that
flag. The regression inspects the actual outer function's _XlaMustCompile
attribute and completed fit diagnostics. No scientific criterion is relaxed.

Reporting outcome:03609 reproduces outer=False versus fit flags=[True,True].
The owner now emits the completed setting and the formatter propagates it;
03610 passes. Default-XLA seeded D3 records, archives, callbacks, reuse and
collection pass03611. All129 policy checks pass03612, with three existing
reporting hashes refreshed and no new exception. Public fitters retain their
True default. No numerical algorithm or comparison threshold changed.
