# Objective-resolution error qualification

Current final CPU checkpoint:273 checks pass on one frozen source cohort,
03057--03098. GPU qualification is pending availability; main remains unmerged.
Receipt: `artifacts/filter-gradient-repair-20260917/objective-resolution-checkpoint-03098.json`.


The owner approved `objective_resolution_limited` on2026-09-23. The sequential
controller now evaluates that exact finite one-ULP predicate after its completed
lifecycle and before mass preparation. Rejected results expose no candidate,
precision or covariance. They preserve history, evaluation counts, terminal
attempt count, terminal score and seed metadata. Ordered controllers charge the
completed evaluation count and reject the handoff before replay or later blocks.

The initial literal floating comparison failed at zero/subnormal boundaries in
03037 because CPU XLA flushes subnormal comparisons. The installed TensorFlow
integer representation computes the identical predicate without floating
comparison: for binary64 sign/magnitude bits, subtract the magnitude from2**63
for negatives and add it for nonnegatives. Both signed zeros map to2**63;
finite numerical order is increasing, and adjacent distinct values differ by
one. Finite operands, executed proposal and rejected-promotion conditions still
apply. This changes no target value, score or proposal. Independent
`math.nextafter` boundaries include both signs, signed zeros, subnormals, the
normal/subnormal boundary, ties, two-step improvements, reverse moves,
infinities, NaNs and inactive proposals.

Initial validation03038--03049 passes: the scalar/batch conditional and public
sequential results preserve99 target evaluations and the complete previous
lifecycle while skipping mass preparation. A healthy positive control invokes
that same instrumented mass dependency. Both real public block calls stop at100
physical rows without replay or a later block. Ten compiled boundary injections
cover the new completed-error accounting and prior veto/handoff rules. Six
ordered full-record cases pass, including unchanged unflagged partial inputs
with201 evaluations, one trace and unchanged HLO. The failed03028 original
comparison remains archived and is not relabeled as equivalence.

03050--03056 passed seven original public groups before a deliberate between-
worker pause to retain additional completed terminal metadata on the rejection.
The final source cohort begins03057; qualification continues under the approved
90-worker/14,400-second tranche and unchanged32 CPU/52 GPU-hour campaign caps.
GPU preflight24601 declined before any worker under foreign compute contention;
no GPU qualification is inferred from CPU results.

Preliminary review: the flag is only a representation-resolution test for
rejected proposal promotions. It does not bound opaque target error, handle
larger cancellation or fit-cloud/search promotions, prove convergence, repair
the separate healthy initializer mismatch, or establish native executable
cache eviction. Accepted-result tolerances and all unflagged original decisions
remain unchanged. CPU/GPU renewal, terminal costs, broader endpoint audit,
actual DZ5 migration and main integration remain campaign obligations.

Final guard cohort03057--03062 passes all15 CPU checks, including retained
terminal metadata. GPU retry48597 also declined before worker launch: no UUID
remained eligible for two consecutive samples. The full original public CPU
matrix resumed at03063. A read-only E5 freshness check confirms the actual
MacroFinance initialize_dense_local source remains SHA256
7c4d5598959fcd9605bed01d16d416f2002fd442301e82264ff396b1aa050f75 with non-JIT
locator, Python attempts/partitions and NumPy assembly/archive operations. This
sequential guard does not repair or qualify that separate caller. Remote main
was fetched at0643b0adc; the repair branch has no upstream divergence. Remote
integration/retests remain pending full campaign qualification.

Final public renewal03063--03081 passes23 original/public boundary checks, and
03082--03089 passes all56 existing sequential consumers. Both comparisons retain
original fields, discrete choices, evaluation counts and analytical target calls
at unchanged tolerances. Block/reporting renewal follows on the same frozen
source. Earlier CPU numerical results are descriptive checkpoints; no old source
cohort is relabeled as this final cohort.

03090--03095 renew all six ordered-block cases on the final source. Each preserves
one trace and HLO across changing inputs. Coupled and unflagged partial records
retain strict original3582b4ac comparisons; only a true runtime guard flag
selects the approved rejection/no-use comparison. The existing legacy public
block controller remains installed pending GPU qualification and complete
ordered-endpoint admission. This repair does not relabel that Python outer loop
as compiled.

Final review and disposition:03096 passes43 existing block consumers,03097
passes actual outer graph/target collection and retained-handle reuse, and03098
passes129 policy tests. Focused test Ruff, critical-error runtime/runner Ruff,
whitespace and the227-source/1332-exception static guard pass. No exception was
added. All273 final CPU checks execute without failures or skips;62 total
workers including the preserved failure and pre-final cohorts consumed
1700.00070101046 CPU seconds. Cumulative charges are62400.59496179693 CPU and
59401.31348332534 GPU seconds, leaving14.67/35.50 hours within32/52-hour caps.

The strongest remaining failure possibility is a cancellation error outside the
narrow predicate: passing these fixtures does not justify a broad tolerance or
error bound. The tests establish no-use for this predicate, not recovery of a
resolved improvement or legacy bitwise arithmetic. Python owner collection does
not establish native executable eviction. No independent subagent review was
used. The reviewed implementation is retained on the repair branch; no public
outer-block switch or main merge is justified yet.

Next: resume the registered objective_resolution_gpu matrix on an eligible
non-desktop UUID, then block_capture_gpu, block_dependency_derivatives_gpu,
block_controller_gpu, block_boundaries_gpu, block_outer_ownership_gpu and the
affected public/consumer GPU groups. Respect the remaining28-worker local
tranche allowance; renew its attempt allocation before exceeding90 total,
without increasing cumulative compute caps. Preserve any GPU arithmetic
mismatch for diagnosis rather than assuming that CPU flags must reproduce on
a different backend. Complete public ordered wiring only after its gates.
Healthy initializer rounding, actual DZ5 source/deadline migration, clean
matched GPU costs, native signature churn, F01--F20 dispositions and remote
integration/retest remain separate tasks. No worker is active at this checkpoint.
