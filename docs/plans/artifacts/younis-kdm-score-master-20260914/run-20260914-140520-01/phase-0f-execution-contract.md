# Phase0F stochastic finite-difference integration

Question: do three-point central, five-point fourth-order and eleven-point cubic-fit derivatives of the verified LEDH value program preserve marginal sampling laws, reconstruct a full score and report stochastic error correctly? Comparator: analytical total derivative of the identical finite LEDH program plus the exact Gaussian model score. Full-score MSE against the model oracle is the stated eventual accuracy criterion; this tiny run tests implementation only.

Run40 CPU/FP64/XLA rows: one calibration dataset and one independent validation dataset, two particle replicates each, three stencils, positive step ladder0.02,0.01,0.005 using common innovations, and independent-node central3 at0.02. Seven directions: six coordinate axes plus normalized all-ones. Native coordinates include log noise/initial standard deviations. Explicit bounds keep every node valid; all nodes must be distinguishable at working precision. N8,d2,T3 and canonical controls from the previous mechanics fixture are unpromoted hypotheses. Exact oracle node values expose truncation directly. The reported amplification budget1.1 is a geometry check for this specified axis-plus-diagonal matrix (minimum singular value1); it does not certify that the actual directional error is at most1.

Skeptical audit: FD of a discontinuous resampling program is not its fixed-ancestor analytical derivative, and either may differ from the model score. LEDH uses its actual deterministic ContractE reset; controls and residual design remain fixed within common-innovation stencils. Independent nodes independently redraw all random designs; their marginal algorithms remain the same. No smoothing or transition score construction is introduced. Zero-weight center calls and oracle/baseline calls are retained and charged honestly. No universal variance slope or U-curve is assumed. The measured empirical MSE includes the bias cross-term and within-stencil covariance. Direction rank, all finite values, unchanged data and covariance reconstruction are validity vetoes. Reconstruction residual and ESS are explanatory; neither establishes score accuracy.

Selection protocol: predeclare dataset-mean vector MSE; choose stencil/h/coupling using calibration only and evaluate that choice on the disjoint validation dataset. Save every candidate. One dataset per role and two particle replicates cannot establish a ranking. Final claim rows remain blocked until scope-bound FD selection consumption is implemented and a properly replicated untouched comparison is prepared. Cross-direction covariance is saved; cross-stencil covariance and final admission belong to the same0F work, not out-of-program prerequisites.

Budget:40 rows,1200 seconds,48 attempts with at most2 per row. This consumes40 of the50 remaining rows in the current120-row engineering slice; one GPU launch remains in the slice, seven in the whole12-launch allocation. Four subsequent GPU smoke rows may use that launch after CPU checks. Failed infrastructure attempts are preserved and repaired within these budgets. Stop for a broken target identity, invalid artifact, exhausted budget or nonlocal scientific-contract change. Poor FD accuracy rejects that setting, not the research direction.

Artifact root: current run's fd-cpu-01, full logs, finite-differences.json, source manifest and phase result. Compile time is included in total cost; no timing superiority claim. All scientific comparisons require nonlinear regimes, scope tuning, practitioner baselines and paired uncertainty before promotion.

Selection repair refresh: the40-row run succeeded (31.288s). Implement frozen FD design issuance/consumption in the next protected source snapshot; rerun8rows on fresh calibration110/validation120 datasets and2heldout mechanics rows on130. The two three-point candidate steps0.02/0.01 exercise the same predefined mechanism, not a scientific selection or retuning on the earlier validation outcomes. This uses the last10rows of this engineering slice. Full controls/FD design/data/backend/source checks reject stale, changed, overlapping and mechanics-to-science selections.
