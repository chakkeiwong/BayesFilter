# HMC continuation reset, September 22

The owner authorizes commit, merge with remote main, conflict resolution, push
to main, refreshed plan audit and continued execution. The new continuation
ceiling is 48 CPU-reference worker hours and 24 GPU worker hours. Conservatively,
this is the available continuation budget, replacing rather than adding the
unused M18 balance. Historical charges remain unchanged in their ledgers.

## Integration plan and skeptical audit

Local main is `d86dadf68`; fetched remote main is `3582b4ac5`. Remote changes
include a resumable HMC campaign and compiled numerical filters, not merely
documentation. Local uncommitted q20 work is separate and must be preserved.
Commit the HMC implementation, tests, documentation and compact campaign
records by explicit path, excluding unrelated q20 changes and the 33 GB raw
campaign tree. Preserve those raw records locally; publish an inventory of the
selected receipts and execution scripts rather than claim Git contains them.
The official PDF remains a local generated output under repository policy.

Merge in an isolated checkout and reconcile algorithmic behavior, especially
the extracted preparation modules versus remote edits to the old facade.
Keep the public shared candidate-set scheduler, all verified members, independent
verification and posterior/tuning separation. Remote historical campaign
support must not restore a single-winner public route or R-hat tuning gates.
Run relevant remote checkpoint/dispatch, local extraction/recovery and public
integration tests on the merged source before pushing. Preserve remote filter
improvements and validate affected adapters; do not choose one entire conflict
side solely because it compiles. No force-push or destructive reset is needed.

Skeptical audit: the M18 source is not the post-merge source, so its source-bound
checkpoints cannot be silently resumed with merged code. Keep immutable old
snapshots for failure diagnosis and start fresh source-bound experiments on the
merged version. A passing unit count cannot establish new-filter posterior
validity. Git integration is engineering work; numerical evidence requires the
separate refreshed experiment design. Existing research authorization covers
routine repair/retry within this ceiling. The first research phase must be
resolved after inspecting the actual saved failures and merged behavior.

## Required master refresh

Give each of the nine remaining gaps a bounded work package, exact baseline,
diagnostic roles, measured cost pilot, continuation/repair conditions and
observable completion criterion. Prioritize difficult geometry and retained
member precision, then delayed burn-in and stopping calibration, null-size and
whole-procedure defect sensitivity, learned training and exact consumer binding.
Do not equate exhausting a phase checklist with fixing a reproducible failure.
Expected model failures trigger the next relevant repair. Missing exact
MacroFinance inputs affect that cell only. Numerical defaults remain unchanged
unless target-specific evidence justifies an explicit later decision.

This reset is an execution record and budget interpretation. The master remains
the single program; its next resolved design will be reviewed before numerical
launches. No new approval ceremony or external reviewer dependency is introduced.
