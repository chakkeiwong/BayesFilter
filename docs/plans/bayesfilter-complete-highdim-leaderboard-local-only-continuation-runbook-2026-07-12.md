# Complete High-Dimensional Leaderboard Local-Only Continuation Runbook

Date: 2026-07-12

Run ID: `complete-highdim-leaderboard-local-20260712-134906`

Status: `ACTIVE_PHASE1_P1A_P1B_REISSUE_AFTER_OWNER_AUTHORITY_REPAIR`

Preserved historical stop record:
`docs/plans/bayesfilter-complete-highdim-leaderboard-local-only-continuation-stop-handoff-2026-07-12.md`.

Resume record:
`docs/plans/bayesfilter-complete-highdim-leaderboard-local-only-continuation-resume-record-2026-07-12.md`.

P1-C harness edits remain forbidden until superseding P1-A and P1-B receipts
both pass. No GPU command, cell admission, or release action is authorized by
the authority repair or either pre-implementation gate.

## Purpose And Authority

Continue the reviewed scientific master program phase-by-phase in the current
Codex session without the rejected nested-model launch boundary. This runbook
changes execution mechanics only. It does not change the six target rows, four
algorithms, parameter coordinates, LEDH seeds, finite-difference policy,
scientific gates, or release criteria.

The owner approved this local-only repair after the exact detached wrapper for
`complete-highdim-leaderboard-20260711-221500` was rejected twice before child
process creation. The old run did not start. Its reviewed package and frozen
source snapshot remain immutable evidence of a rejected prelaunch lane; they
are not a live workspace and cannot be relabeled as this continuation.

Codex in the current managed conversation is supervisor and executor. There is
no runtime child `codex` or `claude` CLI process, external model/API call, copied
credential, credential propagation, detached agent launcher, or rejected outer
wrapper is permitted. No network access is required or authorized by this
runbook.

## Identity Separation

| Item | Binding |
| --- | --- |
| Rejected run | `complete-highdim-leaderboard-20260711-221500` |
| Local continuation | `complete-highdim-leaderboard-local-20260712-134906` |
| Master program | `docs/plans/bayesfilter-complete-highdim-leaderboard-master-program-2026-07-11.md` |
| Phase 0 result | `PASS_PHASE0_BOUNDARY_FREEZE` |
| Original Phase 0 freeze SHA-256 | `4115ef55114ffd73255363f0c62c4a19dd85d7ca3241d002c48409cb9004f878` (`historical_superseded_authority_identity`) |
| Owner-authority amendment SHA-256 | `171b8d42ec5f31869181003636a223b4e3063e38c03a66e1df7f77782d81ce90` |
| Repaired Phase 0 freeze SHA-256 | `4c7d602e16f4856457873347d254b2d48b78ecd3e034f30992f3b45aa72d356b` |
| Original reviewed Phase 1 subplan SHA-256 | `ff75b73fdbc2f75c0d5f05c0ac835fdfec69cc7ccd1448b47c6f66b2d9ebb62b` (`pre-amendment review identity`) |
| Active Phase 1 entry overlay | `docs/plans/bayesfilter-complete-highdim-leaderboard-phase1-entry-amendment-2026-07-12.md` |
| Failed P1-A receipt SHA-256 | `3299d3b797aa41b028fe77ec4d3aabb639fa176da73767fe3b4905a2d614ff67` (`preserved failure`) |
| Pre-edit Phase 0 harness SHA-256 | `2bd7c4c62773657213ccd488c9e55b96f3f7d6d4a3b00a7aaf2a8fb070031d58` |
| Git commit at continuation activation | `d269f5bbd8531b878d4f25897a357fbc8f172488` |
| Activation time | `2026-07-12T14:59:28+08:00` |
| Hard wall-clock deadline | `2026-07-12T22:59:28+08:00` |

The following old-package identities are retained only to prevent accidental
reuse or mutation:

| Old-package artifact | SHA-256 |
| --- | --- |
| Exact launch manifest | `527d2a9c9bd36a5694a8e13853e481ccc25233b601e6a926509fc914967355ae` |
| Iteration-6 review packet | `aaec9ea06fa3cc47f34529ec8118f50a5c1a3ade7fb5e46f924fdea59af4e89d` |
| Iteration-6 review record | `46bdaeb12c6d5a7d80ea8b05469f36d499f118a5c69ba055f79aa6b477e23be1` |
| Launch-readiness receipt | `a412e005172f3bdfc865f8d7a759dddd6e9b921f33b95043876350e908e06726` |
| Frozen inventory | `080eab22cfffa073faaba48b6e44c21908fce056aa758a1386930ed4cd86669f` |
| Runtime fingerprint | `598f9b031eee92c7cf6a51e09ae1a84a7ea4c395f0e1a6f1786a0fddf03cf616` |

The old launch-control `66/66` pass is infrastructure evidence only. It is not
Phase 1 evidence, numerical leaderboard evidence, or launch authority for this
run.

## Research Intent Ledger

| Field | Binding |
| --- | --- |
| Main question | Can the frozen six-row by four-algorithm matrix be completed with target-matched finite total values and total score vectors, without substituting targets or promoting diagnostics? |
| Candidate/mechanism | Current TensorFlow/TFP evaluator routes, including GPU/XLA/TF32 LEDH execution and source-grounded fixed-variant Zhao-Cui routes. |
| Expected failure mode | A row target or source mapping is not exact; a score/value pair is mismatched; an individual FD direction fails; a legacy cell cannot be re-admitted; or a dependency is incomplete. |
| Promotion criterion | Exactly 24 independently admitted main cells and every Phase 8/9 completeness and integrity gate passes. |
| Promotion veto | Any invalid target signature, ungrounded Zhao-Cui route, failed cell invariant, missing five-seed LEDH record, failed individual FD direction, stale dependency, or failed final completeness check. |
| Continuation veto | Invalid harness/data/source/artifact, contradictory target identity, unapproved `extension_or_invention`, missing required diagnostics, unsafe dirty-work overlap, required new authority, or wall-clock deadline. |
| Repair trigger | A candidate-specific implementation, tuning, schema, aggregation, or dependency failure that does not invalidate the target or harness. |
| Explanatory diagnostics | Runtime, memory, prefix/smoke results, CPU-hidden compilation, singleton-seed results, and descriptive seed variation. |
| Must not be concluded | Ranking, superiority, HMC/posterior correctness, confidence coverage, broad scientific validity, or repository-default readiness. |

Before stopping after a failed candidate, classify whether the result invalidates
the harness, implementation, target, data, math, or artifact, or merely rejects
the current candidate. A candidate failure that triggers a planned repair does
not stop the program unless a continuation veto fires.

## Evidence Contract

| Field | Contract |
| --- | --- |
| Engineering/scientific question | Can the master program advance locally while preserving exact targets, source identity, paired value/score semantics, and per-seed evidence? |
| Exact baseline | Phase 0 SHA-frozen July 3 non-LEDH candidates plus the current schema-v4 five-row LEDH harness. July 6/7 artifacts are historical target/shape or forward-value context only. |
| Primary pass criterion | Each phase satisfies its reviewed entry conditions, primary criterion, veto screen, local checks, result record, and exact next-phase handoff. |
| Hard veto diagnostics | Target/signature mismatch, source-anchor failure, nonfinite or malformed artifact, individual seed/direction FD failure, dependency incompleteness, GPU provenance failure for production runs, or Phase 8/9 validator failure. |
| Explanatory-only diagnostics | Smoke/prefix values, runtime, memory, CPU-hidden compilation, and descriptive stochastic differences. |
| FD-only rule | For every seed and coordinate, `relative_error <= 0.05 * sqrt(p)`, with FD reconstructed from stored finite representably distinct endpoint totals. FD is validation only and is never the admitted score. |
| Stochastic evidence | LEDH uses exactly paired seeds `81120..81124`; no aggregate may hide a failed seed or direction. Means are descriptive cell construction, not statistical ranking evidence. |
| Preserved artifacts | Phase subplans/results, canonical target and source-anchor ledgers, per-seed structured results, exact command manifests, dependency manifests, tests/logs, final leaderboard outputs, and the separate local post-run audit. |

## Skeptical Plan Audit

| Required challenge | Finding and control |
| --- | --- |
| Wrong baseline | The July 6 LEDH leaderboard contains superseded score evidence. It remains historical only; current LEDH cells require five new paired seeds. |
| Proxy promotion | Prefix, smoke, CPU-hidden, singleton-seed, FD, runtime, and memory evidence cannot admit a cell or establish scientific validity. |
| Missing stop conditions | Each phase inherits the master vetoes. P1-A and P1-B stop before implementation on target drift, source contradiction, or required invention. |
| Unfair comparison | The program fills target-matched cells and does not rank algorithms by mixed old/new runtime or descriptive metrics. |
| Hidden assumptions | Phase 1 must materialize byte-level canonical targets and exact row-specific Zhao-Cui availability before benchmark edits or numeric runs. |
| Stale context | Current harness and Phase 1 hashes were rechecked at activation. Any later computation-relevant drift invalidates dependent evidence. |
| Environment mismatch | Phase 1 is deliberate CPU-hidden engineering work. Later production numeric work requires trusted/escalated TensorFlow GPU/XLA/TF32 execution and structured provenance. |
| Artifact insufficiency | Every serious run must write a run manifest and terminal structured artifact; Phase 8 reconciles static and observed dependencies before release. |
| Rejected launcher reuse | The old wrapper is prohibited. Current-session commands execute directly and synchronously or through bounded command sessions, never through nested model CLIs. |
| Review mismatch | The old Claude/Codex launch reviews do not review local scientific results. Local checks and explicit skeptical result audits remain required; no old verdict can authorize a cell. |
| Eight-hour cap | The deadline is a continuation veto. At expiry, write a resumable blocker/handoff without claiming numeric completeness. |

Audit decision: `PASS_LOCAL_CONTINUATION_WITH_AMENDED_PHASE1_ENTRY_GATES`. The
local-only repair and owner-authority overlay remove the rejected execution
boundary and SIR identity contradiction without weakening a scientific,
target, source-claim, numerical, GPU, completeness, or release gate.

## Execution State Machine

For each phase:

1. `PRECHECK`: verify the prior result, current subplan, hashes, research intent,
   evidence contract, and entry conditions.
2. `EXECUTE_MINIMAL`: run the smallest discriminating gate before a broad or
   expensive command.
3. `ASSESS`: apply promotion, continuation, and repair classifications.
4. `REPAIR`: patch a fixable in-scope defect, rerun focused checks, and preserve
   failed evidence.
5. `CLOSE`: write the phase result, manifests, inference-status and decision
   tables, post-run red team, and exact hashes.
6. `HANDOFF`: draft or refresh the next phase subplan and verify its consistency,
   feasibility, artifact coverage, and boundary safety before execution.

## Immediate Phase 1 Sequence

Phase 1 is deliberate CPU-hidden engineering work. Every command that imports
TensorFlow must set `CUDA_VISIBLE_DEVICES=-1` before import and record that
choice. Do not run a Phase 2 GPU ladder during Phase 1.

1. `P1-A canonical target freeze`: materialize the exact six target identities
   without editing the frozen harness; enforce the reviewed canonical byte
   encoding and independent ordered field ledgers; write the canonical-target
   artifact and hashed P1-A receipt; stop on any drift or contradiction.
2. `P1-B Zhao-Cui availability`: inspect the primary paper's relevant technical
   and example sections and exact pinned author-source paths for every row;
   record exact row mapping, preliminary route classification, gaps, and a
   hashed receipt; stop if any candidate already requires an unapproved
   `extension_or_invention`.
3. `P1-C harness implementation`: only after P1-A and P1-B pass, add LGSSM and
   the fail-closed paired value/score, endpoint, per-seed, aggregation, target,
   route, and provenance checks plus adversarial tests.
4. `P1-D future command freeze`: generate and check deterministic Phase 2/3 argv
   manifests without running them.
5. Write the Phase 1 result and Phase 2 subplan before any trusted GPU command.

Required Phase 1 artifacts retain the exact paths declared by the reviewed
Phase 1 subplan.

## Later Numeric Phases

- Use TensorFlow/TFP, XLA JIT on, GPU execution, TF32 as declared, and trusted or
  escalated device access for production-target LEDH runs.
- Record device placement, TensorFlow/TFP/CUDA environment, XLA, TF32, dtype,
  command, seeds, wall time, source/config hashes, target signature, and output
  paths in each serious run manifest.
- Preserve all five seed artifacts and all individual FD endpoint records.
- Run the dependency/completeness gate after the final computation-relevant
  change; rerun every transitively affected cell.
- Never infer ranking from descriptive values or hard-screen passes.

## Local Post-Run Integrity Audit

The rejected run's credential/Claude/sealed-handoff auditor is not authority for
this lane. After Phase 9, create and execute a local-only audit that verifies:

- exact Phase 8/9 validator commands exited zero and every required check passed;
- exactly 24 unique admitted main cells and no sidecar contamination;
- six LEDH aggregates each bind exactly seeds `81120..81124`, paired value/score
  records, and reconstructed passing FD evidence for every direction;
- every cell matches the final dependency manifest and canonical target;
- all artifacts are finite, parseable, hash-bound, and no required output is
  missing or duplicated;
- the final semantic result contains the required nonclaims and does not promote
  descriptive evidence.

No completion or release claim is permitted until that separate audit records
`PASS_LOCAL_POST_RUN_INTEGRITY_AUDIT`. That pass cannot override a scientific or
source-faithfulness veto.

## Stop And Handoff Conditions

Stop and write a direct blocker/handoff if any continuation veto fires, the
deadline expires, an overlapping dirty edit cannot be isolated, or continuation
needs a target/default/API/package/network/credential/funding/scientific-claim
decision not already authorized. Do not retry or circumvent the rejected
launcher.

Allowed terminal classifications are:

- `NUMERICALLY_COMPLETE`: all 24 cells and every release plus local post-run
  integrity gate passed;
- `STATUS_COMPLETE_WITH_BLOCKERS`: every cell has an executed or direct blocked
  status but at least one numeric cell is absent;
- `BLOCKED_INVALID_EVIDENCE`: target, source, harness, data, math, or artifact
  evidence is invalid.

Only `NUMERICALLY_COMPLETE` permits the phrase `complete numeric leaderboard`.
