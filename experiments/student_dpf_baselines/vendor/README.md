# Vendored Student Code

Vendored code may be placed here only after provenance and permission details
are recorded in `PERMISSIONS.md` and `sources.yml`.

This directory is comparison-only.  Production BayesFilter code must not import
from it.
