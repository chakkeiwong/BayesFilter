# Merged endpoint execution-policy audit

This unit extends terminal F01--F20 review to runtime modules introduced by
remote parent `5e16df06f` into repair merge `9d8202b77`. The question is whether
every active filtering/score endpoint reaches a TensorFlow/TFP numerical program
with a stable default-XLA boundary and native tensor recurrences. The syntax
inventory alone cannot answer it. Earlier repair qualifications retain their
source scope; new modules do not inherit those results.

Bound this first audit/localization unit to standard-library source analysis and
at most eight focused numerical workers / 2,400 charged seconds, inside the
unchanged 32 CPU / 52 GPU-hour campaign limits. Run after the frozen DZ5 target
matrix. Reuse the campaign runner, default GPU with verified growth, and explicit
CPU graph/reference exceptions. Preserve every failed attempt in new numbered
directories. Stop an arm at its first unexplained numerical/source failure;
allow at most three localized harness retries under an unchanged fixture.

Initial inspected findings:

| Endpoint or source | Checked fact | Disposition and next evidence |
|---|---|---|
| `ledh_alg1_contract.py:165` → `ledh_canonical_filter_tf.py:85` | The registered canonical single-cloud value entry resolves to a host time loop at line 167; finite-state decisions materialize tensors at lines 174--180; resampling uses NumPy at lines 301--306. `_replication_generator` also uses NumPy SeedSequence. | Confirmed execution-policy violation. Trace each actual consumer and compare the existing analytical/value recurrences before choosing a shared native implementation. Block execution-readiness claims for this path; no new canonical-algorithm rebuild is authorized. |
| `ledh_canonical_models_tf.py` | Four numerical RK4 loops at lines 120/138/421/437 and NumPy constants at lines 198/318/501/630/650/664 remain. | Inspect callback call chains and compiled graph growth. Convert numerical recurrences to native TensorFlow while preserving steps, tangent equations, dtypes and constants; do not silently permit numerical unrolling. |
| `ledh_canonical_neutra_targets_tf.py:521` at merged baseline | The KSC fused-model builder imported NumPy to calculate two normalizing constants. | Repaired and callback-qualified in 03809/03810 with frozen-source and independent tangent checks. Complete LEDH target integration remains open. |
| `pruned_srukf_tf.py:206` | An inner non-JIT value call is enclosed by the public default-XLA analytical/autodiff factory at lines 190--199. | Literal `jit_compile=False` is not by itself an enclosing-XLA violation. Verify actual public HLO, derivatives, trace stability and numeric-only return values. Classify its score as autodiff, not an analytical recursion. |
| `ledh_younis_kdm_resampling_tf.py:593/602` | Two component factories request graph functions. | Trace their enclosing public factories and actual consumers before deciding whether XLA is bypassed. No compliance conclusion yet. |
| `ledh_canonical_neutra_targets_tf.py:70` → `ledh_canonical_batch_fused_tf.py:225--276` at merged baseline | The target calls the so-called fused routine, which maps a single-cloud evaluator over each parameter row and direction. The default is sequential `tf.map_fn`; the alternative pfor only changes the direction map. The registry incorrectly labeled the route NeuTra-eligible. | Eligibility labels are corrected; 03809/03810 prove the public training binder rejects the actual factory object before evaluation. The alternate while-loop wrapper is also a row mapping. Implementing a new canonical batch algorithm is outside this campaign's agreed scope. |
| `ledh_canonical_score_tf.py:825` | The public analytical wrapper calls the native executor directly without a default-XLA boundary. Its optional traced-step helper at line 794 also omits JIT. | Inventory public and enclosing callers; distinguish valid enclosing XLA from eager public execution. Add a stable shared wrapper or explicitly classify diagnostic-only entry where appropriate, preserving recursive derivative composition. |

Other touched filtering modules remain unclassified. The audit must discover
public exports, registry/factory dispatch and callables reached indirectly, then
record every endpoint's tested implementation, default and diagnostic exceptions.
Do not classify harmless static schema/report construction as a numerical loop;
do not excuse a numerical Python loop merely because tracing unrolls it.

Execution repair must preserve the numerical method and analytical derivative
composition. Compare fixed supplied clouds/noise, complete values, scores,
validity/rank/support decisions, changed operands and exact replay at existing
tolerances; require one declared trace and enclosing HLO without host callbacks.
Independent small Kalman/closed-form checks are numerical vetoes where applicable.
The exact original merged source is the execution comparator; pre-August-21
LEDH artifacts are prohibited baselines. Keep unresolved or unsupported canonical
claims blocked. No tolerance relaxation, new reset semantics, score substitution,
retuning, learned transport, or main promotion follows from this audit.

SeedSequence replacement is a separate compatibility question: the user-approved
versioned TensorFlow change for two geometry initializers does not automatically
authorize changing this lane's seeded realizations. First inspect whether an
existing shared TensorFlow stream can preserve the relevant input contract. If
not, define and evaluate a versioned candidate stream with frozen-cloud parity
and explicit seeded-data/tuning consequences before any runtime promotion.

Review: reporting syntax counts as unresolved compliance would overstate debt;
accepting every enclosing graph without actual consumer checks would understate
it. The inspected registry-to-value path establishes a concrete violation. The
pruned SRUKF example demonstrates why each negative-JIT literal needs a call-chain
check. New LEDH code must not be demoted or promoted scientifically from these
execution findings alone. This is a primary-agent review; no independent reviewer
has been used. Produce an endpoint ledger and exact focused tests before closing
any F01--F20 disposition. Broader repair costs require a subsequent matched
before/after unit once numerical qualification passes.

First bounded repair after DZ5 checkpoint `801354b77`: correct the unsupported
NeuTra eligibility claims on the row-mapped factory, fused wrapper and registry.
Expose explicit non-training batching metadata and exercise the actual public
training binder, which must reject the factory object before any optimizer or
target evaluation. Preserve signatures and all numeric call paths. The existing
missing training method is the fail-closed mechanism; a newly invented capability
flag is not an admission authority. Also replace the KSC builder's two NumPy
normalizing constants with standard-library math, preserving the equations.
This is execution/backend repair, with no source-faithfulness or scientific claim.
Compare every KSC callback against exact merged source `9d8202b77`, and check the
analytical tangents independently with centered differences at two step sizes.
Use B=3 initial/changed/replay tensors, default-XLA with one stable signature/HLO,
no callbacks, and CPU/GPU checks. This is a callback-local qualification; larger
filter loops and native batch construction remain open. Run at most two 300-second
numerical workers plus the standard 300-second policy group from this unit's
eight-worker/2,400-second allocation. Stop for any unexpected output/derivative or
training-admission result. No model or trained-map changes are made.

First repair result through 03811: all three focused checks pass on CPU 03809
and GPU2 03810; all 129 policy checks pass 03811. The actual LGSSM factory object
is rejected by the public `require_batch_native_neutra_target` before any target
evaluation. Runtime batching metadata, module descriptions and registry notes now
state its ineligibility. The numeric score program and target signatures are
unchanged. This verifies the existing rejection boundary and corrects unsupported
claims; it does not implement a new training target.

The KSC factory module now has no NumPy import. All nine callback outputs match
the exact merged reference under 1e-12 absolute/relative tolerances on initial,
changed and replay inputs. Transition and observation-density analytical tangents
pass independent centered differences at 1e-4 and 5e-5 steps under 1e-7 tolerances;
the second-step scaled discrepancy decreases from about 0.0151 to 0.00374.
The transition tangent also matches its independent normal-CDF derivative.
Both devices preserve one trace, identical changed-input HLO and no host callbacks.
No caller, filter or scientific admission follows from these callback checks.

| Decision | Criterion | Veto status | Main uncertainty | Next action | Not concluded |
|---|---|---|---|---|---|
| Keep row-mapped factory diagnostic-only | Actual public training binder rejects before evaluation | No unsupported eligibility label remains in the touched registry/modules | Other external wrappers need endpoint audit | Trace remaining training/score consumers | Batch-native training capability |
| Accept KSC constant backend repair | Original callback and independent tangent checks pass on CPU/GPU | No numerical mismatch, NumPy import or host callback | Full canonical filter execution still has open gates | Continue whole-endpoint repair | Full LEDH qualification |
| Continue endpoint audit | Registered value host loops/NumPy and model recurrences remain | Main promotion blocked | Complete caller/default inventory | Native shared recurrence/default repair under the remaining unit budget | Repository-wide policy compliance |

This first repair uses three of eight workers and 21.240362 of 2,400 seconds.
Cumulative charges are 75,320.454192 CPU / 75,195.789026 GPU seconds, leaving
11.077652 CPU / 31.112281 GPU hours. No worker remains active. Evidence is
`artifacts/filter-gradient-repair-20260917/merged-ledh-boundary-verification-03811.json`
and its referenced archive. Focused test/runner Ruff checks pass; the three
legacy modules retain 14 pre-existing Ruff findings and introduce none. No
runtime-policy allowance changed. Working-tree whitespace checks pass.

Post-run review: the metadata flags themselves are not authority; the test uses
the actual public binding function and fails if evaluation occurs. KSC callback
parity plus independent tangents constrains this constant-only change, but cannot
certify a filter path whose remaining loops and compilation boundary have not
been qualified. Existing signatures intentionally remain stable because no
numerical definition or dataset changed; no historical admission is refreshed.
