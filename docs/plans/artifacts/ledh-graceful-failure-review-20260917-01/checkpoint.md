# LEDH graceful-failure review checkpoint

- Active question: Is the proposed comprehensive testing plan mathematically sound, correctly connected to the current implementation, and sufficient for its stated conclusions?
- Deliverable: `docs/plans/artifacts/ledh-graceful-failure-codex-review.md`, answering the review memo and classifying required revisions.
- Checkout: `/home/chakwong/BayesFilter`, branch `surrogate-hmc`, HEAD `8a5c23ab1172884ffca63cac390623bf7735afe7`. Existing unrelated edits are preserved.
- Stage: review complete. The request memo, comprehensive plan, HMC tuning interface, model equations, and relevant implementation call chain have been inspected. Executable checks confirm Case 3 has an eigenvalue of 1.2, an indefinite formal Lyapunov solution, and observability rank 3 rather than 4.
- Skeptical audit: the proposed research campaign cannot run as written. It confuses observability with parameter identifiability, leaves inferred parameters and HMC configuration unspecified, and has no exact likelihood/gradient comparator or bounded compute contract. This review will inspect and diagnose those problems, not execute that campaign.
- Review checks: focused existing tests and small deterministic probes only; maximum 15 minutes aggregate framework-test/probe runtime, with each invocation bounded. CPU checks explicitly hide GPUs; any GPU check requires escalated access and memory growth. No posterior campaign, training, tuning, model/default changes, or external review is authorized by this review task.
- Evidence contract: compare stated equations against explicit derivations, consumer endpoints against their actual call chains, and sentinel behavior against finite-input/no-fire and deterministic invalid-input fixtures. These checks can establish specific defects and limited mechanics only, not posterior validity or production readiness.
- Evidence directory: this directory. Preserve commands, environment, relevant source hashes, stdout/stderr, and probe results here.
- Checked findings: focused CPU pytest returned 27 passed, 2 failed, 1 skipped (10.3 seconds including process overhead). Both failures are shape errors in the additional unlisted LGSSM integration tests. The real batch wrapper asserts near-equality on values and then converts every nonfinite value/score to NaN. The dual target only masks negative infinity. Failed safe Cholesky factors feed unguarded triangular solves. Exact evidence is preserved in `focused-pytest.log` and its manifest.
- GPU provenance correction: the first GPU probe selected CUDA ordinal 0, which TensorFlow identified as RTX 4080 SUPER despite the nvidia-smi ordinal ordering. Its verified memory-growth record is preserved. The final isolated probe selected the RTX 5080 by UUID and verified GPU placement and memory growth. Both runs were escalated; no performance comparison is made.
- Reproduced: real batch wrapper rejects an injected -inf sentinel in eager/ordinary graph mode and converts it to NaN under XLA; failed sigma-point factors yield NaN tangents; NaN-only factor validation accepts some infinite/nonfinite inputs and erases singleton batch axes. The fresh-process, unpatched DualParameterLEDHTarget returns NaN value and gradient for an invalid initial covariance under CPU XLA and RTX 5080 GPU XLA. Healthy XLA controls are finite and agree. The initial GPU ordinary-graph probe timed out after 240 seconds; CPU ordinary graph returned empty outputs, an unresolved environment-specific defect.
- Model review: the plan's predator-prey and SIR equations differ from local implementations; base SIR has no inferred parameters. Stationary-initialization derivatives and covariance-parameter tangents are not exposed by the reviewed fused consumer route. Production-readiness claims are unsupported and sentinel propagation is demonstrably broken.
- Framework budget: 326.38 seconds used out of 900 seconds (573.62 seconds unspent). All planned diagnostic checks are complete; no further framework run is needed. JSON outputs, invocation manifests, probe scripts, source hashes, and full logs are preserved here.
- Deliverable result: the Markdown review gives Option C (major revisions), answers all 31 memo questions, derives the mathematical corrections, classifies confirmed defects versus untested risks, and proposes the next bounded validation sequence. Source hashes match the copies used by the probes. No implementation or original plan was changed.
- Next action: return the completed review to the user. Any implementation repair or research campaign is a subsequent task; this review request is complete. Document/link validation is recorded in `document-validation.json`.
