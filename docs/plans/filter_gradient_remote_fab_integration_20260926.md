# Remote FAB integration into the execution repair branch

Remote main advanced from `5e16df06f` to `5eb6dcff5` during this continuation.
Its eleven commits add the optional canonical-IAF FAB trainer, diagnostics,
tests and documentation; existing filtering numerical authorities are unchanged.
Merge into the repair branch is authorized by the user's Git/integration
request. There were no conflicts. Main promotion of the repair remains blocked.

Run the new `test_neutra_fab.py` and `test_fab_campaign_diagnostics.py` through
registered `remote_integration_fab_cpu`, then the existing canonical transport
CPU integration and policy groups. These are explicitly CPU-hidden tiny
mechanics/reference tests (batch size 16); any optimizer steps are reference
checks, never NeuTra training-quality, convergence or posterior evidence.
Serious training remains GPU work and no FAB campaign is launched. Tests use
the TensorFlow port; no JAX package/reference environment is installed or run.

Reserve three CPU invocations, each at most 300 seconds, 900 seconds total
within the unchanged 56 CPU / 52 GPU hours. Failures trigger localized
integration diagnosis before retry; no numerical tolerance or algorithm change
is permitted to force success. Record the merged commit, new source closure,
commands, results and charges. Old full-source qualification records remain
historical; successful new tests do not make every previous group current.

Skeptical review: an optional trainer addition cannot by itself establish
filter/score compliance or invalidate prior numerical source comparisons.
The merge preserves the canonical IAF shared authority. The reference tests
exercise its derivative/checkpoint interaction; the existing transport tests
check that the architecture policy still holds. The new FAB source is outside
the current 270-file filter guard and is not silently declared policy-audited.

Integration result: runs 04165/04166/04167 pass; JUnit counts are [18, 6, 129]. The three runs charge 86.657036 seconds. No conflict or numerical repair was needed. The archive preserves every run file and the runner/plan source; Git commit `07996dd81` supplies the numerical/test dependencies. All original scope limitations remain.
