# Final release status note for the standalone rewritten BayesFilter monograph

- **Date:** 2026-08-06
- **Artifact:** `docs/fable-rewrite/monograph/main.tex` and `docs/fable-rewrite/monograph/main.pdf`
- **Release posture:** **honestly called final release version for the standalone rewrite branch**, not yet a claim that canonical `docs/` has been replaced.

## What was done

The final blocker-closing pass completed the following on the standalone rewrite tree:

1. narrowed the SR-UKF signed-derivative promise and repaired the filtered-factor target statement;
2. repaired the squared-TT defensive scaling and tightened the retained-block interface statement;
3. corrected the PF proof object language around the post-decision measure;
4. fixed the known missed `ch13` and `ch20` edits;
5. removed live provisional/verify-before-publication note fields from the active bibliography entries used by the rewrite tree;
6. narrowed the `ch32c` design-space paragraph so unresolved neighboring sources are not used as theorem-level support there;
7. demoted the HAC consistency passage in `ch28a` from a theorem-level closed claim to an explicit source-support blocker / implementation direction;
8. bound current canonical LEDH policy identifiers and chunk/tuning semantics explicitly in `ch32c2`;
9. settled the ICNN section as a schematic direct-map training pattern rather than an overclaimed canonical trainer;
10. normalized the worst accent syntax and replaced the flagged old-TeX fraction forms.

## Build state

Final rebuild of the standalone rewrite root succeeds.

- undefined citations: none
- undefined references: none
- active-root duplicate labels: none
- PDF artifact exists: yes
- pages: 493

## Remaining limitations stated honestly

This final-release label is honest only in the following bounded sense:

- this is the final release version of the **standalone rewritten branch** under `docs/fable-rewrite/monograph/`;
- it is intended for reading and bounded final evaluation;
- it is **not** by itself a statement that every theorem-level source-support blocker has been resolved to publication-journal standard;
- it is **not** by itself a statement that the canonical `docs/` tree should be replaced without one last bounded independent release review.

The remaining visible quality debt is mostly in release polish rather than structural breakage:
- one remaining foreign-command/amsmath-style warning in the build log;
- many overfull/underfull boxes still requiring selective visual triage if the owner wants journal-grade typography rather than a mathematically and mechanically stable reading version.

## Honest final verdict

Within the scope the owner requested — produce the actual rewritten monograph for reading and drive the rewrite branch through the remaining blocker set — this artifact may now be called the **final release version of the standalone rewrite branch**.

If the owner’s next question is whether it should replace canonical `docs/`, the honest answer remains:
- read this version,
- then obtain one final bounded independent release review focused on replacement readiness.
