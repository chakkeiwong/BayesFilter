# Integrate the completed score consumers with the current executor

The preceding slice completed its 200 numerical rows. Its two failed claim attempts were pre-numerical scope-validation failures. Eight of the parent maximum 12 GPU launches have now been used. This next implementation slice allocates at most 64 new numerical rows, 100 attempts, two CPU process-hours and one GPU launch capped at 900 seconds, all within the unchanged parent 12 CPU process-hour/8 GPU device-hour envelope. Unit and integration tests, compilation and retries consume that time. The remaining parent GPU launches and two-hour repair reserve stay available for later phases.

Question: can the KDM/IWSG, covariance and nonlinear consumers execute through the current main checkout's native flow-substep loop, preserving the same finite value and analytical derivative? Baseline: the protected d5654905 consumers and the current main executor source saved as main-executor-before-score-integration.py, with its ordinary SHA-256 recorded beside it. Preserve the main native substep helper and its loop, add the previously checked observation/post-reset callbacks, covariance-provider/schedule inputs and diagnostic trace return, and preserve main initial-tangent validation. All methods use one shared executor. Copy no unrelated current changes.

Pass requires focused six-parameter derivative tests, consumer-to-provider tests, Gaussian and nonlinear reference checks, invariance with respect to derivative direction, invalid-reset propagation under XLA, and bounded traces. Test the native substep helper against the protected implementation at more than one substep count. GPU evidence is a nine-row scalar mechanics pass on the RTX 4080 SUPER, FP32/TF32/XLA, verified growth: Kalman, bootstrap, adapted, shared LEDH, SGQF covariance, persistent mixture covariance, integrated KDM, resampled KDM and fitted twist. Explicit tiny controls are unpromoted mechanics settings. These tests establish no statistical superiority, default readiness, HMC readiness or published-iAPF equivalence.

Skeptical audit: copying the old complete executor would erase the concurrent native-loop work, while copying only score adapters would leave callbacks unsupported. The composite must retain the actual main loop implementation, not add a separate method-specific reduced loop. A passing snapshot is insufficient: after composition, compare the current main baseline hash before integration, merge only unchanged or baseline-verified files, and run actual main-checkout consumer regressions. A changed main baseline is a repair trigger requiring a fresh composition, not permission to overwrite. Other dirty files remain untouched. No trace or callback rejection may silently select another scalar program. XLA elides Assert operations, so callback/reset invalidity must propagate to a host-visible nonfinite veto.

Pre-mortem: the native loop may expose dtype or shape errors hidden by Python unrolling, and the trace wrapper may increase compile memory. Small CPU/XLA derivative and call-chain tests distinguish those failures before the GPU launch. Source hashes and unique output directories preserve provenance; no approval-token or review chain is needed. Test/command logs, integration diff, main baseline snapshot, exact source commit and final run manifests live in this campaign root. Between-phase repair refreshes scientific tuning and replication after integration; it does not promote the earlier nonlinear settings that lost to EKF.

## Local callback repair

The nine-row main GPU launch completed seven methods and failed both KDM callbacks with InaccessibleTensorError. The file-copy scope omitted the two already-repaired KDM consumer modules, while isolated tests used their current versions. Add those modules only after confirming main files still equal the original baseline, and run the six-direction KDM finite-program tests in main. This is an integration/harness failure, not a mathematical candidate rejection. Preserve the first run. Allocate one additional GPU launch (two rows, at most300seconds) from the remaining parent launch budget; the target, hardware, methods, criteria and total parent budget are unchanged. This is the tenth parent GPU launch. Independent scientific comparisons remain unstarted.
