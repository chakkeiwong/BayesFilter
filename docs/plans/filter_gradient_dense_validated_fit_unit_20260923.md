# Enclosing validated dense fitting for E5

CPU qualification through03416 passes all seven focused tests and121 existing
geometry checks;129 policy checks pass.03414 records exactly zero derivatives
through the diagnostic unfrozen outer loop and restores None derivatives at the
explicit completed outer boundary. All original records, rejection/error order,
changed-input HLO, one trace and graph/owner collection pass. The report extraction
AST is identical apart from receiving static audit_rows as an argument. Guard232
sources/1333 exact exceptions; four reporting exceptions moved unchanged, none
added. The GPU matrix is executing with runtime/scripts/tests frozen. No public
initializer or actual target/transition integration is claimed at this point.

03408--03412 pass complete D1/D3 healthy, audit-veto, incomplete-eligibility
and invalid-rank records, including four valid calls and three zero-fit invalid
calls per case. HLO, one trace and owner/graph collection pass.03413 passes error
precedence and all enclosing numerical records/HLO/collection but fails the
diagnostic wrapper's disconnected-gradient assertion: placing frozen inner
outputs in a further tf.while_loop reconnects an outer gradient edge. Preserve
this failure. The localized retry records its actual derivative magnitudes and
adds an explicit stop_gradient at the enclosing wrapper's completed boundary,
requiring the same original None-gradient criterion. No fitter arithmetic or
comparison changes. The full attempt controller must use that outer boundary.
This retry consumes the one remaining16-worker reservation slot; total planned
qualification including consumers/policy remains16 and at most3600 seconds.

Question: can the dense initializer call the existing fixed-center fitter from
one XLA program without bypassing finite/disjoint-partition checks or losing
original fit errors and reports? The next integration unit composes the qualified
partition checker with the existing complete fitter. It does not alter solvers,
tolerances, target callbacks, RNG, selection or scientific eligibility.

The authority is original3582b4ac fixed-center and factor fitting, with exact
source/excerpt hashes. Also compare the current public fitter. Use its existing
deterministic D1/D3 independent clouds and thresholds as regression fixtures,
not new defaults. Invalid center/score/partition and copied rows must execute
zero fits; a resource counter around the actual fitter proves no-use. Successful,
changed, invalid and return-to-original operands must reuse one trace and HLO.
Compare complete payloads including fits, stability, selected geometry, audit
vetoes and lineage at unchanged1e-10 tolerances. Preserve frozen-output gradient
semantics. Test actual program/graph collection and an enclosing invalid-to-valid
recurrence. Error order is validation, one-factor stability, family stability,
then selector error; error statuses must veto covariance handoff.

Move only completed public-report reconstruction into a shared host helper so
the enclosing initializer can emit the identical schema without rerunning a fit.
Migrate the existing four exact reporting-loop exceptions to that helper without
changing ASTs or adding permissions. Static shapes/configuration and original
caller-buffer alias checks remain preparation requirements. The native path is
for disjoint prepared tensors, not a replacement for raw caller-buffer checks.

At most16 workers/3600 charged seconds,120s ordinary and300s focused combined
workers, within unchanged32CPU/52GPU campaign caps. Register groups under the
same approved runner; CPU is explicit reference, GPU uses verified growth and
the existing selector. Freeze runtime/scripts/tests during workers. Stop promotion
on any record/error/order/HLO/collection mismatch and preserve failures for a
localized repair without weakening comparisons. Stop execution for invalid
evidence, changed scientific contract or budget exhaustion. Use fresh numbered
artifacts, manifests and a result appended here. No timing claim from these tests.

Skeptical review before implementation: an internal fitter alone bypasses public
validation, and a post-fit error flag would waste work or expose invalid geometry.
Use a real conditional around fitting and test nonexecution. TensorFlow can
specialize dynamic indexing even with a stable trace, so require changed-input
HLO equality. Report extraction must preserve every statement and error order;
syntax-guard migration and existing public consumers catch accidental changes.
Original error records take precedence over apparent native status success.
This unit cannot close the complete initializer, actual target, capacity, seeded
RNG or main-merge gates. Compose the attempt controller only after this dependency
passes, keeping the completed external CDF campaign untouched.
