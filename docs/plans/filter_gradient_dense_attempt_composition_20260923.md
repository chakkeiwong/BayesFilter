# Dense initializer attempt composition

This follows the validated-fitting unit; do not implement or execute its
numerical work while that unit's source-frozen workers are active. The engineering
question is whether the exact external2f386f75 `initialize_dense_local` attempt
recurrence can execute as one reusable XLA call with the qualified batched
locator, ordered clouds and validated fixed-center fitter. No completed CDF
campaign is restarted. The isolated external checkout remains the integration
target; the live dirty MacroFinance tree is protected.

The original function is preserved byte-for-byte in
`tests/fixtures/filter_repair_dense_initializer_reference_2f386f75.py`.
For an initial execution-only comparison, replace only its random-offset
preparation with explicit frozen cloud operands in a diagnostic AST adapter.
Keep the remaining numerical statements, order, fitter/error handling and
complete result/archives unchanged and save original/adapted source hashes.
Original BayesFilter locators/fitting retain their already pinned independent
authorities; the existing component checks do not replace the full-call check.
Record the deliberate default JIT change in provenance, rather than comparing
the old false flag as a numerical result. Seeded RNG qualification remains an
explicit subsequent step before public external integration.

One outer TensorFlow loop carries the current center, attempted count, exact
rows, status, and completed per-attempt locator/cloud/fitting records. Numerical
branches preserve locator rejection, scaled-score veto, first invalid cloud,
strict greater-than center promotion, attempt exhaustion, fit rejection/error,
and positive finite marginal-scale checks. Invalid clouds and moved centers
must execute no fit; fit errors must expose no usable scale. Call the batch
target once per original batch and preserve each batch extent. Reuse one locator
owner with resource reset; no zero-argument factories per attempt. Retain actual
partial cloud archives before first invalidity, with padding used only for
storage and excluded from reports and fitting.

Apply stop_gradient to the completed outer-loop result, matching the original
initializer's host-materialized geometry.03413 shows why inner stop_gradient
alone does not guarantee a disconnected gradient through an added outer loop;
retain the diagnostic and its explicit-boundary qualification from the preceding
unit. Do not expose optimizer/eigensolver pullbacks through this initializer.

Keep host configuration validation, process supervision, completed report
formatting and file I/O outside XLA. A host formatter may decode completed status
and history flags, but must not redo selection, norms, finite tests or decisions.
Use the newly shared fixed-center report formatter. Later external serialization
must preserve NPZ member names/shapes/dtypes using TensorFlow and the standard
library; compare its reader-visible payload against original NumPy reference
archives. Progress events can describe the whole compiled boundary; they cannot
serve as an in-graph deadline. Use the already tested independent parent deadline
and a bounded fresh-process lifetime for the actual consumer.

Reserve24 workers/7200 charged seconds after the preceding unit, within the
unchanged32CPU/52GPU caps. Ordinary focused workers120s, justified complete
workers300s; separate a costly fixture rather than silently extending its
deadline. CPU is an explicit reference and GPU is the default qualification
target with verified growth and the existing selector. Preserve manifests,
commands, source hashes, full records, target call/row sequence, graph/HLO,
ownership, and memory observations in new numbered campaign artifacts. Require
changed operands and return-to-original without retracing or HLO specialization.
Cover healthy D1/D3, invalid locator/cloud, score veto, moved-center retries,
attempt exhaustion, rejected/audit-vetoed fits, error precedence and completed
archives. Renew existing dense consumer assertions after composition qualifies.

Stop promotion on any changed original record, predicate, callback order/count,
shape, error, HLO or lifetime mismatch. Preserve and localize within this unit
without weaker comparisons; stop execution on invalid evidence, changed target
or exhausted budget. Do not install an eager fallback. Full seeded actual-DZ5
target/transition qualification, public costs, terminal endpoint/F01--F20 audit
and remote integration/retests remain required for main merge.

Skeptical review: frozen clouds isolate execution but cannot qualify the actual
RNG stream. Carrying only the final fit loses attempt and partial-cloud evidence;
retain complete histories. Recording a fit error after using its covariance is
wrong; the error code must gate the scale branch. Capturing initial center/clouds
as constants defeats reusable execution; enforce tensor operands and compare HLO.
An accepted local scale remains a guide initializer, not posterior or HMC
evidence. Configuration values are inherited regression fixtures, not tuned
scientific defaults. No source or criterion change is justified merely because
the original makes a strict near-tie decision.
