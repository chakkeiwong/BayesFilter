# Complete paired quadratic refinement controller

Question: can the paired-local initializer's initial evaluation, replay,
ordered probes, model/factor decisions, trust proposals and bounded fit-round
recurrence execute in one stable TensorFlow/XLA program? This continues the
authorized execution repair; uniform-cloud dense fitting and its singular
condition-report mismatch are separate open work.

The numerical authority is the complete dependency closure at `3582b4ac`,
with its trust solve explicitly in ordinary graph mode because the original
XLA eigensolver has demonstrated errors. The current qualified paired probe,
batch evaluation, fitted precision and refined trust programs are building
blocks, not substitutes for the complete original public record. Preserve
the exact analytical target callback, seeds, probe stream, widths, all
thresholds, trust update, replay tolerance, callback order/counts, padding,
strict selection, failed-proposal retention, anchor freshness, optional
initial evidence and result/rejection precedence. No optimizer or method
change, ridge, score substitution or tolerance change is included.

Use fixed-capacity tensor histories sized from the existing planned-row and
round limits, with explicit active counts and field-presence masks. Only
completed records may be materialized on the host. The native while loop
must stop at the same round and callback as the original. A plain user target
may be called while tracing but no PyFunc, pfor or Python numerical recurrence
may run inside the compiled program. Static configuration and output-schema
construction are allowed; numerical decisions are tensors. This first stage
qualifies an isolated complete controller before public replacement.

The primary gate is every original value, score, selected index, physical/
padded/invalid row count, status, proposal, round model, factor and history
field at atol=rtol=1e-10; discrete decisions agree exactly. Assert and compare
execution metadata separately where a single compiled boundary necessarily
changes trace reporting. No numerical field can be dropped. Cover D1/3/5,
one/four rounds, centered/moving/nonquadratic targets, initial and per-design
invalidity, replay mismatches, proposal failure, model rejection and optional
initial evidence. Compare original callback rows and invocation order too.
Changed center, scale, seed and evidence must preserve one trace and all
runtime HLO inputs without data specialization. An enclosing-XLA call must
return the same complete numerical state. Unused history slots cannot affect
results or create target calls.

After correctness, compare complete original/controller graph/XLA costs in
fresh processes at D3/5, identical input/output lifetimes, twenty warm calls.
Include build/trace/cold/warm/materialization times, graph/HLO sizes, sampled
RSS/VmHWM and GPU allocator current/peak. Investigate >20% warm regression,
>256 MiB host increase, >2x device peak, materially higher cold cost or
continuing allocation growth. Cost diagnostics are descriptive, not a
statistical ranking or full composed locator/initializer speed claim.

Use only the existing absolute campaign driver with registered groups,
sequential CPU reference/GPU2 workers, GPU idle preflight and verified memory
growth. Bound each correctness group to 300 seconds and each cost process
to 120 seconds initially; a ceiling failure prompts splitting/inspection,
not altered data or iteration limits. Each launch has a fresh numbered
directory under the shared filter-gradient-repair-20260917 artifact root.
All attempts count against the unchanged 32 CPU / 52 GPU process-hour caps.
Do not edit source/tests/driver while a worker is active. Stop for broken
source isolation, missing required observations, uncontrolled growth or
budget exhaustion. Numerical or cost failures reject the candidate and
trigger localized repair; they do not authorize merge or scientific claims.

Skeptical review: tracing all conditional branches can change trace metadata
without changing executed calls, so callback counters and full original
histories are mandatory. Graph assertions can disappear under XLA; the
wrapper keeps static/initial-input validation and the controller carries
explicit tensor status for dynamic failures. Buffer capacity is derived from
the existing declared physical bound, not a convenient smaller fixture.
Recomputing reports or calling the target after the numerical return would
invalidate the claimed boundary and comparison. A passing paired controller
cannot close the uniform dense fitter, original D5 sequential mismatch,
external watchdog/DZ5 evidence, or terminal F01--F20 gates. Primary-agent
review accepts this bounded continuation; independent terminal review remains
pending. No external source/pin or environment changes are included.

01988 fails before candidate execution: the test initial-evidence formula
uses TensorFlow matrix multiplication on a rank-one vector. Repair that
fixture with tf.linalg.matvec, leaving the intended scalar and all tolerances
unchanged. Retry the single moving D3 case under the same 300-second ceiling.

01989 passes the complete moving D3 original/graph/XLA record; 01990 passes
changed-input parity, all runtime operands, one trace, unchanged HLO and an
enclosing XLA call. Expand to the declared D1/3/5 cases and assert expected
status for rejection fixtures so a test cannot pass without exercising its
failure mode. The changed-input test now also changes the optional evidence
flag/value/score, covering every explicit runtime argument. Execute batch
quadratic_rounds with the unchanged 300-second per-worker ceiling and GPU3
idle preflight; GPU3 is the same hardware class used for latest probe costs.

01991 D1 and 01992 D3 pass all 17 complete CPU cases each, including explicit
failure-status assertions, exact target callback counts and full histories.
The failed-proposal fixture intentionally continues and can later succeed,
matching the original incumbent-retention semantics. Larger dimension and
GPU qualification remain pending. Public wiring and cost claims remain gated.

01991--01997 finish all 51 CPU and 51 GPU3 complete cases; 01994 passes the
expanded all-argument/runtime-HLO/enclosing-XLA case, and 01998 passes all 68
policy/controller checks. No failed or skipped JUnit cases remain in this
matrix. Focused Ruff and whitespace pass. The candidate remains unwired.

Cost-harness review: compare the complete original public refinement against
the complete prepared controller plus complete report materialization, with
shared original dependency imports before measurements. Report factory/trace
costs separately; the original endpoint rebuilds internal functions per call,
whereas the prepared candidate reuses its compiled program. That distinction
is explicit and does not establish the eventual public API reuse cost.
Measure all live outputs through full serialization, then run the exact
original reference after measurements and require every initial/changed field
to match. Preserve reports before parity assertions so a failed candidate is
diagnosable. Graph mode must contain no nested XLA; original trust uses the
graph precision authority. Execute quadratic_round_memory as twelve sequential
120-second processes, CPU/GPU3 at D3/5. Runtime source stays frozen. No speed
or memory conclusion is admissible without the complete-field parity gate.

CPU cost arms 01999--02004 pass complete initial and changed-input parity.
XLA observed RSS rises 533/539 MiB over original and cold calls rise to
3.18/3.33 seconds, plus 1.39/1.43 seconds tracing and about .24 seconds factory
construction. This fires the >256 MiB host investigation; public integration
remains gated. The XLA graph has 3734 nodes at D3/5 (graph mode 3429), with
approximately 3.78 MB HLO. Growth is concentrated after tracing at the first
compile; the recorded twenty calls cannot establish allocation stability.
Warm cost is dominated by diagnostic report slicing, another repair trigger.
Complete the already-running matched GPU arms before changing any source.

Next isolate reporting without changing the numerical program: bulk-copy each
completed history tensor once and assemble the same report in Python lists.
Compare every payload field to the prior diagnostic materializer on all
original-record cases before new costs. Numerical tensor endpoints retain
their shapes/dtypes and public APIs are still unwired. A reporting repair is
not a solution to compile RSS; measure factory/trace/first-call RSS and
smaps_rollup, graph/HLO size for round limits 1/4/8, then sparse alternating
input calls with no injected cleanup. Same node count across limits would
rule out round unrolling, not excessive native compiler caching. Preserve the
observed overhead even if warm allocations plateau. All diagnostics retain
120-second per-process bounds, fixed target/seed/settings except the declared
round-capacity comparison, and existing compute/stop rules.

The first full cost matrix 01999--02010 passes all original initial/changed
records. Analysis is quadratic-round-cost-comparison-02010.json, generated by
`/tmp/analyze_quadratic_round_costs_20260921.py`. Observed RSS-peak deltas are
519/524 MiB on CPU and 275/278 MiB GPU3; earlier 533/539 figures compare the
end-of-measurement stage rather than the maximum observed baseline. Both
definitions exceed the declared host threshold. GPU live allocations are
constant over twenty warm calls; CPU XLA rises about 112--120 KiB. Warm report
slicing dominates candidate time (59/85 ms CPU and 96/132 ms GPU3 overall).
Retain this original candidate evidence before optimizing reporting.

The diagnostic materializer now copies whole history arrays to Python lists
before slicing. Compare it to the prior sliced materializer on each original
case; first run the 17 D1 CPU cases, then renew the complete D3/5 cost matrix,
which independently asserts original full-field parity in every process.
No numerical source changes. Capacity/growth diagnostics compare limits 1/4/8
at D5 and alternate initial/changed inputs for 3000 calls, retaining only
1000-call observations. They record /proc smaps_rollup alongside status RSS.
Group quadratic_round_growth uses 120 seconds per process on CPU/GPU3.

02011 passes all 17 bulk-versus-sliced/original CPU record cases. Renewed
costs 02012--02018 pass through GPU3's first original arm, then the idle
preflight vetoes an unrelated 417 MiB allocation. Keep the six CPU and one
GPU3 observations; do not mix the remaining cost arms with another GPU.
While GPU3 is occupied, execute the independent capacity/growth CPU diagnostics
sequentially with the same frozen source. Resume only missing GPU3 cost arms
under the unchanged idle gate. No worker ran in the rejected preflight.

Recovered outcome through 02028: all renewed cost arms finish (02012--02018,
02022--02026; 02019--02021 are CPU growth diagnostics). Analysis is
quadratic-round-cost-comparison-02026.json. The host trigger persists at
approximately 514/522 MiB CPU and 268/271 MiB GPU3, despite faster bulk report
materialization. CPU capacities 1/4/8 pass with graph nodes 3734/3734/3738,
first-call RSS about 1.203 GB and final 1000-call growth 4/8/12 KiB. GPU3
capacity 1 passes 02027. Capacity 4 times out at 120.217 seconds in 02028;
no complete observation artifact exists, so it proves no allocation outcome.
Capacity 8 never launches.

Retry review: D5 GPU warm cost in the completed matrix is 43.672 ms including
reporting, predicting 131 seconds for 3000 calls before build, trace, compile
and original comparisons. The initial 120-second ceiling is inadequate for
this diagnostic. Preserve the timeout and keep all 3000 calls and inputs.
Write append-only progress records only after actual build, trace, cold and
1000-call observations, then run missing groups once at 300 seconds each:
`matrix --stage tests --test-batch quadratic_round_growth --test-gpu-index 3
--test-timeout-seconds 300`. This is a bounded harness repair under the same
campaign; no budget cap, numerical source, method or tolerance changes.
The driver skips passing CPU and GPU1-capacity groups. Primary-agent review
accepts the retry; a second timeout requires inspection of real stage records,
not another blind ceiling increase. Public integration remains gated.

The measurement-test hash changed when progress preservation was added, so
the driver correctly invalidates prior current-source skips and repeats all
six capacities (02029 onward), rather than only missing GPU cases. This is
necessary harness qualification; the earlier complete results stay preserved.
Active driver session is 97587.

Next lifetime contract (execute only after current workers end): construct eight
fresh D5 nonlinear controllers in each of one CPU reference and one GPU3
process, evaluate both unchanged and changed inputs per controller, preserve
every numerical record against original 3582b4ac, and record elapsed time,
RSS/smaps and GPU allocator statistics after every construction. Each callback
captures a real counter variable whose executed count must equal the returned
callback histories. Keep weak references to each enclosing graph, function and
variable; release ordinary references after each iteration, without cache
clearing or manual collection during measurements. A single final diagnostic
Python collection tests whether these Python graphs/resources are collectible.
It is not runtime behavior or proof of native executable eviction. Eight
iterations are a bounded resource-owner probe, not a lifetime leak theorem.

Promotion vetoes: any full-record/count mismatch or retained Python graph/target
variable after explicit collection. A >256 MiB rise across fresh constructions
or nonplateauing live device memory triggers investigation before public
integration. Same 300-second process ceiling, no more than one CPU and one GPU
attempt before inspection, no source/environment/pin or numerical change. Emit
progress only after actual completed constructions and a complete final report
in the ordinary fresh campaign run directory. A failure rejects this candidate,
not the algorithm or broader campaign. Register quadratic_round_lifetime groups
in the existing driver; execute `matrix --stage tests --test-batch
quadratic_round_lifetime --test-gpu-index 3 --test-timeout-seconds 300`.

Skeptical review: stable warm calls can hide repeated function-construction
costs, and retained reporting tensors can masquerade as graph leaks. This
experiment keeps only Python payloads and weak ownership observations after
each call, and imports the same frozen baseline before measurements. Public
reuse remains unproved: do not add an unbounded callback cache or infer an
acceptable cache size from these eight cases. Public configuration must preserve
list-valued paired_steps and explicit jit_compile_trust=False reference behavior;
trace counts must report traced branches separately from executed calls.
Primary-agent review accepts this bounded diagnostic before public wiring.

02029--02034 pass all refreshed CPU/GPU3 capacities, each with 3000 alternating
calls, one trace, unchanged runtime HLO, and complete original-field parity.
The GPU4/8-capacity processes finish in 151.804/156.939 seconds. Analysis:
quadratic-round-growth-comparison-02034.json. Graph nodes remain
3734/3734/3738. Final 1000-call host changes at capacities1/4/8 are 8/16/0 KiB
CPU and 4/0/0 KiB GPU3. GPU current is constant at 7168 bytes; capacity-dependent
peaks are 99840/139520/197888 bytes. Compilation still adds about 496 MiB RSS
CPU and 349--352 MiB GPU from the traced stage; anonymous-memory growth confirms
real host allocation. This answers the bounded warm-growth and round-unrolling
questions, not controller construction/lifetime or native-cache eviction.
The numerical source is unchanged; proceed to the reviewed lifetime diagnostic.

02035 CPU / 02036 GPU3 pass every lifetime numerical/count/ownership assertion.
All eight original/changed records agree, target variables are released during
ordinary turnover, and all enclosing Python graphs/functions are collectible.
However, RSS rises to 2.566 GB CPU and 2.727 GB GPU3 after eight fresh builds,
well beyond the >256 MiB trigger. Graph release does not prove native executable
release, and this growth vetoes naive per-invocation controller construction.

Repair contract: public repeated calls should reuse one most-recent compiled
controller for the identical callback object, dimension, static configuration
and mode. Keep the existing uncached factory for explicitly prepared callers.
A single-entry cache is a deliberate conservative memory bound, sufficient for
the serial repeated-target use case; multiple targets may recompile on turnover,
and no multi-target memory bound or native eviction claim follows. Exclude seed
from static configuration because it is already a qualified runtime operand.
Normalize paired_steps to a tuple only in the key, preserving public list schema.
Use callback identity, so unhashable callable objects remain supported and equal
but distinct targets cannot accidentally share graphs. Serialize construction
with an ordinary lock; do not add a runtime garbage-collection workaround.

Before public replacement, qualify same-target changing-input reuse across
seed/list configuration variants, distinct callback/config/mode separation,
execution through held concrete functions after cache replacement, and explicit
Python cache release without retaining old target variables. Inspect complete
HLO/runtime operands and original payloads. Repeated 20-call cached construction
must use one program/trace and record memory; compare its first/cold and warm
costs separately. CPU/GPU3, 300-second bounds, unchanged numerical source.
A cache failure is a repair trigger; no original-field waiver or API promotion.

Public reporting contract for the subsequent integration: copy completed history
arrays once to ordinary lists; preserve the result tensors, payload schema and
all numerical/status/count fields. Diagnostics dict is documented Any and may
hold standard host values, but audit every repository consumer for assumptions
about its previous internal tensor values. New execution metadata must explicitly
report enclosing refinement XLA, actual fit/trust executions, and traced branches.
Keep full_initializer_xla=False while composition with multistart localization
is unqualified. For paired mode, jit_compile_trust=False selects the explicit
whole-controller graph reference; record every affected JIT field truthfully.
It must never silently choose graph after an XLA failure. Uniform mode remains
unchanged migration debt under its existing separate dense-condition blocker.
Primary-agent review accepts bounded reuse work; public wiring remains conditional
on reuse qualification and complete public/original/consumer and cost checks.

02037 CPU / 02038 GPU3 pass both cache checks: same program across changing
seed/center/scale and list-valued widths, one trace/all HLO operands, distinct
equal-but-unhashable callbacks, configuration/mode separation, graph reference
without nested XLA, execution through a held concrete function after replacement,
and collectible old Python callback/graph/target variable. Twenty warm calls
add about 156 KiB CPU / 24 KiB GPU host RSS, versus >1 GiB across fresh builds.
This qualifies single-target reuse, not arbitrary target turnover. Cold costs
remain about 5.34 seconds CPU / 9.37 seconds GPU, with warm median 28.2/44.4 ms.

Consumer inspection before wiring found that repository paired consumers use
scalar conversion, boolean conversion, model-key membership or payload(); no
paired consumer requires a diagnostic ndarray/tensor .numpy() method. The
uniform-only tensor-report consumer remains unchanged. Use ordinary lists for
completed paired diagnostics, preserving center/score/factor/precision tensors.
Wire only paired_local after existing center/scale/static row-budget validation.
Remove its obsolete unreachable Python branches; retain the separate uniform
migration debt. Complete raw-controller/public/original cases must compare every
numerical field. Additional JIT/call/trace metadata must be explicitly asserted
against the actual compiled boundary and original executed traces, not silently
omitted. Run the 17 D1 CPU cases first, then D1/3/5 CPU/GPU and existing public
paired/quadratic consumers. Cost qualification must include the actual wrapper,
cache lookup and final report, plus cold and changed-input behavior. No merge.

The paired public wrapper now calls the single-entry controller after validation.
Original Python paired round/partition branches are removed; uniform mode retains
its explicit migration debt. A dedicated host reporter copies complete arrays,
formats active histories, preserves result tensors, and adds truthful enclosing
JIT, traced-branch and executed-fit/trust counters. Public reference mode disables
JIT for the complete controller. Eight reporting-only AST exceptions replace the
removed paired-partition reporting exception; numerical loops receive none.
The complete-record test now checks both raw and actual public graph/XLA results
against every original field and explicitly verifies metadata differences. The
first 17 D1 CPU cases run in session 51153 (300-second ceiling). No source edits
or parallel workers until that completes.

02039 passes all 17 D1 original/raw/public graph/XLA cases, including rejection
precedence, target calls, full histories and explicitly asserted JIT/trace fields.
It costs 228.7 seconds because each mode constructs identical raw and public
programs. For the remaining matrix, test the public result first and then call
that same cached program to verify bulk/sliced reporting; this removes redundant
compilation while retaining every callback, status and original-field assertion.
Run quadratic_rounds at 300 seconds, then quadratic_round_consumers. A new cost
harness covers actual public validation/cache/controller/report/payload from
cold start and twenty reused calls, with identical lifetimes across original,
complete graph and XLA arms. It retains exact payloads plus every original
numerical field, and changed inputs/HLO/one trace; factory and compilation costs
are included in the public cold call. Run quadratic_public_memory CPU/GPU3 D3/5
under 120 seconds per process after correctness. This qualifies refinement only;
multistart composition/DZ5 and repeated terminal comparisons remain separate.

Interim wiring review: the factory receives a seed-normalized immutable config,
but the public call passes the caller's seed as int32 and reports that original
seed. The single cache key binds the callback by identity, dimensions, all static
controls and JIT mode; it owns at most one Python target. Original input/budget
validation still precedes any callback. The reporter performs no target call or
numerical recurrence. Every explicit graph-reference program is inspected for
nested XLA. Source guard now passes 204 files / 1289 exact exceptions (partial
coverage), with eight new host-report nodes and the obsolete partition-report
exception removed. No exception was added for a numerical loop. Remote fetch
shows no repair-branch divergence and remote main is an ancestor; main unmerged.

The separate singular dense-condition blocker now has a concrete diagnostic-only
proposal in filter_gradient_dense_condition_decision_20260921.md. Existing
consumer inspection confirms rank and condition both veto rank-deficient fits.
An asynchronous user decision is pending because defining condition under the
existing numerical-rank policy changes a reported field relative to the frozen
raw SVD ratio. Do not change dense runtime/tests or relax that gate without the
answer. Paired-controller qualification proceeds independently.

02040--02042 pass all 51 complete public/raw/original CPU cases at D1/3/5;
02043 passes changed inputs, all HLO operands, one trace and enclosing XLA.
GPU counterparts continue under the same frozen source. These public payloads
include every numerical history/count/status field; trace and execution-mode
changes are asserted independently. No tolerance, algorithm or threshold changed.

Cache assumption review: callback identity assumes TensorFlow tracing semantics.
Python closure/attribute changes on the same callable are not runtime operands;
callers must use tf.Variable for mutable target values or supply a new callback
(or explicitly clear the single-entry cache) when redefining the target. This
is a material limit of public reuse, even though repository consumers already
use compiled/static callbacks. Do not claim arbitrary Python target mutation is
tracked. Add a focused captured floating-variable value/score test and assert
that the public call already traced the retrieved cached controller before the
raw/report comparison. This checks the actual consumer wiring and numeric
variable behavior; it does not make Python closure mutation dynamic.

02040--02047 finish 51 CPU / 51 GPU3 public/raw/original cases plus all-input
HLO/enclosure and 68 policy/controller checks, with no failure or skip. GPU
D1/3/5 take 165.9/237.6/250.7 seconds within the 300-second bounds. The next
focused cache check adds floating tf.Variable target mutation to the already
qualified cache cases; it retrieves the public program and asserts one trace
before any direct call, making the wrapper-to-controller wiring executable.
Runtime source stays unchanged. Run quadratic_round_cache at 300 seconds, then
quadratic_round_consumers and quadratic_public_memory under the existing plan.

02048 CPU / 02049 GPU3 pass all three expanded cache checks. Updating a
captured floating tf.Variable changes the public value/score/factor results to
match the original source without retracing. The actual public controller is
already traced before its retrieval by the test, closing the wiring check.
Distinct target identity, graph mode and old-resource release still pass.
Continue the existing public consumer matrix under frozen numerical source.

For final public refinement cost evidence, use the declared three fresh-process
repeats per arm/device/dimension. The first quadratic_public_memory matrix
provides repeat 0; explicit registered `test --group quadratic_public_memory_...
--device CPU|GPU --test-gpu-index 3 --test-timeout-seconds 120 --repeat 1|2`
commands provide the other repetitions (matrix skipping does not distinguish
repeat numbers). Keep the same source, inputs and numerical gates across all
36 processes. Report per-process medians and ranges; three repeats support the
predeclared engineering regression screen but no statistical superiority claim.
No extra budget cap, target or data change is included. If a process fails,
inspect that result before retrying or proceeding with its comparison.

Harness review found a concrete resume defect: run_matrix's tests stage checks
only group/arm/source/device, ignoring the requested repeat. Thus a passed repeat
0 would incorrectly satisfy --repeat 1 or 2. Repair that skip check to include
the existing repeat key and add focused resume tests before cost execution.
No job format, numerical source, timeout or budget changes. After qualification,
use the same quadratic_public_memory matrix with --repeat 0, 1 and 2 instead of
manual per-group launches. This reduces orchestration mistakes and guarantees
that each requested repetition is actually executed. Preserve failed evidence;
never count a skipped first-repeat result as a new measurement.

02050--02058 pass all public consumer groups on CPU/GPU3: probes, quadratic
center, paired pilot, batch-history comparisons and policy/controller checks.
The repeat-resume defect is now repaired in the driver without changing any
runtime source, and four focused cases cover repeating/skipping repeats 0/1/2.
Execute the policy group before the 36-process public cost series.

02059 passes all 72 policy/controller tests including independent-repeat resume.
The public cost series begins at 02060 (repeat0 session39310); repeat1/2 follow
under the same registered batch and unchanged source. Focused new numerical and
reporter files pass Ruff; the campaign test module retains two pre-existing
C408 style warnings at unchanged lines584/605. Whitespace checks pass. No
repository-wide lint claim or cleanup of unrelated style debt is included.

02060--02071 public cost repeat0 passes every original initial/changed field,
input/source identity, one-trace/runtime-HLO and graph non-XLA check. Analysis:
quadratic-public-cost-comparison-02071.json. GPU public XLA warm times are
25.929/42.872 ms versus original 497.894/576.300 ms at D3/5; cold XLA is
8.639/9.366 seconds versus original 2.781/2.813 seconds. CPU XLA warm is
15.882/28.836 ms versus original379.576/428.839 ms; cold4.871/5.872 seconds.
Host deltas remain 521/528 MiB CPU and283/286 MiB GPU3, consistent with the
prepared-controller compile investigation. These are single-repeat observations;
repeat1 begins in session93261, repeat2 follows. Native cache eviction/target
turnover, larger dimensions and whole multistart/DZ5 costs remain unproved.
