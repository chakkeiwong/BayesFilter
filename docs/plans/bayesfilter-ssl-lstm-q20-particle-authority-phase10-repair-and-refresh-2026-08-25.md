# Phase 10 Repair and Refresh Note

Status: `REFRESHED_PHASE11_LONGER_TRACE`

Classify failures as GPU policy, input hash/mode metadata, split/weight
alignment, batch-native update, target/status, parity, optimizer/tuning, and
only then candidate quality. A long-run loss or latent moment improvement is
descriptive until a predeclared untouched claim run supports it.

After each attempt record the arm configuration, update count, seed, hard-gate
receipts, validation/audit separation, latent diagnostics, and wall time. If
the ladder fails for tuning reasons, keep the audited measure and refresh a
representation or proposal-support diagnostic; do not call the direction
invalid. HMC remains outside the campaign.

## Executed disposition

All three 100-update arms passed hard gates. The compact arm improved from the
Phase 9 short-screen diagnostics to `max|mean|=0.9922` and covariance
off-diagonal maximum `0.6189`; the low-learning-rate arm remained poor. This
is evidence for an optimization component, not IID whitening. Refresh Phase
11 with a predeclared 300-update trace on the same audited bank.
