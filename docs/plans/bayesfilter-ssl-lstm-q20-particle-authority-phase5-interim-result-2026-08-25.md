# Phase 5 Interim Adjudication

Status: `INTERIM_REFRESH_TO_PHASE6`

Phase 5 reviewed the completed identity and mutation branches before deciding
whether the campaign could stop. It did not stop: the unresolved authority and
whitening findings are repair triggers, and the user instruction requires the
next discriminating phase.

## Ledgers

**Engineering.** Phase 1 contract tests, Phase 2/3/4 focused runners, and the
new GPU/XLA screen tests pass. All failed attempts have unique preserved
directories. The GPU run recorded memory growth before logical-device
initialization, XLA compilation, and no HMC launch.

**Numerical.** Known finite fixtures, target/status checks, affine parity,
random-walk transition-symmetry receipts, and transformed-target finite checks
pass. The finite fixture suite does not yet exercise the complete SMC-U
normalizing-constant estimator with its actual resampling/mutation sequence.

**Scientific.** The identity M0 and repaired mutation bank are candidate
particle clouds only. The mutation branch changes one-seed root/mode
diagnostics, but no uncertainty-supported ranking or mode-mass claim is
defensible. NeuTra hard mechanics pass, while latent whitening remains poor.

## Interim decision table

| Decision | Primary criterion | Veto status | Main uncertainty | Next action | Nonclaim |
|---|---|---|---|---|---|
| Continue to Phase 6 | repair triggers remain and no continuation veto fired | authority admission not met | actual SMC-U estimator identity and seed variability | exact algorithm fixture plus independent mutation seeds | no posterior/HMC/default claim |

## Inference status

| Evidence class | Status |
|---|---|
| Hard veto screen | no execution-wide hard veto; all executed terminal candidates are finite/status-valid |
| Statistically supported ranking | none; one-seed arms and mode fractions are underpowered |
| Descriptive-only differences | mutation versus identity root counts, mode fractions, ESS, loss, and whitening |
| Default-readiness | not ready; M0 and NeuTra remain role-limited candidates |
| Next evidence needed | exact SMC-U fixture, independent seeds, retained unnormalized mass, and target-specific NeuTra tuning |

The Phase 6 subplan is the refreshed next step. HMC remains outside this
campaign and cannot be launched by this interim note.
