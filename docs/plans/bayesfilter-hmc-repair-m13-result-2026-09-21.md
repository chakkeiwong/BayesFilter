# M13 preparation recovery and integration result

M13 completes its bounded engineering stage. Optional preparation recovery repairs
the matched M12 regression failure and three fresh regression fits pass their
predeclared posterior and uncertainty-aware reference screens. Four public model
tests need a resource continuation; they are not counted as successful tests.
The master program proceeds to M14 under the existing authorization.

The implementation restarts only a classified rejected nonfinite proposal from
an independently checked beginning-of-window state and its qualified coordinates.
It discards the entire failed attempt's adaptation statistics, archives its draws,
uses fresh streams, and qualifies a contracted step. Retained/shared corruption
remains fatal. The option defaults to zero restarts; experimental runs used three.
R-hat, posterior ESS and MCSE do not decide tuning membership or recovery.

Evidence is under `artifacts/hmc-repair-master-2026-09-16/m13-r1/`. Its terminal
reconciliation records exact commands, source hashes, run manifests, device policy,
tests and charges. CPU tests intentionally hid GPUs. Numerical fits used trusted
TensorFlow/TFP GPU/XLA with memory growth on shared GPU2; runtime comparisons are
descriptive. The matched fit is a development replay, not a fresh replication.

| Run | Outcome | Verified / recorded candidates | Recovery restarts |
| --- | --- | ---: | ---: |
| Matched M12 failure | Preparation passed, three metric updates | Not a tuning run | 1 |
| Fresh regression 0 | Posterior and all six reference screens passed | 23 / 100 | 0 |
| Fresh regression 1 | Posterior and all six reference screens passed | 29 / 100 | 0 |
| Fresh regression 2 | Posterior and all six reference screens passed | 35 / 100 | 1 |
| Public banana | Completed full pipeline | Preserved in inventory | 0 |
| Public Gaussian, beta-binomial, LGSSM, noncentered funnel | Each reached prepared geometry, then 400-second worker cap during candidate search | Partial checkpoints preserved | Not evidence of a numerical veto |

Fresh fit 2 discarded 900 transitions; no failed preparation draw entered a
posterior estimate. Every verified member remains retained; one member was selected
by identity before posterior assessment. Unassessed siblings are not implicitly
posterior-qualified. Three fits provide feasibility evidence, not a calibrated
success rate or grounds to change defaults.

The latest result for each distinct test identity is **467 passed, zero failed,
one inherited skip**. Earlier fixture/AST-locator failures were repaired and their
specific tests rerun; overlapping reports are not summed. Tests include six actual
TF/TFP target families for injected recovery and five public prepared-route models
through verification, export/reload, every-member assessment and idempotent resume.
Tiny CPU allocations test mechanics only. The disabled-option GPU replay exactly
matches M12's five latent tensor files, final transform and epsilon.

Two immutable source snapshots separate fit execution from final validator,
timeout-evidence and reporting fixes. The latter fixes have focused tests; the
final source also passes the disabled-option GPU replay. Terminal reconciliation
records all file differences rather than claiming every fit ran the final source.
No invalid artifact or active M13 worker was found.

The official book was rebuilt (566 pages), the changed recovery page rendered and
inspected, and the verified PDF installed at `docs/main.pdf`. Three pre-existing
missing bibliography keys remain assigned to M18; compilation is not citation
validation.

| Decision | Primary criterion | Veto status | Main uncertainty | Next action | Not concluded |
| --- | --- | --- | --- | --- | --- |
| Accept optional recovery engineering change | Classified recovery, corrupt-state rejection, accounting and no-fire parity pass | No unresolved engineering failure | Limited natural-failure replication | M14 metric/schedule comparison | Universal preparation reliability |
| Continue four timed-out cells | Preparation succeeded; search checkpoint exists | Resource cap, no recorded numerical veto | Remaining search/compile cost | Same-source, same-design resource continuation | Full matrix success |
| Continue broader validation | Three complete fresh reference fits pass | No posterior/reference veto in these fits | Small sample and one law | M15–M17 | Default promotion or superiority |

| Inference status | Finding |
| --- | --- |
| Hard veto screen | Fatal corruption tests pass; public resource caps remain failures |
| Statistically supported ranking | None |
| Descriptive-only differences | Candidate counts, step sizes, timing and three-fit outcomes |
| Default readiness | Not established; recovery remains optional |
| Next evidence | Metric/startup comparisons, replicated stopping/SBC and global geometry checks |

M13 charged 1579.7210649418412 CPU seconds (679.7210649418412 measured plus the
predeclared 900-second audit allowance) and 4295.071047700127 GPU worker seconds,
including unsuccessful attempts. Remaining: **147917.6080958957 CPU** and
**52683.15583304904 GPU seconds**. No budget reset or enlargement occurred.

Post-run red team: the strongest alternative explanation is that a contracted
restart happened to avoid the regression overflow on these seeds. A fresh failure
would weaken reliability, without invalidating the tested recovery invariants.
The weakest evidence is broad calibration; neither local step probes nor successful
preparation establish posterior equilibration. Four timeouts trigger resource
repair, not rejection of those targets or abandonment of subsequent phases.
