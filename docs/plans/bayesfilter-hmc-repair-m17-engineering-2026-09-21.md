# M17 integration preparation

While M15 uses immutable numerical source, add bounded CPU mechanics tests for
existing public transport and position-field routes. This is independent of
M15 outcomes; M17 research experiments still await the intervening phase
refreshes. Reserve at most 900 CPU worker seconds inside M17's 12,000-second
ceiling. Deliberately hide GPUs. No runtime algorithm or default is changed.

Exercise frozen dense-IAF payloads on Gaussian, banana and simplex targets
through public tuning, retention of all verified members, export/reload and
posterior warmup exclusion. The fixed nonzero weights come from the existing
artifact-loader fixture. They are a deterministic nonlinear mechanics case,
not trained transport or transport-quality evidence. Tiny counts and broad
acceptance bands are explicitly mechanics-only fixtures.

Exercise the public position-field entry point on Gaussian, beta-binomial,
LGSSM location and banana. Use a deterministic 0.8 multiple of the exact
potential gradient, retaining the exact endpoint potential. This explicitly
different force tests the conditional route; it must never acquire exact-score
retained-member authority. Pause after one work item, reload and complete the
declared bounded search. Preserve actual observations and verify unchanged
candidate identities/conditional authority. Do not require stochastic posterior
success as the mechanics-test criterion.

Skeptical review: a Gaussian-only transport check misses nonlinear target/map
composition and dimension-changing model coordinates. A call to a helper would
not test public dispatch or durable members. A nonexact force must not be routed
through the fixed-transport tuner. The chosen tests use the actual supported
entry points and distinguish these authorities. The 0.8 factor, fixed weights,
short counts and wide bands are inherited or convenience stress fixtures;
they cannot nominate numerical defaults. Existing independent density/score
oracles remain the mathematical checks. The scope passes for routine tests.
