# Phase 0: Registry, Topology Inventory, And Default Audit Subplan

Date: 2026-07-15

Status: `REVIEWED_ACTIVE_EXECUTION`

Program: `contract-e-tp-all-model-clean-xla-validation`

Review record: bounded Claude review round 1 returned `REVISE` on item-kind
schema and exact log/result/Phase 1 readiness handoff paths. The same subplan was
patched. Focused round 2 returned `VERDICT: AGREE`.

## Objective And Research Question

Freeze a clean-XLA-specific row registry that says which target scalar, model
adapter, feature recursion, derivative owner, compiled factory, source topology,
reference, and prior artifact actually exist for each master-program row. Audit
every inherited chart, feature family, lookahead, quadrature order, horizon,
dtype, threshold, and support assumption before implementation or new GPU work.

Question: which rows may enter functional-loop engineering now, which are
scientific negative results, and which lack the target or adapter needed to run
the program without substitution?

## Entry Conditions

- The LGSSM clean-XLA repair result is `PASS_CLEAN_XLA_COMPILE_EXPLOSION_REPAIRED`.
- The all-model master received Claude `AGREE` after structural-order,
  full-horizon derivative, fail-closed, and conditional-deliverable repairs.
- The owner authorized 96 CPU core-hours, 32 trusted GPU-hours, and three
  full-horizon attempts per eligible model.
- Contract E--Chol remains canonical. Contract E--TP remains experimental.
- The earlier gradient-comparison registry may supply target identity anchors,
  but its six comparison rows and statistical protocol do not determine
  clean-XLA eligibility or topology status.

## Exact Rows And Entry Classification To Verify

| Row | Expected Phase 0 classification |
| --- | --- |
| LGSSM `d_x=3,p=5,T=50` | `reference_pass`; exact Kalman oracle and repaired loop factory |
| actual non-Gaussian SV `d_x=1,p=2,T=1000` | `eligible_inventory_required` |
| KSC-SV `d_x=1,p=2,T=1000` | `eligible_inventory_required` |
| generalized SV `d_x=1,p=3,T=1008` | `negative_result`; tested feature family cannot be scaled |
| predator--prey `d_x=2,p=6,T=20` | `eligible_inventory_required` |
| Austria SIR `d_x=18,p=3,T=20` | `target_blocked`; target measure and total-score owners missing |
| structural deterministic fixture | `required_shared_regression`; not a complete model row |
| DSGE/NAWM client | `target_blocked`; executable client scalar/metadata absent |

These are hypotheses to verify against code and artifacts, not labels to force.
Every JSON entry has `item_kind`: the six statistical model entries and
DSGE/NAWM client use `model_row`; the structural deterministic fixture uses
`shared_regression_item`. Automation must not count the shared fixture as a
completed all-model row.

## Required Artifact

Create
`docs/benchmarks/artifacts/contract_e_tp_all_model_clean_xla_validation_20260715/phase-00/registry/attempt-01-20260715/phase0_registry.json`
with:

- program, schema, git commit, generated time, CPU-hidden intent, and source
  dependency hashes;
- the eight rows above with target identity anchor, horizon, state/parameter
  dimensions, current adapter/core/factory paths, derivative route, feature and
  support policy, reference, existing evidence, source-loop findings, default
  audit, classification, blockers, and next eligible phase;
- separate lists for compiled dynamic Python loops, fixed small Python loops,
  functional TensorFlow loops, offline/preparation NumPy/SciPy, and runtime
  NumPy/SciPy findings;
- explicit evidence roles and nonclaims;
- artifact hashes for the controlling LGSSM graph/GPU/fail-closed evidence and
  target registry where present;
- budget ledger initialized without charging historical experiments.

Preserve full check output at
`docs/benchmarks/artifacts/contract_e_tp_all_model_clean_xla_validation_20260715/phase-00/registry/attempt-01-20260715/phase0_checks.log`.
The exact close record is
`docs/plans/bayesfilter-contract-e-tp-all-model-clean-xla-validation-phase0-registry-topology-default-audit-result-2026-07-15.md`.

The builder must fail closed if a declared source or controlling artifact is
missing, a required row is absent or duplicated, an ineligible row is scheduled
for GPU, `0.05*sqrt(p)` is used outside same-scalar FD, or Contract E--TP is
marked canonical/default/HMC/leaderboard ready.

## Default And Assumption Audit

| Choice | Provenance | Status | Failure mode | Early diagnostic |
| --- | --- | --- | --- | --- |
| LGSSM finite lookahead 8/order 5/float64 | controlling repaired route | reference only, not cross-model default | silent feature transfer | every nonlinear row records its own unset or sourced choice |
| actual/KSC `T=1000` and predator `T=20` | frozen datasets/master registry | target horizons | stale dataset or time-order mismatch | bind generator/hash/time order before implementation |
| generalized-SV exclusion | prior progressive-feature evidence | scientific negative for tested family | mislabel infrastructure failure or overgeneralize to all features | cite exact result and limit the claim to tested family |
| SIR blocked state | clipped-push/Gaussian-density and derivative-owner audit | target blocker | run a different law under same row label | bind missing owners and forbid scheduling |
| structural fixture early | reviewed master repair | shared continuation guard | functional-loop work weakens singular support | Phase 2 exact support/tangent/off-support controls |
| topology ratios `1.10/1.25` | reviewed master engineering gates | reviewed topology guards | interpret as scientific tolerance | artifact labels them topology-only |
| `0.05*sqrt(p)` | owner FD policy | individual-direction same-scalar FD only | cross-method equivalence misuse | schema/test rejects any other role |

Unknown nonlinear lookaheads, quadrature orders, chart capacities, dtypes, and
parameter regions remain `unset_requires_phase_specific_audit`; they cannot be
filled from the LGSSM values for registry completeness.

## Evidence Contract

| Field | Phase 0 contract |
| --- | --- |
| Question | which exact rows and code paths can validly enter clean-loop work? |
| Baseline | reviewed master registry plus repository source and controlling artifacts |
| Primary criterion | all eight rows have source-backed identity, topology/default audit, legal classification, blocker, and next-phase status |
| Vetoes | wrong program registry, silent default transfer, missing source/artifact, proxy row, eligible GPU scheduling of a blocked/negative row, or unsupported readiness claim |
| Explanatory | counts of Python loops, existing prefix artifacts, and historical timings |
| Not concluded | implementation correctness beyond cited evidence, nonlinear clean XLA, scientific accuracy/equivalence, canonical/default/HMC/leaderboard readiness |
| Preserved artifact | clean-XLA Phase 0 JSON, quiet check log, phase result, and hashes |

## Implementation And Checks

1. Add a CPU-only reporting builder dedicated to this master program. It may
   use Python/JSON for inspection; it must not become a gradient-bearing route.
2. Add focused tests for schema, exact eight-row coverage, classification
   boundaries, no illegal defaults, source/artifact existence, FD-only policy,
   and GPU scheduling exclusions.
3. Run the builder with `CUDA_VISIBLE_DEVICES=-1` before any TensorFlow import.
4. Parse the JSON independently, verify referenced SHA-256 hashes, and run the
   focused Phase 0 test.
5. Run `python -m compileall` on the builder, `git diff --check`, and a bounded
   source audit for reachable Python time/lookahead/RK4/solver loops.
6. Write the Phase 0 result with direct classifications and budget use.
7. Draft Phase 1 from the actual inventory; do not assume a shared abstraction
   is warranted before the inventory is read.
8. Review Phase 1 for consistency, correctness, feasibility, artifact coverage,
   and boundary safety. Continue automatically only if the runbook ready gate
   passes.

No GPU command is required in Phase 0. Existing trusted LGSSM artifacts are
hashed as historical entry evidence, not rerun or charged to this campaign.

## Forbidden Claims And Actions

- Do not modify algorithmic paths in Phase 0.
- Do not reuse the gradient-comparison registry as if it were this program's
  topology registry.
- Do not schedule generalized SV, SIR, or DSGE/NAWM for GPU.
- Do not invent missing model metadata, target laws, derivative owners,
  features, charts, or tolerances.
- Do not call Contract E--TP canonical, default, HMC-facing, leaderboard-ready,
  scientifically accurate, or cross-method equivalent.
- Do not use a Python-loop source count alone as proof that a loop is dynamic or
  reachable from a compiled factory; classify reachability and loop role.

## Budget

Phase cap: 4 CPU core-hours, zero GPU-hours, zero full-horizon attempts. Phase 0
`minimum_entry_budget` is 1 CPU core-hour and its `repair_reserve` is 1 CPU
core-hour. The remainder is reserved for later implementation and trusted GPU
phases.

## Repair And Stop Conditions

Repair in phase if source paths moved, an artifact hash is stale but a preserved
controlling replacement exists, the builder schema is incomplete, or loop-role
classification needs a bounded source trace. Rerun focused checks after each
repair.

Stop only if target identity cannot be recovered without an owner scientific
decision, evidence is corrupted with no preserved replacement, required
inspection crosses an external/private boundary, or the Phase 0 CPU cap is
exhausted. A blocked model row is a valid Phase 0 output and does not stop the
other eligible rows.

## Exact Phase 1 Handoff

The required next artifact is
`docs/plans/bayesfilter-contract-e-tp-all-model-clean-xla-validation-phase1-shared-clean-loop-guardrails-subplan-2026-07-15.md`.
It must name only inventory-justified shared orchestration and guards, inherit
all eight Phase 0 item classifications, declare exact source/test/artifact
paths, preserve the LGSSM scalar/graph/fail-closed controls, declare
`minimum_entry_budget` and `repair_reserve`, and exclude nonlinear feature,
chart, target-law, and GPU campaign work.

Phase 0 closes only when:

- the required JSON and hashes pass all Phase 0 checks;
- the Phase 0 result classifies every row and separates negative, target-
  blocked, and engineering-eligible states;
- a Phase 1 subplan is refreshed from the inventory and names only justified
  shared primitives and static guardrails;
- that subplan preserves LGSSM scalar/graph/fail-closed regressions, does not
  implement nonlinear scientific features, and passes the runbook review gate.

The Phase 0 result and ledger must record `NEXT_PHASE_READINESS` with exact
evidence paths and `PASS` for each of these clauses: current result/check
consistency; legal inherited classifications; explicit target/scalar/
derivative/comparator/device/artifact identities; audited assumptions; declared
criteria/vetoes/diagnostics/nonclaims/fresh paths; executable commands that
answer Phase 1; no unresolved material finding or human boundary; and the
componentwise runbook budget calculation. Any failure enters repair or a true
stop classification; readiness cannot be asserted in prose alone.

On handoff readiness, enter Phase 1 `PRECHECK` immediately under the unchanged
campaign authorization.

## Skeptical Pre-Execution Audit

Status: `PASS_READY_FOR_BOUNDED_REVIEW`.

- Wrong baseline: the older gradient-comparison registry is an identity source,
  not this program's topology registry.
- Proxy promotion: loop counts and historical prefix artifacts are explanatory,
  not nonlinear clean-XLA admission.
- Missing stops: target identity, evidence corruption, external boundaries, and
  CPU cap are explicit; row blockers do not incorrectly stop the campaign.
- Hidden defaults: nonlinear lookahead/order/chart/dtype/regions remain unset.
- Stale context: the completed LGSSM loop repair is bound by exact artifact
  hashes; no historical compile result is rerun as new evidence.
- Environment: Phase 0 is deliberately CPU-hidden and uses no GPU result.
- Artifact sufficiency: the JSON, focused test, hashes, result, and reviewed
  Phase 1 handoff directly answer the inventory question.
