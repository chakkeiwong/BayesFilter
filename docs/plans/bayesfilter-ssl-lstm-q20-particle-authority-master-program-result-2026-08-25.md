# SSL-LSTM q=20 Particle Authority Master Program Result

Status: `COMPLETE_DIRECT_LEDH_ROUTE_BLOCKED_PHASE26`

The master program executed phases 0 through 26 with unique artifacts and
between-phase repair/refresh notes. The final Phase 26 receipt is:

`docs/plans/artifacts/ssl-lstm-q20-particle-authority-master-2026-08-25/phase26-attempt1/`

The terminal skeptical review is recorded in
`docs/plans/bayesfilter-ssl-lstm-q20-particle-authority-master-program-terminal-review-2026-08-25.md`.

## Final adjudication

| Decision | Primary criterion | Veto status | Main uncertainty | Next justified action | Not concluded |
|---|---|---|---|---|---|
| Fresh particle/modular evidence retained | phase-specific fixtures and target probes passed their stated hard gates | role and density gates prevent broad promotion | finite-run mode coverage and posterior uncertainty remain limited | use role-limited ETPF/SMC/NeuTra evidence in a separately reviewed continuation | no posterior correctness, IID whitening, exhaustive mode discovery, or HMC readiness |
| Direct q=20 LEDH closed | Phase 25 rank and Phase 26 measure ledger | full-state singular-support and target-binding veto | a different explicitly defined target might support another flow | require a new plan if target/measure is changed | no claim that all particle-flow ideas fail |

## Evidence ledgers

- Engineering: source-faithful ETPF, GenUT, and LEDH fixtures, q=20 callback
  audits, focused tests, source hashes, CPU/GPU policy receipts, and unique
  output roots are preserved.
- Numerical: ETPF row/column and moment receipts, GenUT feasibility failures,
  LEDH inverse/determinant identities, q=20 rank `20/60`, and reduced density
  residual `0` are recorded in their phase artifacts.
- Scientific: poor whitening, mode occupancy, ESS, and training metrics remain
  descriptive or role-limited. They never promoted a method to posterior or
  HMC authority.

## Inference-status table

| Evidence class | Final status |
|---|---|
| Hard veto screen | no unresolved harness failure; direct LEDH target-binding veto fired |
| Statistically supported ranking | none; runs were diagnostic/role-limited and not a multi-seed superiority study |
| Descriptive-only differences | whitening, covariance, ESS, mode occupancy, losses, and fixture spectra |
| Default-readiness | no new default; HMC explicitly deferred |
| Next evidence needed | a separately reviewed target/measure definition if LEDH is revisited |

## Post-run red team

The strongest alternative explanation is that a manifold-valued latent-state
filter or a parameter-space flow extension could be useful under a different
explicit target. That possibility does not make the current computed density
equal to the declared q=20 parameter posterior. The conclusion would be
overturned only by a common-measure, source-bound q=20 density/Jacobian
identity. The weakest evidence is the finite-run descriptive geometry; the
strongest blocker evidence is the exact dimension/rank and density ledger.

No HMC command was launched. No package installation, network fetch,
destructive operation, or public default change was performed.

## Budget ledger

The phase-table caps are explicitly local ceilings drawn from one global pool;
they are not additive. The 36 result manifests record `14780.9 s` of phase
wall time (`4.11 h`). Test and inspection overhead is not fully represented,
so this is a lower bound, but the executed campaign remained below the
user-authorized `64800 s` cap. No further phase is authorized without a fresh
remaining-pool check.

## Mathematical audit

The rank conclusion has the direct derivation

`rank(G Q G^T) <= min(rank(G), rank(Q), rank(G^T)) <= 20 < 60`,

for `G` in `R^(60 x 20)` and `Q` in `R^(20 x 20)`. Therefore the induced
full-state covariance cannot define a 60-dimensional nonsingular Gaussian
density, regardless of the observed eigenvalue tolerance. The measured
receipt supplies the corresponding finite target/covariance values and the
deterministic-residual check.

MathDevMCP was run on the terminal claim and code path. Its bounded outputs
classified the report claim as `needs_boundary_clarification` and the
math-to-code comparison as `scope_limited_match`, explicitly refusing to
certify a general theorem from the runner. That is the correct audit result:
the algebraic inequality above is the mathematical argument; the runner is
empirical instance evidence, not a proof engine. The result note preserves
that boundary and does not upgrade the diagnostic to a theorem about every
possible future target.

## Repair record

The terminal focused test suite initially exposed one stale role-boundary
assertion after the Phase 17 scaffold relabel. The test was updated to require
the explicit `symmetric_ut_sigma_point_auxiliary` and `not_genut` labels; the
affected test then passed, followed by the complete 46-test focused suite.
