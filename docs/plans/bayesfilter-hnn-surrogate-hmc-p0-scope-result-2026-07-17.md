# P0 Result: Scope, Identity, Defaults, And Budget Freeze

Date: 2026-07-17

Decision: `PASS_P0_SCOPE_IDENTITY_DEFAULT_BUDGET_FREEZE`

## Outcome

The five Tier A configurations are bound to readable, hash-verified target and
transport evidence. The later dedicated structural campaign is recorded as a
qualified historical supersession of the older multi-model
`COMPARATOR_BLOCKED_GEOMETRY` state. Its post-result ESS threshold change is not
inherited by this program.

All blocked NeuTra target/filter/source-route cells are excluded. Eight Tier B
configurations are frozen for later requalification. The historical
`DelayedAcceptanceHMC` execution is explicitly ineligible for transfer because
it lacks a separate stage-one accept/reject before applying the combined
correction ratio.

P1/P2 APIs, analytic-fixture grids, training hypotheses, seed domains,
environment, timeouts, and phase budgets are recorded in the control ledgers.
No model-specific HNN architecture or optimizer has been promoted as a global
default.

## Local Checks

- all five Tier A evidence and transport paths exist;
- source and result SHA-256 values match the target registry;
- the historical multi-model cell ledger hash matches
  `7e078b56a4fa71ac86d8dd7171825ff4d53d845e746d3276ac577b4640ebaf5d`;
- the four retrospective truth-tail cells pass their preserved health/tail
  contracts; structural evidence remains qualified and noncentral;
- all eight Tier B rows have 6-hour ceilings, summing to P6/P7's 48 GPU hours;
- phase budgets sum to 22 CPU and 120 GPU wall-hours;
- `jq` was unavailable; no dependency was installed and focused JSON text
  inspection was used instead.

## Decision Table

| Field | Status |
| --- | --- |
| P0 primary criterion | passed |
| Target identity replay | passed for five Tier A rows |
| Filtering boundary | exact Kalman versus named UKF/SGQF targets preserved |
| Historical structural conflict | resolved by explicit qualified supersession |
| Budget and attempt contract | passed |
| Main uncertainty | P2 force-training choices remain target-specific hypotheses |
| Next justified action | implement and test the standalone P1 kernel |
| Not concluded | no kernel, training, HMC, validity, truth, or performance admission |

## Inference Status

| Field | Status |
| --- | --- |
| Hard veto screen | clear for P0 documentation/identity scope |
| Statistically supported ranking | none |
| Descriptive-only differences | historical model diagnostics and runtimes |
| Default readiness | false |
| Next evidence | P1 map, energy, Gaussian, negative, batching, and GPU/XLA tests |

## Post-Run Red Team

Strongest alternative explanation: readable historical artifacts may still be
incompatible with the future P2 adapter implementation. P2 therefore requires
fresh target/chart substitution-negative tests and does not infer adapter
compatibility from P0 hashes alone.

The weakest evidence is the historical structural pass because its primary
bulk-ESS threshold changed after the run. That is why it remains qualified and
the new program requires a fresh hard `>=1000` gate.

## P1 Handoff Review

The P1 subplan was refreshed conceptually by freezing a new standalone module
and new tests, avoiding concurrent modifications in shared inference files.
The baseline, map, target-Jacobian, negative-test, GPU/XLA, and failure
contracts are feasible and complete. No real continuation veto fired.

Status: `CONTINUE_TO_P1`.
